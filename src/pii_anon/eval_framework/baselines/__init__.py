"""Vendored baseline benchmark artifacts.

Contains ``benchmark-results.json`` — the published pii-rate-elo
leaderboard snapshot for pii-anon, pii-anon-swarm, and the standard
competitors (Presidio, GLiNER, Scrubadub).  Shipped as package data so
:func:`pii_anon.eval_framework.load_baseline_leaderboard` works from a
wheel install without requiring a source checkout.

The artifact is regenerated by ``make benchmark-full`` and the CI
publish workflow copies it here before each release.
"""
