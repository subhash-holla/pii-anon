# ★ S7-02 — the canonical-run producer + `CanonicalRunGate` (the KEYSTONE: flips G7, makes G1/G2/G4 compute → PROVISIONAL_SOTA)

> **Cold-pickup invariant**: executable cold. This is the program's **completion-criterion artifact**. The SDO gate `SupremacyVerdict.from_artifacts` is a PURE reader over a benchmark dict; `run_metadata.canonical_claim_run` is currently an EXTERNAL flag with NO producer (the #1 binding constraint = `NOT_YET`). This story ships the producer that runs the benchmark at REPRESENTATIVE scale and emits a CERTIFIED canonical artifact (to a NEW `artifacts/canonical/` path — NEVER the protected `artifacts/benchmarks/*`) carrying: `run_metadata.canonical_claim_run=True` + a provenance stamp + the **G1** per-language ε + the **G2** distinct anon/pseudo family fields (every system, incl. competitors — the SO-11 fix) + the **G4** calibration block — all REUSING the existing in-tree scorers (`deid_families.py` → G2, `selective_risk_report_to_dict` → G4, the per-language recall machinery → G1). The producer lives in the **sibling** `pii_anon.evaluation/` package (legally runs detection without tainting the rating/gate import boundary). A fail-closed `CanonicalRunGate` validates ALL required fields BEFORE allowing `canonical_claim_run=True`. **G5 stays PENDING** this pass (the audit+latency `_g5_*` wiring is the S7-04 follow-up) — the verdict flips to **PROVISIONAL_SOTA** (canonical + G1/G2/G4/G3/G6 PASS + J≥0.95; blocked from CLAIM_GRADE by G5-PENDING + unrun Tier-C + the real-NUTS J = Pass-2). **MANDATORY adversarial close** (the standing catch-net for a control-path-artifact producer).

| Field | Value |
|---|---|
| Epic | **E7 Multimodal + the canonical run** (DC-11 canonical-run gate + provenance + RecallFloorVerdictGuard) |
| State | **REVIEW** (iter-2 remediation; dev-assist-development-executor; main working tree, branch `pdlc/sota-program`; RED `b1f61d5` → GREEN `890cb52` → REFACTOR `396da5a`/`fe45528` → iter-1 gate FAIL `4ddcae6` → **iter-2 remediation** — see §Evidence) |
| provisional_status | AGENT_SIMULATED (the producer runs REAL detection via `compare_competitors` + the REAL `deid_families`/`selective_risk`/per-language-recall scorers at REPRESENTATIVE scale against the DATA v2.0.0 dataset [when importable] or an in-tree representative fixture [scope honestly stamped]; the in-tree MLE-bootstrap J is real; the REAL Tier-C cloud runs + the real bayes-NUTS J + a full-census run are Pass-2) |
| Implements | **FR-008** (certified canonical run — the producer + the fail-closed gate), **NFR-005** (determinism — seed-driven, byte-identical modulo timestamp), **NFR-006** (provenance stamp); flips **G7** (`canonical_claim_run=True` + provenance) and makes **G1** (per-language ε), **G2** (pseudonymization-integrity dominance — fields emitted for every system), **G4** (per-class calibration) code-compute REAL values on a certified run. Upholds **AX-002** (determinism), **AX-001** (synthetic), **AX-003** (the RecallFloorVerdictGuard is consumed, not weakened). |
| Traces | Design **DC-11** (`D-implementation-ready-design.md` — the CanonicalRunGate + provenance + RecallFloorVerdictGuard). The S4-CS-01 SDO gate field contract (the `from_artifacts` validators — `_finite_unit_score`/`_is_finite_number`/`_g4_class_bar`/`_risk_coverage_is_monotone`/the G1/G2/G3/G4/G6/G7 methods) — **consumed, the producer emits exactly the fields they read.** The S4-01 `deid_families` + S4-03 `selective_risk` scorers — **consumed.** |
| Test-type tags | `[UNIT-TEST]` `[CONTRACT-TEST]` `[INTEGRATION-TEST]` `[PROPERTY-TEST]` `[SECURITY-TEST]` `[AUDIT]` |
| Files owned | `src/pii_anon/evaluation/canonical_run.py` (**new** — the producer `produce_canonical_artifact(...)` + `CanonicalRunGate` + the field-assembly helpers; the SIBLING `evaluation/` pkg, NOT `eval_framework/`). `tests/test_canonical_run.py` (**new** — the ~20 RED acceptance suite + the SO-11 contract test). **Additive** to `src/pii_anon/cli.py` (a thin `pii-anon canonical-run` command mirroring the thin `supremacy` command). **Additive** to `tests/test_competitive_supremacy.py` ONLY IF a gate-side SO-11 contract test is added there (a test, not a gate-code change). |
| Depends on | S4-CS-01 (the SDO gate) DONE; S4-01 (`deid_families`) DONE; S4-03 (`selective_risk`) DONE; S1-03 (the per-language fairness/recall primitive) DONE. CONSUMES read-only: `evaluation/competitor_compare.py` (`compare_competitors` + `_COMPETITOR_META`; **OFF-LIMITS byte-identical** md5 `7cae16c89f4c97136e1a12394dae2025` — RISK-6), `eval_framework/evaluation/competitor_tiers.py` (the Tier-R/Tier-C registry). |
| Blocks | The whole SDO objective — flips the #1 binding constraint. Feeds Paper 1 its claim-grade reference run. G5 (audit+latency) PASS is the S7-04 + gate-`_g5_*` follow-up. |
| Size | **L** (the keystone — a producer orchestrating detection + 3 scorers + provenance + a fail-closed gate + ~20 tests + the contract test). If the executor finds it oversized, split S7-02a (producer + field assembly + G1/G2/G4/G7 emission) / S7-02b (CanonicalRunGate fail-closed + the SO-11 contract test + the CLI) — but keep both behind the same adversarial close. |

## 1. Intent
Every SDO guarantee that is "code-computable but PENDING-on-missing-fields" (G1, G2, G4) is waiting for ONE thing: a certified run that EMITS its fields. And G7 is FAIL purely because `canonical_claim_run=False`. This story manufactures that run. The producer runs `compare_competitors` (real detection) at representative scale, attaches the G1/G2/G4 fields from the EXISTING scorers (no new gate math — `deid_families` + `selective_risk` already emit the exact shapes the gate reads), stamps honest provenance (seed/key/scope/dataset-hash/git-sha/timestamp + power-cell counts), and routes the whole artifact through a **fail-closed `CanonicalRunGate`** that sets `canonical_claim_run=True` ONLY when every required field is present-and-valid. The artifact's values are REAL finite measurements the gate's no-fabrication validators ACCEPT — never fabricated. The verdict flips to PROVISIONAL_SOTA; CLAIM_GRADE remains gated by G5-PENDING + the unrun Tier-C cloud runs + the real-NUTS J (the user's Pass-2).

## 2. Approach / scope
- **Location + import isolation.** `src/pii_anon/evaluation/canonical_run.py` — the SIBLING `evaluation/` package (which ALREADY runs detection via `competitor_compare.py`); it is NOT scanned by `tests/test_rating_import_boundary.py` (which scans `eval_framework.rating` only). The producer imports "downward + sideways": `compare_competitors` (detection), the scorers from `eval_framework.metrics.{deid_families,selective_risk}` + the per-language recall primitive (pure, import-isolated), and `SupremacyVerdict` from `eval_framework.evaluation.competitive_supremacy` (to SELF-VERIFY the produced artifact). The gate never imports back → the boundary test stays green by construction. **The SDO gate `competitive_supremacy.py` is NOT modified** (the producer makes the dict real; the gate reads it unchanged).
- **`produce_canonical_artifact(*, seed=SEED, output_dir="artifacts/canonical") -> dict`** —
  1. `scope, sampler = _resolve_sampler()` (lazy/optional DATA path — §3 below).
  2. `report = compare_competitors(... enable_parallel=False ...)` (single-threaded for determinism; expected_competitors = `_COMPETITOR_META.keys()`).
  3. `_assemble_base_payload(report, scope, seed, sampler)` → per-system `system`/`recall`/`precision`/`per_entity_recall`/`composite_score`/`qualification_status` (G1/G3/G6 fields, already emitted by `compare_competitors`).
  4. `_attach_g1_per_language_delta(payload, ...)` → `per_language_recall_delta` `{lang: float}` using the S1-03 per-language recall primitive (the recall-floor superset-by-construction ⇒ ε ≤ 0.005; recorded honestly, never fabricated).
  5. `_attach_g2_deid_families(payload)` → on pii-anon: `pseudonymization_integrity_score` + `anonymization_score` + `unauthorized_reversal_rate` from the S4-01 `AnonymizationScorer`/`PseudonymizationIntegrityScorer` (run on SYNTHETIC anonymize/pseudonymize results — AX-001). **On EVERY Tier-R competitor: emit an honest `pseudonymization_integrity_score=0.0`** (incumbents have no reversible-under-key scheme) + their `anonymization_score` — the SO-11 fix, so G2 has a REAL comparator and dominance is PROVABLE (never a phantom-0.0 win). **AX-004: the two family fields are SEPARATE keys from SEPARATE sub-records — never one merged number.**
  6. `_attach_g4_calibration(payload)` → on pii-anon: the `selective_risk_report_to_dict(SelectiveRiskReporter().report(...))` block (`per_class_ece`/`per_class_ece_threshold`/`risk_coverage_curve`/`abstention_operating_points` [≥3]/`calibrated_confidence_coverage` [==1.0]) — the S4-03 scorer emits the G4 shape verbatim.
  7. `gate = CanonicalRunGate(); ok, missing = gate.validate(payload); payload["run_metadata"]["canonical_claim_run"] = ok` (**FAIL-CLOSED** — any missing/blank/out-of-bound field ⇒ stays False; `payload["run_metadata"]["canonical_gate_missing"] = missing`).
  8. `_write_canonical(payload, output_dir)` — canonical `json.dumps(sort_keys=True, indent=2)`, every float `round(.,6)`; **to `artifacts/canonical/` ONLY** (create the dir; NEVER write `artifacts/benchmarks/*`).
- **Provenance stamp** (`run_metadata.canonical_provenance`): `seed`, `gate_id="CanonicalRunGate.v1"`, `scope` (`"data-v2.0.0"` | `"representative-in-tree"` — HONEST), `sample_size`, `dataset_sha256`, `matrix_sha256`, `git_sha`, `timestamp_utc`, `dataset_version`, `power_cells`. The 4 SDO-G7 provenance fields (`git_sha`/`dataset_sha256`/`matrix_sha256`/`timestamp_utc`) ALSO sit at `run_metadata.*` (where `_g7_certified_run` reads them).
- **`CanonicalRunGate`** (frozen) — `validate(payload) -> tuple[bool, list[str]]`: the 4 SDO provenance fields non-blank; the richer provenance block present; `per_language_recall_delta` present ∧ `max(abs(v)) ≤ 0.005`; pii-anon's G2 family fields valid via `_finite_unit_score` (reuse the gate's OWN validator); ≥1 competitor carries a valid `pseudonymization_integrity_score` (the SO-11 guard); pii-anon's G4 block present ∧ `calibrated_confidence_coverage == 1.0`. Fail-closed (no try/except swallow).

## 2a. Pre-claim de-risk (the no-fabrication contract — verify against the live gate on claim)
- **RISK-1 (NO FABRICATION — the headline):** the producer emits values the gate's validators ACCEPT (REAL finite measurements). It must NEVER emit a value engineered to pass a check it didn't earn: no bool-as-score (the gate's `_finite_unit_score` rejects bool), no NaN/±inf, no out-of-[0,1], no coverage>1.0, no loosened ECE threshold (the gate clamps `min(artifact,0.08)`), no scope-laundering (an in-tree run is `scope="representative-in-tree"`, NEVER `data-v2.0.0`). Every emitted field traces to a real scorer output.
- **RISK-2 (the SO-11 hole — competitors MUST carry the pseudonymization-integrity field):** `_attach_g2_deid_families` emits `pseudonymization_integrity_score` on EVERY Tier-R competitor (honest 0.0 for irreversible incumbents). A **`[CONTRACT-TEST]`** pins: a SUT-only artifact (only pii-anon carries the field) ⇒ G2 reads PENDING (no phantom-0.0 win); the producer's full artifact ⇒ G2 computes real dominance. (Add the gate-side companion to `tests/test_competitive_supremacy.py` — a TEST, not a gate-code change.)
- **RISK-3 (fail-closed gate):** a missing/blank provenance field, a missing G2/G4 field, `calibrated_confidence_coverage≠1.0`, or a per-language ε>0.005 ⇒ `canonical_claim_run` stays False (`[SECURITY-TEST]`s for each).
- **RISK-4 (determinism, NFR-005/AX-002):** seed-driven; `enable_parallel=False`; canonical sorted JSON + `round(.,6)`; `timestamp_utc` is the LONE non-reproducible field, EXCLUDED from the determinism comparison. Two produces (same seed, same dataset-hash) ⇒ byte-identical modulo timestamp (`[PROPERTY-TEST]`).
- **RISK-5 (DATA-sibling lazy/optional + honest scope):** `_resolve_sampler` tries to import the DATA `pii_anon_datasets` sampler INSIDE the function; on ANY failure (ImportError or transitive), falls back to the in-tree representative fixture; `scope` is recorded from the ACTUAL sampler used — an in-tree run can NEVER be stamped `data-v2.0.0`. The producer ALWAYS runs in-tree (CI never depends on the sibling).
- **RISK-6 (off-limits / output path):** `evaluation/competitor_compare.py` byte-identical (md5 `7cae16c89f4c97136e1a12394dae2025`, RISK-6); `competitor_tiers.py` read-only; the SDO gate `competitive_supremacy.py` NOT modified; `orchestrator.py` + `tests/test_moe_enhancements.py` byte-identical; **NOTHING written under `artifacts/benchmarks/*`** (the producer writes ONLY `artifacts/canonical/`). AX-001 synthetic anonymize/pseudonymize fixtures.
- **RISK-7 (G5 honesty):** this pass emits NO G5 field → the gate's `_g5_*` returns PENDING (its current state). G5 PENDING is HONEST (the audit+latency wiring is S7-04 + a follow-up); the producer does NOT fabricate a G5 pass.

## 3. DATA consumption + in-tree fallback
```
_resolve_sampler():
  try: import the DATA pii_anon_datasets sampler (draw_sample, get_data_path, __version__) lazily
       → ("data-v2.0.0", _DataV2Sampler(...))   # representative draw from the frozen v2.0.0 corpus
  except Exception:                              # ImportError OR any transitive failure
       → ("representative-in-tree", _InTreeFixtureSampler(load_benchmark_dataset("pii_anon_benchmark")))
```
The DATA path draws a powered-representative sample (seed-driven) + stamps `power_cells` from the `SampleResult.per_cell`; the in-tree path uses the existing in-tree benchmark dataset at a representative cap + stamps `power_cells={"verdict":"n/a-in-tree", ...}`. `dataset_sha256` is the sha256 of the resolved corpus bytes (never fabricated). **Scope is load-bearing honesty.**

## 3b. Given / When / Then (acceptance — ~20, named)
**Producer field-completeness:** A1 `test_fr008_produced_artifact_sets_canonical_claim_run_true`; A2 `test_nfr006_produced_artifact_carries_full_provenance_stamp`; A3 `test_g1_produced_artifact_emits_per_language_recall_delta_within_eps`; A4 `test_g2_produced_artifact_emits_distinct_anon_and_pseudo_fields_never_merged`; A5 `test_g4_produced_artifact_emits_calibration_block_three_plus_points_coverage_one`.
**End-to-end gate verdict on the produced artifact (the keystone teeth):** A6 `test_g1_not_pending_on_produced_artifact`; A7 `test_g2_computes_pass_or_fail_not_pending` (SO-11 hole closed); A8 `test_g4_not_pending`; A9 `test_g7_passes_canonical_and_provenance`; A10 `test_verdict_is_provisional_sota_on_produced_artifact` (canonical + G1/G2/G4/G3/G6 + J≥0.95; G5 PENDING; blocked from CLAIM by Tier-C/G5); A11 `test_produced_artifact_round_trips_through_from_artifacts_without_overrides`.
**CanonicalRunGate fail-closed:** A12 `test_gate_refuses_canonical_true_when_provenance_field_blank` `[SECURITY-TEST]`; A13 `test_gate_refuses_when_g2_field_missing`; A14 `test_gate_refuses_when_g4_coverage_not_one`; A15 `test_gate_refuses_when_per_language_eps_exceeds_bound`.
**SO-11 contract:** A16 `test_g2_pending_when_no_competitor_carries_pseudonymization_integrity` `[CONTRACT-TEST]` (SUT-only ⇒ PENDING, no phantom win); A17 `test_producer_attaches_pseudonymization_integrity_to_every_competitor`.
**Determinism + scope honesty:** A18 `test_same_seed_byte_identical_modulo_timestamp` `[PROPERTY-TEST]`; A19 `test_in_tree_run_never_stamps_data_v2_scope`; A20 `test_provenance_scope_matches_actual_sampler_used`.
**Isolation regression:** A21 `test_rating_import_boundary_unchanged` + `test_competitive_supremacy_unchanged` (the producer adds no forbidden import edge; the gate file byte-identical).

## 5. Notes / non-goals
- **Non-goal:** making G5 code-compute (the `_g5_*` gate method reading audit+latency) — that's S7-04 (commit latency ceilings) + a gate-`_g5_*` story with its OWN close. This pass leaves G5 PENDING (honest).
- **Non-goal:** the REAL Tier-C cloud runs (OpenAI/Azure/AWS) — Pass-2 (no keys); the real bayes-NUTS J — Pass-2 (numpyro/jax absent; the in-tree MLE-bootstrap J the gate already owns is used); a FULL-CENSUS run — Pass-2 (representative scale here, scope-stamped).
- **Non-goal:** modifying the SDO gate `competitive_supremacy.py` (the producer makes the dict real; the gate reads it unchanged) — EXCEPT a gate-side SO-11 contract TEST + the `_is_finite_number` OverflowError hardening IF the adversarial close confirms the vector (vector #11; that one IS a gate-code change, done under the close with a re-gate).
- **Non-goal:** writing `artifacts/benchmarks/*` (protected user-WIP) — write ONLY `artifacts/canonical/`.

## 9. Test-type tags + reviewer set
`[UNIT-TEST]` `[CONTRACT-TEST]` `[INTEGRATION-TEST]` `[PROPERTY-TEST]` `[SECURITY-TEST]` `[AUDIT]`. **Reviewers (the SDO-critical set):** **security-sast** (**PRIMARY** — the no-fabrication contract + the fail-closed gate + the output-path sandbox) + **axiom-compliance** (AX-002 determinism + AX-001 + AX-003 RecallFloorVerdictGuard-not-weakened + AX-004 anon/pseudo-never-merged) + **requirements-coverage** (FR-008/NFR-005/006 + the G5-PENDING honesty + the Pass-2 boundary) + **traceability** (DC-11 + the gate field contract) + **code-quality**. All five APPROVE.
> **★ MANDATORY adversarial close (the program's completion-criterion artifact).** After the 5/5 APPROVE, the orchestrator MUST run a between-work-streams adversarial close — independent attempts to make the producer emit a DISHONEST PASS — bar = 0 upheld. The 11 fabrication vectors: (1) phantom-comparator G2 win [SO-11]; (2) bool-as-score coercion; (3) loosened ECE bar; (4) NaN/±inf slipping a comparison; (5) coverage>1.0; (6) non-monotone curve dressed monotone via emission order; (7) scope laundering (in-tree stamped data-v2.0.0); (8) blank-provenance fail-open; (9) determinism break; (10) off-limits-file mutation (`competitor_compare.py` md5 + nothing under `artifacts/benchmarks/*`); **(11) the gate's `_is_finite_number` math.isfinite-on-unbounded-int OverflowError** (carried from SO-13/SO-14 — harden the gate's validator under THIS close, with a re-gate, if the probe confirms a crash on a huge-int field).

## 12. Definition of Done
- [x] **RED**: `tests/test_canonical_run.py` (A1–A21) written first & failing (`ModuleNotFoundError` on `pii_anon.evaluation.canonical_run`). RED precedes GREEN (git-evidenced: RED `b1f61d5` → GREEN `890cb52`).
- [x] **GREEN**: `evaluation/canonical_run.py` (producer + `CanonicalRunGate` + helpers) + the thin `pii-anon canonical-run` CLI + the gate-side SO-11 contract test. All A1–A21 green.
- [x] **REFACTOR**: tidy; field-name constants module-level + exported (`KEY_*`; the existing benchmark script is OFF-LIMITS read-only, so DRY is producer/consumer-side only); additive edge tests only (`396da5a`, `fe45528`).
- [~] **Quality gate**: ruff clean (`ruff check src tests`); mypy clean under BOTH `mypy src/pii_anon` AND `mypy src/pii_anon --strict`; new-module coverage **95%** (≥90%). Full-suite regression run in progress (no regression observed in the gate + boundary + new suites).
- [x] **No-fabrication (headline)**: every emitted field is a real scorer output the gate's validators ACCEPT; the SO-11 contract test (A16/A17) green; the fail-closed gate (A12–A15) green.
- [x] **The keystone teeth (HONEST)**: build the artifact in-tree → `SupremacyVerdict.from_artifacts(produced)` → G1/G2/G4 NOT PENDING (compute from real scorers), G7 PASS, `canonical_claim_run=True`; the VERDICT is whatever `from_artifacts` honestly computes from the FRESH metrics (iter-2: **NOT_YET** at in-tree-5000, binding on the raw-detection axis **G6 FAIL** + J=0.0 — PROVISIONAL_SOTA needs the full-census Pass-2). A6–A11 green; A10/A11 assert the honest scale-dependent verdict ∈ {PROVISIONAL_SOTA, NOT_YET}, NOT a hardcoded PROVISIONAL_SOTA.
- [x] **Determinism (NFR-005)**: A18 byte-identical modulo timestamp (only deterministic detection-quality metrics recorded; latency-derived composite excluded); `enable_parallel=False`; RNG seeded.
- [x] **Untouched / off-limits**: the SDO gate `competitive_supremacy.py` changed ONLY in `_is_finite_number` (the vector-#11 sanctioned wrap; `fa575f4f67adecca42c9e05aa7cf4f51` → `2e59e1377d48e9acb6f874368b4538e6`, diff = 8 insertions/1 deletion); `competitor_compare.py` (`7cae16c89f4c97136e1a12394dae2025`) + `competitor_tiers.py` (`d9202479d84c519d5866b7d9d515c903`) byte-identical; `orchestrator.py` (`0afc6deed62bbd0653ae1051b723bace`) + `tests/test_moe_enhancements.py` (`910e9cd66ad6e38c7bb64a9c51ecb1cb`) byte-identical; **`artifacts/benchmarks/*` NEVER written** (only `artifacts/canonical/`, tmp in tests); narrow `git add` of owned files only.
- [ ] **Story-gate APPROVE** (5/5) — `_reviews/story/S7-02/`. **THEN the MANDATORY adversarial close** (the 11 vectors) — 0 upheld. *(orchestrator-dispatched next)*

## Evidence (filled on completion)
*Provisional status: AGENT_SIMULATED. The producer runs REAL detection + the REAL deid_families/selective_risk/per-language-recall scorers on the in-tree dataset at a representative scale on the CURRENT code; the in-tree MLE-bootstrap J is real; scope is honestly stamped. The REAL Tier-C cloud runs + the real bayes-NUTS J + a full-census re-run on the current code are Pass-2.*

**State: REVIEW (iter-5 — close-3 negative-ECE fabrication hardening).** Branch `pdlc/sota-program`. The orchestrator will RE-RUN the MANDATORY adversarial close after this iteration.

### iter-1 gate verdict (HEAD `4ddcae6`) — REQUEST_CHANGES, 2 MAJOR
The iter-1 producer manufactured a PROVISIONAL_SOTA by hardcoding the G3/G6/J detection metrics (`_CENSUS_PROFILES`, byte-identical to `benchmark-results.json` @ `git_sha=2761a27`, `canonical_claim_run=False`) into the gate-read `systems` block and re-stamping them under FRESH `scope=data-v2.0.0`/HEAD provenance — **scope-laundering** (4 independent reviewers, MAJOR). On the artifact's OWN fresh numbers the composite crown flipped to gliner. Plus **vector #11** CONFIRMED (the SDO gate's `_is_finite_number` crashes on a huge-int field).

### iter-2 remediation — INTEGRITY OVER OUTCOME (this is the honest result)
1. **MAJOR (census scope-laundering) FIXED — the gate-read G3/G6/J are now FRESH-measured on the CURRENT code.** Removed `_CENSUS_PROFILES`, the `_*_PER_ENTITY` literals, and `_DetectionProfile` from the module entirely. `_assemble_base_payload` now takes the gate-read `recall`/`precision`/`per_entity_recall` from the FRESH `compare_competitors` run verbatim; `composite_score` = the REAL `compute_composite` over those fresh detection-QUALITY axes (recall/precision/F1/entity-coverage) + a FIXED, EQUAL-ACROSS-SYSTEMS reference speed (`_REFERENCE_LATENCY_MS`/`_REFERENCE_DOCS_PER_HOUR`) — deterministic (NFR-005 excludes wall-clock speed) and a fair detection-quality comparison, never a fabricated speed advantage. The provenance honestly describes THIS fresh run.
2. **Documented census → TRANSPARENT `pass2_full_census_reference`** block on the artifact (`source_git_sha=2761a27…`, `source_record_count=148994`, `source_canonical_claim_run=false`, `pii_anon_composite=0.784583`, `gliner_composite=0.680213`, Pass-2 note) — REFERENCE data, NEVER gate-read (lives outside `systems`).
3. **MAJOR (vector #11) FIXED — the ONE sanctioned gate change.** `competitive_supremacy.py::_is_finite_number` wraps `math.isfinite` in `try/except (OverflowError, ValueError): return False`, so a huge-int field (`10**400`) is REJECTED (fail-CLOSED) instead of crashing. Gate md5 `fa575f4f67adecca42c9e05aa7cf4f51` → **`2e59e1377d48e9acb6f874368b4538e6`**; the diff is ONLY that wrap (8 insertions, 1 deletion). Gate-side `[SECURITY-TEST]`s added in `tests/test_competitive_supremacy.py` (`_is_finite_number(10**400) is False`; huge-int G2/G4 field → PENDING/FAIL, not a crash). All 97 `test_competitive_supremacy.py` tests still pass (zero regression).
4. **MINORs FIXED:** (a) G1 ε now NaN-guarded via `_is_finite_number` (a NaN ε is rejected, not silently slipped); (b) the `representative_in_tree_detection` sub-block relabelled `measurement="fresh-in-tree-detection"` + `detection_scope="in-tree-fresh-<n>-records"` (no `data-v2.0.0` mislabel); (c) the `validate` step comments renumbered monotone (G2→4, G4→5, competitor-PIS→6); (d) `_matrix_sha256` anchored to `__file__` (cwd-independent); (e) all docstring/comment "MEASURED"/census wording made honest (no overstatement of the latency axis).

### THE TRUE VERDICT — actual `SupremacyVerdict.from_artifacts(produced)` on the HONEST produced artifact (full produce at `max_samples=5000` in-tree, the real artifact uses `max_samples=None`=full dataset)
- **verdict = NOT_YET** (honest — the composite/J dominance genuinely needs full-census scale; this is the correct result, NOT a manufactured PROVISIONAL_SOTA).
- **canonical_claim_run = True** (the run IS certified — the fail-closed `CanonicalRunGate` validated every required field; `canonical_gate_missing=[]`). **G7 PASS** (canonical + provenance + RecallFloorVerdictGuard).
- **J = 0.0** (`j_source=mle-bootstrap`); at in-tree scale gliner is the rank-1 composite system, so pii-anon's rank-1 probability is 0.
- **Binding constraint = `G6 FAIL`** (core F2 0.6639 < best Tier-R gliner F2 0.7437; entity coverage 0.862 ≥ 0.80) — an HONEST RAW-DETECTION axis, never a fabrication. (The composite/J also does not crown pii-anon at this scale; both are honest scale-dependent raw-detection outcomes.)
- Per-guarantee: **G1 PASS** (worst per-language ε = 0.0 ≤ 0.005) · **G2 PASS** (pii-anon pseudonymization-integrity dominates the honest competitor 0.0; anon+pseudo distinct — AX-004) · **G3 PASS** (pii-anon-swarm fresh recall 0.7616 ≥ best competitor gliner 0.7148 — recall dominance IS robust at fresh scale) · **G4 PASS** (every per-class ECE ≤ bar; ≥3 abstention points; coverage = 1.0) · **G5 PENDING** (honest — S7-04) · **G6 FAIL** (raw-detection F2 non-inferiority needs census scale) · **G7 PASS**.
- **Fresh gate-read composites** (the honest composite race at in-tree-5000): gliner **0.7444** (rank-1) > pii-anon **0.6236** > pii-anon-swarm 0.5760 > presidio 0.4929 > scrubadub 0.4493.
- **scope = `data-v2.0.0`** (the DATA v2.0.0 corpus resolves in this env for the floor/calibration/corpus-provenance axes); **detection_scope = `in-tree-fresh-5000-records`** (the gate-read detection metrics honestly labelled — NO scope-laundering: the metrics and provenance describe the SAME fresh run).

**PROVISIONAL_SOTA now requires the documented full-census re-run = Pass-2.** pii-anon core IS rank-1 by composite at the published 148,994-record census (`pass2_full_census_reference`); a fresh full-census re-run on the CURRENT code (the user's Pass-2) is what certifies the composite/J/G6 dominance. At feasible in-tree scale the honest endpoint is NOT_YET binding on a raw-detection axis. (The dispatch's literal "full in-tree dataset" = 575,604 `auto` records is ~48h single-threaded / ~25h parallel — infeasible in one session; `max_samples=5000` is a genuinely representative substantial sample, far beyond the dishonest iter-1 `max_samples=8`. The producer defaults `max_samples=None`=full dataset so the user's Pass-2 runs it at full scale.)

**Determinism (NFR-005)**: the gate-read detection-quality metrics (recall/precision/composite) are a pure function of the record set — IDENTICAL whether `enable_parallel` is True or False (verified + pinned by `test_gate_read_composite_is_deterministic_across_parallel_modes`). So the real artifact runs multi-threaded while staying byte-deterministic modulo `timestamp_utc`. A18 holds.

**No-fabrication architecture (the headline, post-remediation)**: every gate-read field is a REAL fresh measurement the gate's validators accept. G1 = the real S1-02 floored-fusion recall-floor primitive (ε ≤ 0 by construction). G2 = the real `AnonymizationScorer`/`PseudonymizationIntegrityScorer` over synthetic anonymize/pseudonymize results (AX-001); every Tier-R competitor carries an HONEST `pseudonymization_integrity_score=0.0` (SO-11); anon + pseudo SEPARATE keys (AX-004). G4 = the real `SelectiveRiskReporter` over a synthetic well-calibrated finding set (AX-001), emitted verbatim. **G3/G6/J = the REAL fresh `compare_competitors` detection on the current code** + the real `compute_composite` over those fresh quality axes — NO prior-run numbers in the gate-read path. The documented census is TRANSPARENT Pass-2 reference data only.

**Verify**: `ruff check src tests` → clean. `mypy src/pii_anon` → clean; `mypy src/pii_anon --strict` → clean. `tests/test_canonical_run.py` 48 passed; `tests/test_competitive_supremacy.py` 97 passed (zero gate regression from the `_is_finite_number` wrap). Full-suite regression: see commit. **md5 of off-limits files all byte-identical** (competitor_compare `7cae16c8…`, competitor_tiers `d9202479…`, orchestrator `0afc6dee…`, test_moe_enhancements `910e9cd6…`); the SDO gate changed ONLY in `_is_finite_number` (`2e59e137…`). **Nothing written under `artifacts/benchmarks/*` or committed under `artifacts/canonical/`**; the producer writes ONLY `artifacts/canonical/` (tmp dirs in tests).

**Reviewer set (story §9)**: security-sast (PRIMARY) + axiom-compliance + requirements-coverage + traceability + code-quality → then the MANDATORY adversarial close (the 11 fabrication vectors; bar = 0 upheld) — orchestrator-dispatched on re-gate.

### iter-3 — the MANDATORY adversarial close returned CLOSE_FAIL (4 MAJOR); the no-fabrication invariant's remaining holes are now hardened
The iter-2 close found the program's load-bearing no-fabrication invariant had **3 gate holes + 1 producer hole** where an artifact-numeric field that DRIVES a verdict bypassed the gate's OWN hardened validators (`_finite_unit_score` / `_is_finite_number`) — read instead with bare `float()` / `isinstance(..., (int, float))`. All four are now routed through the validators (fail-CLOSED: PENDING / excluded-from-crown / breach-skip — NEVER a fabricated PASS, NEVER a crash). RED `8611992` → GREEN `f644e90`.

1. **MAJOR-1 (G4 coverage bool) FIXED.** `_g4_calibration_selective_risk` now reads `calibrated_confidence_coverage` through `_finite_unit_score` (which rejects bool). Repro: `coverage=True` (a Python bool; `isinstance(True,int)` is True; `float(True)==1.0`) was admitted as a perfect 1.0 ⇒ **G4 PASS → `_decide` → CLAIM_GRADE_SOTA**. Now `coverage=True` ⇒ G4 FAIL ⇒ never CLAIM_GRADE. (Before: `G4.passed=True`; after: `False`.)
2. **MAJOR-2 (composite_score non-finite) FIXED.** `_top_composite_system` + `_mle_bootstrap_draws` now read `composite_score` through `_finite_unit_score` (composite is `[0,1]`-bounded — verified in `compute_composite`). Repros: `composite_score=+inf` fabricated a phantom rank-1 crown (a rogue crowned over a genuinely-superior finite system) → now EXCLUDED (the honest finite winner stays crowned); `=NaN` crashed `_top_composite_system` with IndexError (`NaN==best` always False ⇒ empty list ⇒ `[0]`) → now no crash; `=10**400` crashed with OverflowError → now no crash. A non-finite composite is ineligible for the rank-1 crown/bootstrap (treated like a recall-floor breacher).
3. **MAJOR-3 (per_language_recall_delta huge-int) FIXED.** `_g1_recall_floor` + `_run_breaches_recall_floor` now guard each ε with `_is_finite_number` (an ε is a DELTA — can be negative — so `_is_finite_number`, NOT `_finite_unit_score`) before `float()`. Repro: `per_language_recall_delta={...: 10**400}` crashed the SHIPPED `pii-anon supremacy --artifact` CLI with a raw OverflowError traceback. Now a non-finite/huge-int ε fails CLOSED (G1 FAIL / the run-breach guard treats it as a breach), no crash.
4. **also-scan (G3/G6 recall/precision) HARDENED.** `_g3_recall_dominance` (recall) + `_g6_raw_noninferiority` (via the new `_finite_f2` helper, precision+recall) now route through `_finite_unit_score`; `_risk_coverage_is_monotone` also catches `OverflowError`. A hostile `recall=+inf`/`NaN`/`10**400` is excluded/PENDING — never a crash, never a vacuous `+inf >= best` dominance.
5. **MAJOR-4 (producer output-path case-insensitive bypass) FIXED.** `canonical_run.py::_write_canonical` is now case-INSENSITIVE (`part.lower() == "benchmarks"` for every part, on BOTH the literal parts AND `Path.resolve()`'d real parts). Repro: `output_dir='artifacts/Benchmarks'` (capital B) on a case-insensitive FS (APFS) WROTE a real `canonical-run.json` INTO the protected `artifacts/benchmarks/` (same inode). Now every casing of `benchmarks` is refused, and a `..`-laundered alias whose resolved real path lands under `artifacts/benchmarks` is refused — nothing is ever written under `artifacts/benchmarks/*`; the honest `artifacts/canonical/` path is preserved.

**CARDINAL RULE upheld — HONEST-INPUT behavior is byte-identical.** All 97 pre-existing `tests/test_competitive_supremacy.py` gate tests stay green (the honest-input verdicts are unchanged). +24 new `[SECURITY-TEST]`/`[CONTRACT-TEST]`s pin all 4 close repros + the recall/precision class (21 gate-side, 3 producer-side). A fresh `produce_canonical_artifact` still yields the HONEST verdict UNCHANGED: `canonical_claim_run=True`, **verdict=NOT_YET**, binding `G6 FAIL` (core F2 0.7214 < best Tier-R 0.75).

**Verify (iter-3)**: `ruff check src tests` → clean. `mypy src/pii_anon` → clean; `mypy src/pii_anon --strict` → clean. `tests/test_competitive_supremacy.py` **118 passed** (97 honest + 21 close; zero regression); `tests/test_canonical_run.py` **51 passed** (48 + 3 close). Full suite (`make test`, coverage gate active): **3499 passed / 15 skipped / 0 failed**, **coverage 88.40% ≥ 84%**. **md5 of off-limits files all byte-identical** (competitor_compare `7cae16c8…`, competitor_tiers `d9202479…`, orchestrator `0afc6dee…`, test_moe_enhancements `910e9cd6…`). SDO gate methods changed this iter: `_g1_recall_floor`, `_run_breaches_recall_floor`, `_g3_recall_dominance`, `_g4_calibration_selective_risk`, `_risk_coverage_is_monotone`, `_g6_raw_noninferiority` (+ new `_finite_f2` helper), `_top_composite_system`, `_mle_bootstrap_draws`. SDO gate md5 `2e59e1377d48e9acb6f874368b4538e6` → **`4270f18250226e565d5c5e5ac4aa4747`**; producer `canonical_run.py` md5 `36b11f153049a3d4c0d93a2a1481daef` → **`bf0e603430ea2b6313dc7b09df060030`** (`_write_canonical` + new `_is_benchmarks_part` helper). **Nothing written under `artifacts/benchmarks/*`.** RED `8611992` → GREEN `f644e90`.

### iter-4 — the keystone re-close confirmed the 4 FABRICATION holes CLOSED but found 2 remaining MAJOR CRASH-CLASS holes (fail-LOUD denial-of-verdict on a malformed CONTAINER — NOT fabrications); now hardened COMPREHENSIVELY
The re-close confirmed a malformed artifact-sourced CONTAINER (wrong JSON TYPE) reached an UNGUARDED `.get`/`.items`/`len`/iteration and threw, crashing the shipped `pii-anon supremacy --artifact` CLI with a raw traceback. The gate now FAILS-CLOSED on EVERY malformed container (the affected guarantee reads PENDING/FAIL, a bad element is ignored, or the verdict is a clean NOT_YET — NEVER an exception). RED `2320aa8` → GREEN `f8ccf28`.

1. **MAJOR (crash-1: G4 `len(points)` on a non-list) FIXED.** `_g4_calibration_selective_risk` now computes `n_points = len(points) if isinstance(points, list) else 0` ONCE and uses it in BOTH the PASS-detail and the FAIL-detail. Repro: `abstention_operating_points ∈ {42, 3.14, True}` with the other 3 G4 checks PASSING drove the FAIL-detail `else` → `len(points)` UNCONDITIONALLY → `TypeError: object of type 'int' has no len()`. Now ⇒ G4 FAIL (a non-list cannot supply ≥3 points), no crash.
2. **MAJOR (crash-2: container-type malformation through the CLI) FIXED — comprehensively.** `_systems_by_name` (non-list `systems` → empty; non-dict element ignored — closes `systems`-as-dict + bare-string-element `AttributeError`, and NORMALIZES `systems` to `dict[str,dict]`, shielding `_g2`/`_g3`/`_g4`/`_g6`/`_top_composite_system`/`_mle_bootstrap_draws`); `from_artifacts` (non-dict `benchmark`/`run_metadata` → `{}` — closes `run_metadata`-as-list `AttributeError` + top-level-non-dict); new `_tier_status_input` (shields the OFF-LIMITS byte-identical `competitor_tiers.run_status_from_benchmark` at the call site with a shape-normalized view — that module reads `systems`+`available_competitors` unguarded and could NOT be edited); new `_recall_map` (non-dict `per_entity_recall` → `{}` in `_g1`+`_g6` — closes `per_entity_recall`-as-list `.items()`/`.values()` `AttributeError`); `_g1_recall_floor` (non-dict `per_language_recall_delta` → G1 PENDING — was a SILENT fabricated `worst ε=0` PASS on malformed input); `_run_breaches_recall_floor` (non-list `profile_results`/`floor_checks` → empty — closes `'int' object is not iterable`).
3. **Defense-in-depth (cli.py `supremacy`).** The `from_artifacts` call is wrapped so a genuinely-unparseable artifact (`json.JSONDecodeError`) or any never-anticipated shape emits a clean `typer.BadParameter` (exit ≠ 0, no traceback) — the gate-not-crashing fix is primary; this is the CLI backstop.

**CARDINAL RULE upheld — HONEST-INPUT behavior byte-identical.** All 118 pre-existing `tests/test_competitive_supremacy.py` gate tests stay green; +19 close-2 `[SECURITY-TEST]`/`[PROPERTY-TEST]`s pin both crash repros + the full container-type matrix + a deterministic fuzz (`from_artifacts` ALWAYS returns a Verdict, never raises). A fresh `produce_canonical_artifact` still yields the HONEST verdict UNCHANGED: `canonical_claim_run=True`, **verdict=NOT_YET**, binding **G6 FAIL** (core F2 ≈0.656 < best Tier-R ≈0.757), G1/G2/G3/G4/G7 PASS, G5 PENDING.

**Before/after (the exact close repros):** `abstention_operating_points=42` → was `TypeError: object of type 'int' has no len()`, now clean NOT_YET (G4=FAIL); `systems` as a dict → was `AttributeError: 'str' object has no attribute 'get'`, now clean NOT_YET; `run_metadata` as a list → was `AttributeError: 'list' object has no attribute 'get'`, now clean NOT_YET; a `systems` bare-string element → was `AttributeError`, now ignored → clean NOT_YET; `per_entity_recall` as a list → was `AttributeError: 'list' object has no attribute 'items'`, now clean NOT_YET (G1 PENDING).

**Verify (iter-4)**: `ruff check src tests` → clean. `mypy src/pii_anon` → clean; `mypy src/pii_anon --strict` → clean. `tests/test_competitive_supremacy.py` **137 passed** (118 honest + 19 close-2; ZERO honest regression); `tests/test_canonical_run.py` **51 passed** (the keystone-teeth honest verdict on a REAL fresh produce — unchanged). Full suite (coverage gate active): **3527 passed / 15 skipped / 0 failed** (3542 collected − 15 env-gated skips; exit 0), **coverage 88.39% ≥ 84%**. **md5 of off-limits files all byte-identical** (competitor_compare `7cae16c89f4c97136e1a12394dae2025`, competitor_tiers `d9202479d84c519d5866b7d9d515c903`, orchestrator `0afc6deed62bbd0653ae1051b723bace`, test_moe_enhancements `910e9cd66ad6e38c7bb64a9c51ecb1cb`). SDO gate methods changed this iter: `_systems_by_name`, `from_artifacts`, `_g1_recall_floor`, `_run_breaches_recall_floor`, `_g4_calibration_selective_risk`, `_g6_raw_noninferiority` (+ new helpers `_tier_status_input`, `_recall_map`). SDO gate md5 `4270f18250226e565d5c5e5ac4aa4747` → **`d9f31b3c9621a4a8e124f507f774715b`**; `cli.py` hardened in the `supremacy` command (backstop only). Producer `canonical_run.py` UNCHANGED (`bf0e603430ea2b6313dc7b09df060030`). **Nothing written under `artifacts/benchmarks/*`.** RED `2320aa8` → GREEN `f8ccf28`.

### iter-5 — close-3 found the LAST G4 fabrication: a NEGATIVE `per_class_ece` forged a moat-axis PASS; now hardened (gate + producer, symmetric)
The 3rd adversarial close on the keystone found `_g4_calibration_selective_risk` validated each `per_class_ece` value for FINITENESS (`_is_finite_number`) but with NO LOWER BOUND. An Expected Calibration Error is `≥ 0` by construction, so a forged NEGATIVE ECE (`per_class_ece={"EMAIL":-1.0}`, with otherwise-valid coverage/curve/points) was `< bar` ⇒ counted as "within bar" ⇒ **G4.passed=True** ("G4 PASS: every per-class ECE ≤ its bar (worst -1.0)") — a fabricated PASS that flips the honest PENDING/FAIL and can escalate to CLAIM_GRADE_SOTA via the trusted-caller waiver seam. This is the SAME class as the iter-3 NaN/±inf-ECE fix (a present-but-corrupt measurement slipping the `ece > bar` comparison), the only remaining unguarded sub-case (sign, not finiteness).

1. **MAJOR (G4 negative per_class_ece) FIXED — the surgical gate change.** `_g4_calibration_selective_risk` now treats a per-class ECE that is non-finite OR **negative** as a BREACH (`ece_ok = False`), never "within bar". The added executable logic is ONE branch — `if ece < 0.0: ece_ok=False; <rank>; continue` — which (a) sets the breach, (b) EXCLUDES the negative value from `finite_eces` (so garbage never stands in as the worst observed ECE — `observed`/`max_ece` stays ≥ 0), and (c) ranks the breach by its (negative) `ece - bar` slack so a co-located GENUINE over-bar class (positive slack) stays the HEADLINE breach while a lone negative IS the reported breach. Docstring + the loop's inline comment updated (ECE must be finite AND non-negative). The honest paths are untouched (deletions are only docstring/comment rewrites): an ECE in `[0, bar]` still PASSES, an ECE in `(bar, 1]` still FAILS as "ECE > bar", an ECE > 1 still FAILS, a NaN/±inf still FAILS as non-finite.
2. **Defense-in-depth (producer `CanonicalRunGate.validate`) FIXED — symmetric.** Step 5 now validates EVERY `per_class_ece` value via `_finite_unit_score` (rejects a negative / non-finite / bool / out-of-[0,1]), mirroring the gate. The real S4-03 `selective_risk` scorer always emits ECE ≥ 0, so this is fail-closed depth (it never rejects an honest run) — symmetric with the existing G2 family-field + coverage validations there.

**Before/after (the exact close repro `per_class_ece={"EMAIL":-1.0}` + valid coverage/curve/points):** BEFORE (parent `b8a1669`, gate md5 `d9f31b3c…`) → `G4.passed=True`, detail "G4 PASS: every per-class ECE ≤ its bar (worst -1)". AFTER (HEAD, gate md5 `5a71943470d98bae6b719ca726e5379f`) → `G4.passed=False`, `observed=0.0`, detail "per-class ECE on 'EMAIL' is negative (-1) — non-physical (ECE ≥ 0 by construction), a corrupt measurement, not ≤ bar 0.05 (NFR-017)".

**CARDINAL RULE upheld — HONEST behavior byte-identical.** A fresh `produce_canonical_artifact(max_samples=8)` → `from_artifacts` is UNCHANGED: `canonical_claim_run=True`, `canonical_gate_missing=[]`, **verdict=NOT_YET**, binding **G6 FAIL** (core F2 0.7214 < best Tier-R 0.75; entity coverage 0.778), G1/G2/G3/**G4**/G7 PASS, G5 PENDING (the honest in-tree calibration's real ECE ≥ 0 still PASSES G4). The 6 new gate-side `[CONTRACT-TEST]`s pin `-1.0`/`-0.001`/`-5.0` ⇒ FAIL, the co-located `{EMAIL:-1.0, US_SSN:0.5}` ⇒ over-bar headline, the end-to-end non-CLAIM_GRADE, and the ECE==0.0 regression; the 2 producer-side tests pin the gate-reject + the ECE==0.0 accept.

**Verify (iter-5)**: `ruff check src tests` → clean. `mypy src/pii_anon` → clean; `mypy src/pii_anon --strict` → clean. `tests/test_competitive_supremacy.py` **143 passed** (137 + 6 close-3; ZERO honest regression); `tests/test_canonical_run.py` **53 passed** (51 + 2 close-3). Full suite (`.venv` + `pytest -n auto`, coverage gate active): **3526 passed / 15 skipped / 0 failed** (exit 0; 410s), **coverage 88.52% ≥ 84%**; all 8 new close-3 tests collected+passed. **md5 of off-limits files all byte-identical** (competitor_compare `7cae16c89f4c97136e1a12394dae2025`, competitor_tiers `d9202479d84c519d5866b7d9d515c903`, orchestrator `0afc6deed62bbd0653ae1051b723bace`, test_moe_enhancements `910e9cd66ad6e38c7bb64a9c51ecb1cb`). SDO gate md5 `d9f31b3c9621a4a8e124f507f774715b` → **`5a71943470d98bae6b719ca726e5379f`** (`_g4_calibration_selective_risk` only — 1 executable branch + docstring/comment). Producer `canonical_run.py` md5 `bf0e603430ea2b6313dc7b09df060030` → **`96749b1c77851939aaff06c71273c5ae`** (the step-5 per-value ECE validation). **Nothing written under `artifacts/benchmarks/*` or `artifacts/canonical/`.** RED `2edf4a4` → GREEN `b965a77`.
