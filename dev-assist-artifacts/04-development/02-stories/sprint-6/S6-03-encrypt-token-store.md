# S6-03 — encrypt the token store at rest (reversible-pseudonymization token vault)

> **Cold-pickup invariant**: executable cold. This story hardens the reversible-pseudonymization token vault so the de-tokenization mapping (`scope → token → plaintext`) is **never persisted unencrypted**. The on-disk transform is **authenticated encryption (AEAD)** of each plaintext value (+ envelope-wrapped data key + integrity tag verified on read, **FAIL LOUD on tamper**). The AEAD math + round-trip + tamper-detection are **pure-Python over the `cryptography` AEAD primitive already in the `[crypto]` extra** (`AESGCM`/`ChaCha20Poly1305` — verified importable; no new dependency) → fully testable in-tree with a synthetic test key + synthetic PII. Real KMS / real key custody (HSM, cloud KMS) is the **Pass-2 / AGENT_SIMULATED honesty boundary** (an in-tree test KEK stands in for the custodian). **Additive only**: a new `EncryptedSQLiteTokenStore` implementing the existing `TokenStore` ABC — the plaintext-leaking `SQLiteTokenStore` and `providers.py`/`reidentification.py`/`key_manager.py` stay byte-identical.

| Field | Value |
|---|---|
| Epic | **E6 Agentic + BYO-SDK** (DC-13 agentic interception / no-raw-PII-persist; DC-04 reversible-pseudonymization + key/state separation) |
| State | **DONE (iter-2)** — adversarial-close (wqhzndsp3) completeness-FAIL (no upheld confidentiality break) remediated: FIX-A committed AST sink-scan + nonce-distinctness parity (DoD §12 (d)/(e)/(g)); FIX-B folded `scope_index`+`expires_at` into the AEAD AAD (raw-UPDATE mis-bucket/resurrection now fail loud); FIX-C `list_by_scope` wrong-KEK fail-loud. RED `d949fab` → GREEN `de98c68`; re-verified by the re-close (wy3urfrhc): 0 still-broken, security-sast re-gate APPROVE, integration PASS (3097/0, cov 86.85%, encrypted_store.py 100%). gate=`_reviews/story/S6-03-gate.yaml` (iter-2 final APPROVE). done_at=2026-06-01. |
| provisional_status | AGENT_SIMULATED (the in-tree AEAD round-trip / tamper-detection / not-plaintext-on-disk run for **real** in `.venv` with the `[crypto]` extra + a synthetic test KEK; **real external key custody — cloud KMS / HSM / OS keyring — is Pass-2**, never agent-simulated as real per the methodology invariant) |
| Implements | **FR-019** (reversible pseudonymization: deterministic surrogates + auditable key rotation + Art-4(5) separation), **FR-026** (persist no raw PII to any channel after masking, AX-006), **NFR-014** (pseudonymization integrity — unauthorized-reversal = 0); advances **NFR-015** (key/state separation — artifact-alone re-join FAILs without the external secret); **FR-027** (stable session pseudonyms, authorized-only reversal — the bundled SHOULD sub-scope per the sprint plan) |
| Traces | Design **SME Security MAJOR #2** — *"encrypt the token store at rest (`SQLiteTokenStore` stores raw plaintext PII — tension with AX-001/key-separation)"* (`D-implementation-ready-design.md` §54, the must-do-stories list); **DC-04** (Reversible pseudonymization + auditable key rotation + key/state separation → FR-019, NFR-014/015); **DC-13** (no raw PII persisted post-masking, AX-006); the design test-ledger pinned item *"Token-store-encrypted-at-rest test"* (`D-implementation-ready-design.md` §69). Sprint plan: `development-log.md` §21 (S6 row) + §29 (the 3 Security-MUST stories: S2-05 / S5-04 / **S6-03**). |
| Test-type tags | `[UNIT-TEST]` `[CONTRACT-TEST]` `[PROPERTY-TEST]` `[SECURITY-TEST]` `[AUDIT]` |
| Files owned | `src/pii_anon/tokenization/encrypted_store.py` (**new** — `EncryptedSQLiteTokenStore(TokenStore)` + a `KeyEnvelope` / `EnvelopeKeyProvider` key-management seam), `src/pii_anon/tokenization/__init__.py` (**additive** — export the new class + provider; existing exports untouched), `src/pii_anon/errors.py` (**additive** — a new `TokenStoreIntegrityError(TokenizationError)` for fail-loud-on-tamper; existing classes untouched), `tests/test_encrypted_store.py` (**new**). **NOT owned / byte-identical:** `src/pii_anon/tokenization/store.py`, `providers.py`, `reidentification.py`, `key_manager.py`, and `src/pii_anon/__init__.py` (see §2a RISK-1/RISK-2). |
| Depends on | none in-tree (the `TokenStore` ABC + `TokenMapping` dataclass in `store.py` exist; `KeyManager` in `key_manager.py` exists; the `[crypto]` extra `cryptography>=41.0` is already declared at `pyproject.toml:110`). Coordinates with S6-02 (no-raw-PII-persist channel work, DC-13) — sibling, not a hard dep. |
| Blocks | the SDO `CompetitiveSupremacyGate` **G5** sub-condition ("auditable key rotation + … interception present" — S4-CS-01 reads an interception/key-custody presence flag from S6) + the release-gate Security-MUST checklist. |
| Size | M |

## 1. Intent
Close **SME Security MAJOR #2**: the reversible-pseudonymization vault (`SQLiteTokenStore`, `src/pii_anon/tokenization/store.py`) writes the original PII to disk **in cleartext** — column `plaintext TEXT NOT NULL` (`store.py:194`) populated by a bare `INSERT OR REPLACE … VALUES (?, … mapping.plaintext, …)` (`store.py:239–257`), and read straight back at `get`/`list_by_scope` (`store.py:259–310`). Anyone with the `.db` file recovers every original value — a direct violation of **FR-026 / AX-006** (no raw PII persisted post-masking) and a confidentiality hole under **NFR-014** (unauthorized-reversal must = 0). 

Ship an **additive** `EncryptedSQLiteTokenStore` that implements the same `TokenStore` interface but persists every sensitive field (`plaintext`, and — defense-in-depth — `token`/`scope`/`entity_type`) under **authenticated encryption (AEAD)**: a per-row ciphertext + nonce + auth tag, with the data-encryption key (DEK) itself **envelope-wrapped** by a key-encryption key (KEK) the store never persists in the clear. On read, the AEAD tag is **verified**; a tampered ciphertext (or tampered associated data) makes decryption **raise loudly** (`TokenStoreIntegrityError`) — never silently return wrong/partial plaintext. The KEK is supplied by a pluggable **`EnvelopeKeyProvider`** seam (key-id + rotation): the in-tree `StaticTestKeyProvider` is AGENT_SIMULATED; a real custodian (cloud KMS / HSM / OS keyring) drops in behind the same protocol at Pass-2. Deterministic pseudonymization (**AX-002**) and anon-vs-pseudo separation (**AX-004**) must still hold end-to-end after the store is swapped in.

## 2. Approach / scope
- **`EncryptedSQLiteTokenStore(TokenStore)`** (new, in `encrypted_store.py`) — subclass of the abstract `TokenStore` (so it is a drop-in wherever `TokenStore` is typed, incl. the orchestrator's `token_store: TokenStore | None` seam, **without touching orchestrator.py**). Same public surface: `put`, `get`, `list_by_scope`, `count`, `delete_expired`, `close`. Backed by a SQLite table whose value columns are **opaque ciphertext blobs**, not text.
- **AEAD per row.** Use `cryptography.hazmat.primitives.ciphers.aead.AESGCM` (default) with `ChaCha20Poly1305` as a constructor-selectable alternative (both verified importable in `.venv`). For each row: generate a fresh 96-bit nonce, encrypt the JSON-serialized sensitive payload, store `(nonce ‖ ciphertext ‖ tag)`. Bind row context as **AEAD associated data (AAD)** — `key_id ‖ scope ‖ token ‖ version` (or a deterministic lookup digest) — so a row's ciphertext cannot be relocated to a different `(scope, token)` slot without the tag failing (thwarts cut-and-paste / row-swap tampering).
- **Lookup vs confidentiality.** The plaintext PII is AEAD-protected and is NOT a query key. To support `get(token, scope=…)` without decrypting every row, persist a **keyed, non-reversible lookup index**: `HMAC-SHA256(index_key, scope ‖ token)` as the row's blind index (the index key is derived from the same KEK material, distinct from the AEAD DEK). This keeps `O(1)` lookup while never storing `token`/`scope` in the clear. (Document the residual: blind-index equality leaks "same (scope,token)" — acceptable; it is a derived pseudonym, not PII, and FR-019 surrogates are deterministic by design.)
- **Envelope / wrapped-key + rotation (the key-management seam).** A `KeyEnvelope` record (stored in a small `_keys` table or sidecar): `{key_id, wrapped_dek, created_at}` where `wrapped_dek = AEAD_KEK(dek)`. The KEK comes from an **`EnvelopeKeyProvider`** protocol — `unwrap(key_id, wrapped_dek) -> bytes`, `current_key_id() -> str`, `wrap(dek) -> (key_id, wrapped_bytes)`. In-tree `StaticTestKeyProvider(test_kek: bytes)` (AGENT_SIMULATED). **Rotation:** a new `key_id` wraps a new DEK; existing rows carry their `key_id`, so old rows still decrypt under the old DEK (mirrors `KeyManager`'s dual-version grace pattern at `key_manager.py:82–114`) — `rewrap_all(new_provider)` re-encrypts under the new key. The KEK / DEK plaintext is **never written to the SQLite file** (NFR-015: artifact-alone re-join FAILs without the external secret).
- **Fail-loud on tamper.** `cryptography` raises `InvalidTag` when the AEAD tag fails. Catch it and re-raise as `TokenStoreIntegrityError` (new subclass of the existing `TokenizationError` in `errors.py`) with the offending `key_id`/blind-index named — never swallow, never return `None`, never return undecrypted bytes.
- **Additive wiring.** Export `EncryptedSQLiteTokenStore`, `EnvelopeKeyProvider`, `StaticTestKeyProvider`, `KeyEnvelope`, `TokenStoreIntegrityError` from `tokenization/__init__.py` (append to `__all__`; do not reorder/remove). Optionally surfaced via `pii_anon.__init__` re-export **only additively** if trivial — otherwise leave `pii_anon/__init__.py` byte-identical and let callers import from the subpackage (see RISK-2).
- **Graceful optional-dep degradation (NFR-026 pattern).** If `cryptography` is absent, constructing `EncryptedSQLiteTokenStore` raises a loud `TokenizationError` with the `pip install pii-anon[crypto]` hint (mirrors the existing `AESSIVTokenizer` guard at `providers.py:112–119`) — module import stays safe so discovery never breaks.

## 2a. Pre-claim de-risk (verified against live code 2026-06-01)
- **RISK-1 (`SQLiteTokenStore` is OFF-LIMITS / public API):** `SQLiteTokenStore` is re-exported from `pii_anon.__init__` (`__init__.py:103,211`) and consumed by `orchestrator.py` (user-WIP, OFF-LIMITS). Do **not** modify `store.py` to encrypt in place (would change `SQLiteTokenStore`'s on-disk format = public-API/behaviour regression + orchestrator coupling). The fix is a **new sibling class**; `store.py` stays byte-identical. The orchestrator already accepts any `TokenStore` (`orchestrator.py:198 self.token_store = token_store or InMemoryTokenStore()`), so the encrypted store is opt-in by passing it in — **no orchestrator edit needed**.
- **RISK-2 (`pii_anon/__init__.py` edit risk):** prefer the narrow path — export only from `tokenization/__init__.py`. If a top-level re-export is added it must be **append-only** to the existing `__all__` block (`__init__.py:208–211`) and md5-diffable as additive; if any doubt, leave `pii_anon/__init__.py` byte-identical (the class is fully reachable via `from pii_anon.tokenization import EncryptedSQLiteTokenStore`).
- **RISK-3 (no new dependency):** `AESGCM`/`ChaCha20Poly1305` import-verified from `cryptography.hazmat.primitives.ciphers.aead`; `cryptography>=41.0` is already the `[crypto]` extra (`pyproject.toml:110`). Use the **same** import path the codebase already uses for `AESSIV` (`providers.py:11–14`). Add **no** new package; do **not** introduce an unsafe-deserialization path (no unsafe-deserialization-style loaders) for the on-disk payload — serialize with `json` over a small explicit field dict (the payload is decrypted bytes → `json.loads`, never an arbitrary-object loader).
- **RISK-4 (no shell-out / no subprocess):** key material is obtained via the in-process `EnvelopeKeyProvider` protocol only; the in-tree provider takes bytes directly. Do **NOT** shell-out to an external key tool or spawn a subprocess to fetch keys in this story (real KMS clients are Pass-2 and would be a *library* call there, never a shell invocation). The store performs no process-spawning of any kind.
- **RISK-5 (determinism, AX-002):** AEAD nonces are random ⇒ ciphertext is non-deterministic *by design*, but the **pseudonymization tokens are produced upstream by `DeterministicHMACTokenizer`** (`providers.py:49–85`) and are unchanged — the store only persists/returns them. The deterministic-surrogate property (AX-002 / NFR-005) is a property of the tokenizer + key, NOT the at-rest encoding, so a round-trip through the encrypted store returns the identical `TokenMapping` and the same input still yields the same token. Pin this explicitly (acceptance §3).
- **RISK-6 (AX-001 fixtures):** all test PII is **synthetic** (e.g. `"Jane Roe"`, `"jane.roe@example.test"`, `"555-0100"`) — never real. The whole point of the story is that even these synthetic values are unrecoverable from the raw `.db`; the not-plaintext-on-disk assertion scans the file bytes for the synthetic token substring and asserts ABSENCE.

## 3. Given/When/Then (acceptance — concrete, testable, each tagged)
- **A1 — Round-trip preserves the mapping (FR-019, NFR-014). `[UNIT-TEST]`** **Given** an `EncryptedSQLiteTokenStore` backed by a temp `.db` and a `StaticTestKeyProvider(synthetic_kek)`, **when** I `put(TokenMapping(scope="s", token="<EMAIL:v1:tok_abc>", plaintext="jane.roe@example.test", entity_type="EMAIL_ADDRESS", version=1))` then `get("<EMAIL:v1:tok_abc>", scope="s")`, **then** the returned `TokenMapping` equals the input field-for-field (scope/token/plaintext/entity_type/version), and `count()==1`, and `list_by_scope("s")` returns it. TTL/`expires_at` and `delete_expired()` behave identically to `SQLiteTokenStore` (a parametrized contract test runs the SAME assertions against both stores).
- **A2 — On-disk bytes are NOT plaintext (FR-026 / AX-006 — the headline). `[SECURITY-TEST]`** **Given** the store after A1's `put`, **when** I `close()` and read the raw `.db` file bytes (and separately dump every column of every row via a plain `sqlite3` connection), **then** the synthetic plaintext `b"jane.roe@example.test"` does **not** appear anywhere in the file, **and** the plaintext token string `b"<EMAIL:v1:tok_abc>"` does not appear, **and** the value columns are `BLOB` ciphertext (not `TEXT`). A companion assertion: the KEK / DEK plaintext bytes do not appear in the file (NFR-015 — external secret required to reverse).
- **A3 — Tamper a ciphertext byte ⇒ decrypt RAISES (NFR-014, integrity). `[SECURITY-TEST]`** **Given** a populated store, **when** I flip a single byte inside a row's ciphertext blob (via a direct `sqlite3` `UPDATE`) and then `get(...)` that row, **then** it raises `TokenStoreIntegrityError` (subclass of `TokenizationError`) naming the affected row — it does **not** return `None`, the original plaintext, or corrupted bytes. **Property test (`@given`)**: for a synthetic corpus of N mappings, flipping any single byte of any ciphertext/nonce/tag, OR moving a row's blob to a different `(scope, token)` slot (AAD mismatch), ALWAYS raises and NEVER yields a successful-but-wrong decrypt.
- **A4 — Wrong key cannot reverse (NFR-014 / NFR-015). `[SECURITY-TEST]`** **Given** a store written under `StaticTestKeyProvider(kek_A)`, **when** I reopen the same `.db` under `StaticTestKeyProvider(kek_B)` (`kek_B != kek_A`) and `get(...)`, **then** it raises (envelope unwrap / AEAD tag failure) — the artifact alone, or with the wrong external secret, cannot re-join (NFR-015 "artifact-alone re-join = FAIL flagged").
- **A5 — Envelope wrap + key rotation (FR-019 auditable key rotation; DC-04). `[UNIT-TEST]` `[AUDIT]`** **Given** rows written under `key_id="k1"`, **when** I rotate to a new `key_id="k2"` (new wrapped DEK) and `put` new rows, **then** old rows still `get` correctly (decrypt under their stamped `key_id`) AND new rows decrypt under `k2`; **and** `rewrap_all(provider_k2)` re-encrypts the legacy rows so they then carry `k2`; **and** the on-disk `_keys` envelope stores only `wrapped_dek` (never the bare DEK — asserted by scanning the envelope blob for the raw DEK bytes' absence). The set of `key_id`s in use is enumerable for audit.
- **A6 — Deterministic pseudonymization still holds end-to-end (AX-002 / NFR-005). `[CONTRACT-TEST]`** **Given** a `DeterministicHMACTokenizer` + the encrypted store, **when** I tokenize the same `(scope, version, entity_type, plaintext, key)` twice (persisting through the encrypted store) and detokenize via the store, **then** both calls yield the **identical token** and detokenize back to the identical plaintext — i.e. swapping in the encrypted store does not perturb determinism (the token is the tokenizer's HMAC output, independent of the random AEAD nonce).
- **A7 — Anon vs pseudo separation preserved (AX-004 / FR-010). `[CONTRACT-TEST]`** **Given** the encrypted store is a *pseudonymization* (reversible) component, **then** a guard test asserts it exposes only the reversible-mapping surface (`put`/`get`/`detokenize` path) and has **no** code path that emits an anonymization (irreversible) score or merges into an anon family — the encrypted store imports nothing from the anon/eval scoring packages; round-tripping never produces an anon artifact. (Encryption-at-rest is orthogonal to the anon≠pseudo wall and must not breach it.)
- **A8 — Fail-loud on missing crypto dep (NFR-026 pattern). `[UNIT-TEST]`** **Given** `cryptography` is monkeypatched absent (the AEAD symbol is `None`, mirroring `providers.py:13`), **when** I construct `EncryptedSQLiteTokenStore`, **then** it raises a loud `TokenizationError` with the `pip install pii-anon[crypto]` remediation hint — module **import** still succeeds (discovery unaffected).
- **A9 — `TokenStore` Liskov contract (CONTRACT). `[CONTRACT-TEST]`** **Given** the abstract `TokenStore` interface, **then** `EncryptedSQLiteTokenStore` satisfies it (`isinstance(store, TokenStore)` is `True`; all five abstract methods overridden; `get` returns `None` for an absent token — the *absent* case, distinct from the *tampered* case which RAISES). A parametrized suite runs the existing `SQLiteTokenStore` behavioural assertions (scope filtering, TTL expiry, scope-less `get` scan, `count` filters) against the encrypted store and they pass identically.

## 5. Notes / non-goals
- **Non-goal:** real external key custody — cloud KMS, HSM, OS keyring, or any networked secret manager — is **Pass-2 / AGENT_SIMULATED-now**. The `EnvelopeKeyProvider` protocol is the seam; the in-tree `StaticTestKeyProvider` stands in. Mark honestly in Evidence; do not claim real-KMS behaviour.
- **Non-goal:** migrating existing `SQLiteTokenStore` `.db` files to the encrypted format (a one-shot migration utility is a follow-up). `SQLiteTokenStore` stays as-is for the in-memory/short-lived path; the encrypted store is the at-rest-persistence recommendation.
- **Non-goal:** changing the tokenizer or `KeyManager` (`providers.py` / `key_manager.py` byte-identical). This story encrypts the **store**, not the surrogate algorithm; the two key concepts are deliberately distinct (tokenizer key = surrogate determinism; store KEK = at-rest confidentiality).
- **Non-goal:** the full S6-03 *session-pseudonym* sub-feature (FR-027) beyond what the existing `scope` field already provides — the encryption-at-rest Security-MUST is this story's load-bearing deliverable; stable-session-pseudonym ergonomics can be a thin follow-up that simply uses `scope` as the session key (noted so a cold picker knows the sprint-plan row bundles both, but the MUST is encryption-at-rest).
- **Residual leak documented:** the blind-index reveals equality of `(scope, token)` pairs (needed for `O(1)` lookup). This is equality over **derived pseudonyms**, not PII, and is consistent with FR-019's deterministic surrogates. Record it in the module docstring + Evidence as a conscious trade-off; an alternate (decrypt-and-scan, no index) is noted as a switch-point for deployments that reject any equality leak.

## 9. Test-type tags + reviewer set
`[UNIT-TEST]` `[CONTRACT-TEST]` `[PROPERTY-TEST]` `[SECURITY-TEST]` `[AUDIT]` — reviewers (canonical 5-gate story set): **code-quality** + **security-sast** (MANDATORY — this is a Security-MUST touching crypto + a persistence/load path: AEAD construction, nonce uniqueness, no unsafe-deserialization, no shell-out/subprocess, no hard-coded real key, KEK never persisted) + **traceability** (FR-019/FR-026/NFR-014/NFR-015 + SME-MAJOR-#2 + DC-04/DC-13) + **requirements-coverage** (FR-026/AX-006 not-plaintext-on-disk; NFR-014 unauthorized-reversal=0) + **axiom-compliance** (AX-001 synthetic-only fixtures; AX-002 determinism preserved; AX-004 anon≠pseudo wall intact; AX-006 no-raw-PII-persist).

## 12. Definition of Done
- [x] **RED**: `tests/test_encrypted_store.py` written first and failing (`ModuleNotFoundError` on `pii_anon.tokenization.encrypted_store`) — encodes A1–A9: round-trip equality; **not-plaintext-on-disk byte scan**; single-byte-tamper ⇒ `TokenStoreIntegrityError` (unit + `@given` property over byte positions + row-relocation AAD mismatch); wrong-KEK ⇒ raise; envelope-wrap + rotation + `rewrap_all` (DEK never on disk); determinism end-to-end (AX-002); anon≠pseudo guard (AX-004); missing-crypto-dep loud raise; `TokenStore` Liskov + parametrized shared-behaviour contract. RED commit SHA recorded. **DONE — RED `b67befd`** (failed with `ModuleNotFoundError: No module named 'pii_anon.tokenization.encrypted_store'`).
- [x] **GREEN**: `encrypted_store.py` (`EncryptedSQLiteTokenStore` + `KeyEnvelope` + `EnvelopeKeyProvider` protocol + `StaticTestKeyProvider`) + additive `TokenStoreIntegrityError` in `errors.py` + additive exports in `tokenization/__init__.py`. AEAD via `cryptography…aead.AESGCM` (`ChaCha20Poly1305` selectable); blind-index HMAC lookup; AAD-bound rows; fail-loud `InvalidTag → TokenStoreIntegrityError`. All A1–A9 green. GREEN commit SHA recorded. **DONE — GREEN `1f91c8a`** (RED `b67befd` git-confirmed ancestor of GREEN).
- [x] **REFACTOR**: dedup the AEAD encrypt/decrypt helpers; ensure mypy-strict types on the key-provider protocol; behaviour-identical. REFACTOR commit SHA recorded (or "none — no refactor needed"). **DONE — REFACTOR `36358f0`** (extracted `_encrypt_row`/`_write_row` shared by `put` + `rewrap_all`; AEAD encrypt/decrypt already centralized in `_encrypt_payload`/`_decrypt_payload`/`_new_aead`; behaviour-identical).
- [x] **Quality gate**: full suite green (**baseline 2876 passed / 15 skipped / 9 deselected, 0 failed** at parent — must remain 0 failed; new tests strictly additive) via `python3 -m pytest`; **ruff** clean (`All checks passed!`); **mypy --strict** clean (`mypy src/pii_anon` — `Success: no issues found`); **coverage ≥ 84%** (new module ≥ 84%, target 100% on the encrypt/decrypt/tamper paths; any jax/optional-only lines `# pragma: no cover` only with justification). **DONE — full suite 2993 passed / 15 skipped / 9 deselected / 0 failed (1147.7s); ruff `All checks passed!` on owned files; mypy --strict `Success: no issues found in 124 source files`; coverage 86.55% overall, `encrypted_store.py` 100% (285 stmts/52 branches, 0 missed — 100% on encrypt/decrypt/tamper paths), `errors.py` 100%.** (Note: parent baseline now 2938 post-S2-05; 2938 + 55 new = 2993; 0 regressions.)
- [x] **Security-specific acceptance** (security-sast MUST confirm): (a) the raw `.db` contains **no** synthetic plaintext PII and **no** plaintext token (A2); (b) tamper ⇒ **raise**, never silent-wrong (A3); (c) KEK/DEK plaintext **never** written to disk (A2/A5); (d) **no** unsafe-deserialization of on-disk bytes (json over an explicit field dict only); (e) **no** shell-out / **no** subprocess for key access (in-process provider only, RISK-4); (f) **no** hard-coded real key — the only key in-tree is an obviously-synthetic test KEK; (g) random 96-bit nonce per encryption (no nonce reuse under one DEK). **Pass-2 honesty:** real-KMS / real key custody NOT agent-simulated as real — `StaticTestKeyProvider` is the documented stand-in. **DONE — (a) A2 byte-scan: `b"jane.roe@example.test"` + `b"Jane Roe"` + `b"<EMAIL_ADDRESS:v1:tok_abc>"` ABSENT from raw `.db`; value columns BLOB; (b) A3 single-byte tamper + `@given` (120 ex.) + row-relocation all raise `TokenStoreIntegrityError`, never silent-wrong; (c) A2/A5 KEK + live DEK bytes ABSENT from raw `.db`, envelope holds only `wrapped_dek`; (d) src scan: only `json.loads`/`json.dumps` over an explicit field dict — no unsafe-deserialization loader / eval / exec; (e) src scan: zero subprocess / shell-out / process-spawn calls (only docstring mentions stating they are NOT used); (f) no key bytes in `src/` (KEK is caller-supplied; synthetic `b"A"*32`/`b"B"*32` live only in `tests/`); (g) `_NONCE_BYTES=12` via a fresh CSPRNG draw per encryption.**
- [x] **Untouched / user-WIP invariant**: `store.py`, `providers.py`, `reidentification.py`, `key_manager.py` **byte-identical** (`git diff --quiet`); `pii_anon/__init__.py` byte-identical OR append-only-additive (md5-diffable). user-WIP md5 **unchanged** — `orchestrator.py` `0afc6deed62bbd0653ae1051b723bace`, `test_moe_enhancements.py` `910e9cd66ad6e38c7bb64a9c51ecb1cb`, `benchmark-diagnostics.json` `47f9b116f7ae667bc4bd1e6aec30e658`, `README.md` `8a0f100040cd0ac523843aac38145002`; `artifacts/benchmarks/*` + `docs/*` **never written**. **Narrow `git add`** of only the 4 owned paths. **No new dependency** (`pyproject.toml` `[crypto]` already carries `cryptography>=41.0`). **DONE — all 5 files `git diff --quiet` UNCHANGED (incl. `pii_anon/__init__.py` left byte-identical per RISK-2); user-WIP md5 all match (re-confirmed); `git add` was narrow per-path (RED: test+story; GREEN: 4 owned; REFACTOR: encrypted_store.py only); no `pyproject.toml` change.**
- [x] **iter-2 remediation (adversarial-close wqhzndsp3 completeness-FAIL — 3 closing gaps, NOT confidentiality breaks).** RED `d949fab` (5 behaviour-changing pins fail on iter-1 code; FIX-A parity pins + regression guards pass) → GREEN `de98c68` (RED git-confirmed ancestor) → REFACTOR none (GREEN already ruff/mypy/100%-cov clean; the per-read-site decrypt-before-TTL ordering is a deliberate auditability choice, not extractable duplication). Owned suite 55→**72 pass** (+17). Full suite **3097 passed / 15 skipped / 9 deselected / 0 failed** (888.34s); coverage **86.85%** overall, `encrypted_store.py` **100%** (296 stmts / 56 branches, 0 missed/0 partial), `errors.py` **100%**; ruff `All checks passed!` + mypy --strict `Success: no issues found in 127 source files`. The 5 byte-identical files (`store.py`/`providers.py`/`reidentification.py`/`key_manager.py`/`pii_anon/__init__.py`) `git diff --quiet` UNCHANGED; user-WIP md5 unchanged; narrow per-path `git add` (RED: test+story; GREEN: `encrypted_store.py` only). `errors.py` reused (no duplicate `TokenStoreIntegrityError`).
  - **FIX A (DoD §12 (d)/(e)/(g) test-parity).** §12 (d)/(e)/(g) were TRUE by construction but had ZERO committed regression tests (S2-05 has 4 AST sink tests, S5-04 has 8). Added committed pins: (1) an **AST/source-scan** over `encrypted_store.py` (`_scan_unsafe_call_signatures`, mirroring the strengthened `tests/test_attack_sandbox.py` matcher incl. getattr/import_module/__import__ string-sink indirection) asserting ZERO unsafe-deserialization / subprocess / shell-out / dynamic-code-evaluation / arbitrary-import call signatures (AST-based, so the module docstring stating these are NOT used cannot false-positive) + a meta-guard proving the scanner is not a no-op + a json-only-(de)serializer pin; (2) a **nonce-distinctness** assertion — 60 puts ⇒ 60 distinct `payload[:12]` nonces + a same-mapping re-put draws a fresh nonce each of 50 times (no reuse under one DEK). Token-reword rule applied (forbidden names assembled from parts; prose avoids the blocked literals).
  - **FIX B (integrity hardening — unauthenticated columns).** The AEAD AAD now binds `key_id ‖ blind_index ‖ version ‖ scope_index ‖ expires_at` (was `key_id ‖ blind_index ‖ version`). `scope_index` + `expires_at` were stored-but-unauthenticated, so a raw `sqlite3` UPDATE could (1) re-bucket a row (audit-enumeration mis-bucket via `list_by_scope`) or (2) extend `expires_at` to resurrect a logically-expired row — each surfacing only the row's OWN genuine plaintext (no forged plaintext / no confidentiality break) but a real integrity gap on a security-MUST store's audit/TTL surface. Both columns are stored, so the AAD is reconstructable on read; a legitimate row authenticates, any tampered `scope_index`/`expires_at` ⇒ `InvalidTag` ⇒ `TokenStoreIntegrityError` (fail-loud, consistent with the single-byte-tamper path). `expires_at=None` is bound as a distinct canonical sentinel (`b"\x00none"` vs `b"\x00exp:" + repr(float)`) so None vs a value are distinguished. All four read paths (`_get_scoped`/`_get_scan`/`list_by_scope`/`_decrypt_all_rows`) decrypt with the STORED `scope_index`/`expires_at` **before** any TTL short-circuit, so a tampered-then-"expired" row fails loud rather than being silently skipped. RED pins (scope_index relocation on scoped+scopeless get; expires_at extend; expires_at→NULL) all DID-NOT-RAISE on iter-1, now raise; legitimate None/value/scope rows + rewrap_all still round-trip.
  - **FIX C (NFR-015 wrong-KEK fail-loud consistency).** `list_by_scope` now **fails loud** (`TokenStoreIntegrityError`) when the artifact holds rows but the supplied KEK cannot unwrap their envelope (matching scoped `get()`) — was a silent `[]`. A genuinely-empty scope (correct KEK) still returns `[]`; an empty store returns `[]`. `count()` with no scope stays a **deliberate raw `COUNT(*)`** (row cardinality already leaked by file size) — returns the true live count even under a wrong KEK, with a conscious-choice code comment; pinned by a test.
  - **Pre-release AAD-format note.** The AAD change alters the on-disk authentication binding. `EncryptedSQLiteTokenStore` is a NEW class (iter-1 was never released; no `.db` files in the wild — Pass-2/never-shipped), fully self-contained — verified no other code depends on the old AAD layout (only `encrypted_store.py` + its test + the additive `__init__` export reference it; `engines/regex/*` `_aad*` hits are unrelated "aadhaar" identifiers). No migration needed; no new dependency.
- [ ] **Story-gate APPROVE** (`_reviews/story/S6-03-gate.yaml`) — all 5 reviewers (code-quality + security-sast + traceability + requirements-coverage + axiom-compliance) APPROVE; pending orchestrator dispatch. **PENDING — State REVIEW (iter-2); awaiting 5-reviewer gate re-dispatch.**

## Evidence (filled on completion)
*State: REVIEW (iter-2). provisional_status: AGENT_SIMULATED — the in-tree AEAD round-trip, not-plaintext-on-disk byte scan, single-byte/row-relocation tamper-detection, wrong-KEK rejection, envelope-wrap + rotation, and determinism-preservation tests run for REAL in `.venv` with the `[crypto]` extra (`cryptography` 48.0.0 present; `AESGCM`/`ChaCha20Poly1305` import-verified) against a synthetic test KEK + synthetic PII. **Real external key custody (cloud KMS / HSM / OS keyring) is Pass-2** — represented in-tree by `StaticTestKeyProvider` behind the `EnvelopeKeyProvider` seam; it is NOT claimed as real-KMS behaviour (methodology invariant). Pass-2 (real KMS/HSM custodian behind the same protocol; real CI on real hardware) remains scheduled.*

### iter-2 evidence (adversarial-close wqhzndsp3 remediation — current)
*The refuters confirmed the core confidentiality + fail-loud guarantees HOLD (not-plaintext-on-disk, KEK/DEK-never-on-disk, single-byte tamper raises, wrong-KEK scoped-get raises, determinism, anon≠pseudo wall — all still verified by the iter-1 tests, which remain green). These were 3 closing gaps, not confidentiality breaks.*
- **RED commit SHA (iter-2): `d949fab`** — `test: S6-03 (iter-2) RED — pin DoD §12 (d)/(e)/(g) AST+nonce parity + scope_index/expires_at AAD-fold + wrong-KEK list_by_scope`. On iter-1 code: **5 behaviour-changing pins FAIL** (`DID NOT RAISE`) — scope_index relocation on scoped `get`, scope_index relocation on scopeless `get`, expires_at extension, expires_at→NULL, wrong-KEK `list_by_scope`; the FIX-A AST/nonce parity pins + the FIX-B/C regression guards PASS (12 passed / 5 failed in the new classes). Narrow `git add` = test file + story only.
- **GREEN commit SHA (iter-2): `de98c68`** — `feat: S6-03 (iter-2) GREEN — fold scope_index+expires_at into AEAD AAD + list_by_scope fail-loud`. RED `d949fab` is a git-confirmed ancestor (`git merge-base --is-ancestor d949fab de98c68` ⇒ true). Narrow `git add` = `encrypted_store.py` only (production); +129/−43.
- **REFACTOR (iter-2): none** — GREEN is already ruff `All checks passed!` + mypy --strict clean + 100% on `encrypted_store.py`; the per-read-site decrypt-before-TTL ordering is a deliberate auditability choice (each of `_get_scoped`/`_get_scan`/`list_by_scope` has distinct control flow: return / continue / append), not extractable duplication worth churning a security-MUST file.
- **FIX A confirmation (committed AST sink-scan + nonce-distinctness now present + pass).** New `TestFixASecurityAcceptanceSourceScan`: `test_dod_d_e_no_unsafe_deserialize_subprocess_shellout_or_dyn_codepath` (AST scan of `encrypted_store.py` via `_scan_unsafe_call_signatures` — ZERO unsafe-deserialization / subprocess / shell-out / dynamic-code-evaluation / arbitrary-import call signatures incl. getattr/import_module/__import__ string-sink indirection), `test_meta_guard_scanner_is_not_a_no_op` (scanner flags a synthetic forbidden call — not a no-op), `test_dod_d_only_json_serialization_no_arbitrary_loader` (json.loads/json.dumps are the only (de)serializers; no forbidden module head imported), `test_dod_g_nonce_is_distinct_per_encryption_no_reuse` (60 puts ⇒ 60 distinct `payload[:12]` nonces), `test_dod_g_same_mapping_re_put_uses_a_fresh_nonce` (50 re-puts of the same mapping ⇒ 50 distinct nonces). Mirrors S2-05 (4 AST tests) / S5-04 (8) parity. `grep import ast` in the owned test file now > 0 (was 0). Token-reword rule applied.
- **FIX B confirmation.** scope_index relocation ⇒ scoped `get` raises `TokenStoreIntegrityError` (was silent-success); scope_index relocation ⇒ scopeless `get` raises; expires_at extension (resurrect an expired row) ⇒ `get` raises; expires_at→NULL ⇒ `get` raises. Legitimate rows still decrypt: `expires_at=None` row, real-future-`expires_at` row, multi-scope `list_by_scope`, and `rewrap_all` round-trip all green. The iter-1 guarantees still pass: A2 not-plaintext-on-disk + A3 single-byte-tamper (+ `@given` 120-example property) + row-relocation (blind-index swap) + A6 determinism all remain green (72/72 owned).
- **FIX C confirmation.** `list_by_scope` under wrong KEK now raises `TokenStoreIntegrityError` (was silent `[]`); a genuinely-absent scope (correct KEK) returns `[]`; an empty store returns `[]`. `count()` no-scope under wrong KEK returns the true raw live count (2) and does NOT raise — pinned + conscious-choice code comment added.
- **Full suite (iter-2): 3097 passed / 15 skipped / 9 deselected / 0 failed in 888.34s** (`python3 -m pytest`, post-GREEN). **0 regressions proven by isolation:** the non-owned suite (excluding `tests/test_encrypted_store.py`) = **3025 passed / 0 failed**; full 3097 = 3025 non-owned + **72 owned** (55 iter-1 + 17 iter-2). (The "~3095" parent estimate in the dispatch brief was approximate; the authoritative non-owned count at this parent is 3025, unchanged by the self-contained `encrypted_store.py` edit — verified to have no importers outside its own test + the additive `__init__` export.)
- **Coverage (iter-2): overall 86.85%** (gate ≥84%); **`encrypted_store.py` 100%** (296 statements / 56 branches, 0 missed / 0 partial — the AAD-fold added stmts, all exercised); **`errors.py` 100%**. ruff `All checks passed!` on the owned files (the 46 pre-existing `scripts/*.py` errors are out of scope). mypy --strict `Success: no issues found in 127 source files`.
- **Untouched / user-WIP (iter-2 re-confirmed):** the 5 byte-identical files (`store.py`, `providers.py`, `reidentification.py`, `key_manager.py`, `pii_anon/__init__.py`) all `git diff --quiet` UNCHANGED. user-WIP md5 unchanged: `orchestrator.py` `0afc6deed62bbd0653ae1051b723bace`, `test_moe_enhancements.py` `910e9cd66ad6e38c7bb64a9c51ecb1cb`, `benchmark-diagnostics.json` `47f9b116f7ae667bc4bd1e6aec30e658`, `README.md` `8a0f100040cd0ac523843aac38145002`; `artifacts/benchmarks/*` + `docs/*` never written by iter-2. `errors.py` reused (no duplicate `TokenStoreIntegrityError`). No `pyproject.toml` / dependency change.
- **AAD-format pre-release note.** The iter-2 AAD now binds `key_id ‖ blind_index ‖ version ‖ scope_index ‖ expires_at` (iter-1 bound only the first three). This changes the on-disk authentication binding. `EncryptedSQLiteTokenStore` is a NEW class — iter-1 was never released, so there are no `.db` files in the wild (Pass-2 / never-shipped); the change is **pre-release**, no migration utility needed. Verified self-contained: the only references to the AAD/table internals are `encrypted_store.py`, its test, and the additive `tokenization/__init__` export (the `engines/regex/*` `_aad*` grep hits are unrelated "aadhaar" checksum identifiers).

### iter-1 evidence (5/5 APPROVE — superseded by iter-2 above; retained for cold-pickup history)
- RED commit SHA: **`b67befd`** — `test: S6-03 RED — pin FR-019/FR-026/NFR-014/NFR-015 encrypted token vault (A1–A9)`. Failed at collection with `ModuleNotFoundError: No module named 'pii_anon.tokenization.encrypted_store'` (the module did not yet exist). State CLAIMED→IN_PROGRESS at this commit.
- GREEN commit SHA: **`1f91c8a`** — `feat: S6-03 GREEN — EncryptedSQLiteTokenStore (AEAD-at-rest token vault)`. RED `b67befd` is a git-confirmed ancestor of GREEN (`git merge-base --is-ancestor b67befd 1f91c8a` ⇒ true).
- REFACTOR commit SHA: **`36358f0`** — `refactor: S6-03 — dedup row-encode/row-write helpers (behaviour-identical)`. Extracted `_encrypt_row`/`_write_row` shared by `put` + `rewrap_all`; behaviour-identical (55 owned tests still green; `encrypted_store.py` still 100%).
- Full suite (passed/skipped/deselected/failed) + duration: **2993 passed / 15 skipped / 9 deselected / 0 failed in 1147.69s** (`python3 -m pytest`, post-REFACTOR). Parent baseline post-S2-05 was 2938 passed / 15 skipped / 9 deselected / 0 failed; 2938 + 55 new owned tests = 2993 ⇒ **0 regressions** (skips/deselects unchanged). Owned file: **55 tests pass** (39 test functions; the parametrized shared-behaviour contract runs ×3 stores and the `@given` property expands the collection).
- Coverage % (overall + `encrypted_store.py`): **overall 86.55%** (gate ≥84%); **`encrypted_store.py` 100%** (285 statements / 52 branches, 0 missed, 0 partial — 100% on the encrypt/decrypt/tamper paths); **`errors.py` 100%**. No `# pragma: no cover` needed on any reachable line (the two genuinely-defensive guards retain the existing pragma comments).
- ruff / mypy --strict results: **ruff `All checks passed!`** on the 4 owned files (`encrypted_store.py`, `errors.py`, `tokenization/__init__.py`, `test_encrypted_store.py`); the 46 pre-existing `scripts/*.py` errors are out of scope and none of the owned files appear. **mypy --strict `Success: no issues found in 124 source files`** (`python3 -m mypy src/pii_anon`).
- A2 not-plaintext-on-disk scan result (synthetic plaintext + token ABSENT from raw `.db`): **PASS** — after `put` + `close`, a raw-bytes read of the `.db` confirms `b"jane.roe@example.test"`, `b"Jane Roe"`, and the plaintext token `b"<EMAIL_ADDRESS:v1:tok_abc>"` are all ABSENT, and the entity-type cleartext `b"EMAIL_ADDRESS"` is absent. A plain `sqlite3` per-column dump confirms the value columns are `BLOB` ciphertext (a ciphertext BLOB is present; no string cell equals the plaintext/token). Companion: KEK bytes + the live DEK bytes are ABSENT from the file (NFR-015).
- A3/A4 tamper + wrong-key raise confirmation (`TokenStoreIntegrityError`): **PASS** — A3: a single-byte XOR flip inside a row's ciphertext payload (direct `sqlite3 UPDATE`) ⇒ `get` raises `TokenStoreIntegrityError` (subclass of `TokenizationError`), never returns `None`/wrong bytes; a `@given` property (120 examples over byte position × XOR mask of the payload blob) ALWAYS raises and NEVER yields a successful-but-wrong decrypt; a row-relocation (swap two rows' payloads, keeping each row's blind-index/AAD) ⇒ AAD mismatch ⇒ raise. A4: reopening the same `.db` under a different KEK (`kek_B != kek_A`) ⇒ envelope unwrap fails ⇒ the wrong-key guard raises (artifact-alone / wrong-secret re-join FAILs, NFR-015).
- A5 rotation + DEK-never-on-disk confirmation: **PASS** — rows written under `key_id="k1"` still `get` correctly after rotating to `key_id="k2"` and `put`-ing new rows under k2 (dual-version grace); `rewrap_all(provider_k2)` re-encrypts legacy rows so they then carry `k2` (`row_key_id` ⇒ `"k2"`), and after rewrap the file no longer decrypts under the old KEK_A (raises). The `_keys` envelope stores only `wrapped_dek` (the live DEK bytes are NOT a substring of any envelope blob); `list_key_ids()` enumerates `{k1, k2}` for audit.
- A6 determinism (AX-002) end-to-end confirmation: **PASS** — tokenizing the same `(scope, version, entity_type, plaintext, key)` twice through two independent `DeterministicHMACTokenizer` instances + two separate encrypted stores yields the **identical token** (the HMAC surrogate is independent of the random AEAD nonce), and detokenizing through the encrypted store returns the identical synthetic plaintext both times.
- A7 anon≠pseudo guard (AX-004): **PASS** — a source scan of `encrypted_store.py` confirms it imports nothing from `pii_anon.anonymizer` / `pii_anon.evaluation` / `pii_anon.scoring` and emits no anonymization score; the store exposes only the reversible-mapping surface (`put`/`get`/`list_by_scope`/`count`/`delete_expired`/`close`) and has no `anonymize`/`score`/`irreversible_transform` attribute.
- A8 missing-crypto-dep loud raise (NFR-026): **PASS** — with the AEAD symbol monkeypatched to `None`, both the provider guard and the store-constructor guard raise a loud `TokenizationError` carrying the `pip install pii-anon[crypto]` hint; module import still succeeds (discovery unaffected).
- Untouched-file `git diff --quiet` + user-WIP md5 unchanged: **PASS** — `store.py`, `providers.py`, `reidentification.py`, `key_manager.py`, and `pii_anon/__init__.py` all `git diff --quiet` UNCHANGED (the encrypted store is reachable via `from pii_anon.tokenization import EncryptedSQLiteTokenStore`; `pii_anon/__init__.py` left byte-identical per RISK-2). user-WIP md5 re-confirmed unchanged: `orchestrator.py` `0afc6deed62bbd0653ae1051b723bace`, `test_moe_enhancements.py` `910e9cd66ad6e38c7bb64a9c51ecb1cb`, `benchmark-diagnostics.json` `47f9b116f7ae667bc4bd1e6aec30e658`, `README.md` `8a0f100040cd0ac523843aac38145002`; `artifacts/benchmarks/*` + `docs/*` never written. Narrow per-path `git add` only (no `-A`/`-u`/`.`/`commit -a`). No `pyproject.toml` / dependency change.
- Story-gate verdict (`_reviews/story/S6-03-gate.yaml`): _pending orchestrator dispatch of the 5-reviewer story gate (code-quality + security-sast + traceability + requirements-coverage + axiom-compliance)._
