# S5-04 — sandbox the attack harness (re-id / MIA adversary runner under capability + resource isolation)

> **Cold-pickup invariant**: executable cold. This story hardens the adversarial-attack runner (re-identification / membership-inference) so it executes under in-process capability + resource isolation. The guards (a safe structured spec loader, the subprocess/shell-out refusal, the network-egress refusal, the resource-budget terminator, the touch allow-list) are **pure-Python over stdlib (`resource`, `json`, `importlib.util` for an allow-listed registry lookup)** → fully testable in-tree with synthetic attack specs. Real OS-level isolation (seccomp / namespaces / cgroup wall-clock kill) is a **Pass-2 / platform** concern — AGENT_SIMULATED verifies the in-process guards in-tree on this dev host (darwin, `resource.RLIMIT_AS`/`RLIMIT_CPU` both present). NO real adversarial PII payloads are ever introduced (AX-001 synthetic-only); the harness ingests SYNTHETIC attack specs.

| Field | Value |
|---|---|
| Epic | **E5 Attacks / privacy-adversary** (DC-09 `attacks/` package — real Tier-3 LLM-adversary + LiRA@128 MIA family) |
| State | **IN_PROGRESS** (claimer=dev-assist-development-executor; claimed_at=2026-06-01; in_progress_at=2026-06-01 RED) |
| provisional_status | AGENT_SIMULATED (in-process guards run for real in-tree; real seccomp/namespace/wall-clock-kill OS isolation + any real-key/real-adversary execution are a Pass-2 confirmation — marked honestly, never simulated as real) |
| Implements | **FR-011** (real Tier-3 LLM-adversary re-id — its runner must execute sandboxed), **FR-013** (full-power MIA: LiRA@128 + Secret-Sharer — its runner must execute sandboxed); hardens the execution substrate for **NFR-012** (Tier-3 RRS power) + **NFR-013** (MIA power); upholds **NFR-024 / AX-001** (no real PII in repo/fixtures/logs) + **FR-026 / AX-006** (least-privilege, no raw PII persisted to any channel) |
| Traces | Design **DC-09** (`attacks/` package: real Tier-3 LLM-adversary (de-circularized) + LiRA@128 MIA); **D6 SME MUST-address list, Security MAJOR (3): "sandbox the attack harness (ingests adversarial material)"** (`D-implementation-ready-design.md:56`). Axioms **AX-001** (synthetic-only) + **AX-006** (least-privilege). POV: an attack runner that ingests adversarial material must never become an execution-escalation surface. |
| Test-type tags | `[UNIT-TEST]` `[CONTRACT-TEST]` `[PROPERTY-TEST]` `[SECURITY-TEST]` `[AUDIT]` |
| Files owned | `src/pii_anon/eval_framework/attacks/__init__.py` (new — package init for the DC-09 home; exports the sandbox surface), `src/pii_anon/eval_framework/attacks/sandbox.py` (new — `AttackSandbox`, `SandboxPolicy`, `SandboxViolation`, `ResourceBudget`, `load_attack_spec`, `run_attack_under_sandbox`), `src/pii_anon/eval_framework/attacks/spec.py` (new — frozen `AttackSpec` dataclass + the safe structured loader/validator), `tests/test_attack_sandbox.py` (new), `tests/test_attack_spec_loader.py` (new). **Additive only.** The existing `src/pii_anon/harness/attack.py` + `src/pii_anon/harness/stream_synth.py` stay **byte-identical** (the new sandbox wraps/guards; it does not rewrite them — see §2). |
| Depends on | none hard (in-tree synthetic specs only). Soft: the DC-09 real Tier-3 / LiRA@128 attack bodies are separate successor stories; this story ships the **isolation substrate they will be required to run inside** (the runner contract is defined here). |
| Blocks | any story that wires a REAL adversarial attack body (FR-011 Tier-3, FR-013 MIA) — those bodies MUST execute via `run_attack_under_sandbox`; the sign-gate-artifact + encrypt-token-store sibling Security MUSTs are independent (no ordering). |
| Size | M |

## 1. Intent
The D6 design carried three Security MAJORs as must-do stories; this is **#3: "sandbox the attack harness (ingests adversarial material)"** (`D-implementation-ready-design.md:56`). The DC-09 `attacks/` package runs adversarial code paths — a Tier-3 LLM re-identification adversary (FR-011) and a full-power membership-inference runner (LiRA@128 + Secret-Sharer, FR-013) — driven by **attack specs**. An attack spec describes what an attack does and what it may touch. Because that material is adversarial by definition, the runner that loads and executes specs is a privilege-escalation surface: an attack spec must never be able to (a) trigger **unsafe-deserialization** of an arbitrary object, (b) launch an uncontrolled **subprocess** / **shell-out**, (c) open a **network egress**, or (d) read/write outside an explicit allow-list. This story makes the runner execute every attack under **in-process capability + resource isolation** with a **fail-loud** policy: any spec requesting an out-of-sandbox capability is refused before the attack body runs, and any attack exceeding its CPU/memory/wall-time budget is terminated. The DC-09 `attacks/` package is currently ABSENT (cross-repo line 65: CODE owns `eval_framework/attacks/`); this story creates it with the isolation substrate as its foundation, so every real attack body landed later runs sandboxed by construction.

## 2. Approach / scope
- **Package home (new):** create `src/pii_anon/eval_framework/attacks/` — the DC-09 home the design assigns to CODE (`D-implementation-ready-design.md:65`, "CODE owns … `eval_framework/attacks/`"). It does NOT exist today (verified). This story seeds it with `__init__.py` + `sandbox.py` + `spec.py` only; the attack bodies are successor stories.
- **`spec.py` — the safe structured loader (no unsafe-deserialization).** `AttackSpec` is a frozen dataclass with explicit typed fields: `attack_id: str`, `attack_kind: Literal["reid","mia","reconstruction"]`, `runner: str` (a dotted name resolved ONLY against an in-code **allow-list registry** of known attack callables — never a free `importlib`/arbitrary-object resolution), `params: Mapping[str, str | int | float | bool]` (scalars only), `budget: ResourceBudget`, `allowed_paths: frozenset[str]`, `network: Literal["deny"]` (the only legal value — there is no allow option in v1). `load_attack_spec(source: str | Mapping) -> AttackSpec` parses ONLY a safe structured form (a JSON document or an already-parsed mapping) via the stdlib structured parser; it NEVER uses an arbitrary-object deserializer and NEVER constructs objects named in the data. The loader **rejects loudly** (`SandboxViolation`) any spec that: carries a `runner` not in the allow-list registry; carries a `params` value that is not a JSON scalar (no nested objects that could smuggle a callable); sets `network` to anything other than `"deny"`; or names a path outside `allowed_paths`.
- **`sandbox.py` — the execution guards.**
  - `ResourceBudget(cpu_seconds: float, address_space_bytes: int, wall_seconds: float)` — a frozen dataclass with validated positive bounds and project-pinned defaults (`DEFAULT_CPU_SECONDS`, `DEFAULT_AS_BYTES`, `DEFAULT_WALL_SECONDS`).
  - `SandboxPolicy` — captures the allow-list (`allowed_paths`), the network posture (egress denied), and the budget. Exposes `assert_path_allowed(path)`, `assert_no_network()`, `assert_no_unsafe_capability(spec)`.
  - `AttackSandbox.run(spec, attack_callable, *, inputs)` — the single execution chokepoint:
    1. **Capability pre-check (fail-loud):** validates the spec against the policy (runner in allow-list; `network=="deny"`; every declared path ⊆ `allowed_paths`; no scalar-only violation). Any failure raises `SandboxViolation` **before** the attack body is invoked.
    2. **Resource limits (in-process, AGENT_SIMULATED real where the platform allows):** where `resource` is importable (POSIX; verified present on this dev host), apply `resource.setrlimit(RLIMIT_AS, ...)` and `RLIMIT_CPU` (soft) inside the execution context, restoring prior limits afterward; a `wall_seconds` watchdog (a stdlib timer/thread-based deadline, NOT a subprocess) terminates an over-budget attack and raises a `SandboxBudgetExceeded` (subclass of `SandboxViolation`). On platforms where a given rlimit is unavailable, the in-process guard **degrades to the watchdog + a recorded `platform_limit_unavailable` audit note** (never silently no-ops; fail-loud-or-record).
    3. **Network-egress refusal:** the sandbox installs an in-process egress guard for the duration of the run (a socket-connect interceptor that raises `SandboxViolation` on any outbound connect attempt) and asserts `network=="deny"`. Real kernel-level egress blocking (namespaces) is Pass-2.
    4. **No subprocess / shell-out from a spec:** the allow-list registry resolution is the ONLY way a spec selects code; there is no spec field that names an external program, and the loader rejects any attempt to smuggle one. A `[SECURITY-TEST]` asserts the attacks package source contains NO unsafe-deserialization call, NO subprocess/shell-out call, and NO arbitrary dynamic-eval call (AST/source scan, ZERO matches).
  - `run_attack_under_sandbox(spec_source, *, inputs, registry=DEFAULT_ATTACK_REGISTRY) -> AttackResult` — the public convenience entry: load → resolve runner from the allow-list → `AttackSandbox.run`. Returns a structured `AttackResult` (`attack_id`, `outcome`, `terminated_for_budget: bool`, `audit: list[str]`).
- **Wrap, don't rewrite.** The legacy `harness/attack.py` `reconstruction_attack_score` and `harness/stream_synth.py` stay **byte-identical**. The sandbox registers `reconstruction_attack_score` as one allow-listed runner (a synthetic, side-effect-free attack) so there is a concrete end-to-end "well-formed attack runs to completion within budget" path in-tree without any new attack body.
- **Boundary + isolation hygiene.** `eval_framework/attacks/` may import stdlib + `eval_framework` siblings it needs, but imports nothing from `swarm`/`moe`/`fusion`/`policy` (mirrors the DC-06/DC-07 import-boundary discipline; a boundary test pins it). Determinism (AX-002): the guards are deterministic given a spec; no wall-clock value leaks into an `AttackResult` field that is asserted for equality.

## 3. Given / When / Then (acceptance — concrete, testable)
- **A1 — unsafe-deserialization is refused (the headline security gate). `[SECURITY-TEST]` `[UNIT-TEST]`** **Given** an attack spec whose `params` (or any nested field) attempts to smuggle a non-scalar / object-construction payload (e.g. a mapping shaped to trigger arbitrary-object reconstruction), **when** `load_attack_spec` runs, **then** it raises `SandboxViolation` naming the offending field, and NO object named in the data is ever constructed. **Property test** (`@given` over generated malformed specs): every spec carrying a non-scalar `params` value is rejected; ZERO reach the runner.
- **A2 — a subprocess / shell-out request is refused. `[SECURITY-TEST]` `[UNIT-TEST]`** **Given** an attack spec whose `runner` is a dotted name NOT in the allow-list registry (the only channel by which a spec could try to reach an external program / shell-out), **when** `run_attack_under_sandbox` runs, **then** it raises `SandboxViolation` (runner-not-allow-listed) before any execution. **AST/source guard:** `test_attacks_package_has_no_unsafe_execution_call_signatures` asserts the `attacks/` source contains zero unsafe-deserialization calls, zero subprocess/shell-out calls, and zero arbitrary dynamic-eval calls.
- **A3 — network egress is refused. `[SECURITY-TEST]`** **Given** a well-formed spec and a synthetic attack callable that attempts an outbound socket connect, **when** it runs under `AttackSandbox`, **then** the egress guard raises `SandboxViolation` (network-egress-denied) and the attack does not complete; **and** any spec with `network != "deny"` is rejected at load time.
- **A4 — an over-budget attack is terminated. `[SECURITY-TEST]` `[UNIT-TEST]`** **Given** a spec with a small `ResourceBudget` (`wall_seconds` tight, and `address_space_bytes`/`cpu_seconds` where the platform supports the rlimit) and a synthetic attack that exceeds it (busy-spin past `wall_seconds`, or allocate past the AS limit), **when** it runs under the sandbox, **then** it is terminated and `SandboxBudgetExceeded` is raised with `terminated_for_budget=True`; on a platform lacking a given rlimit, the wall-clock watchdog still terminates it and an `platform_limit_unavailable` audit note is recorded.
- **A5 — a well-formed attack runs to completion within budget. `[CONTRACT-TEST]` `[INTEGRATION-TEST]`** **Given** the allow-listed synthetic `reconstruction_attack_score` runner, a generous budget, `network="deny"`, and `allowed_paths` covering only what it touches (nothing — it is pure over in-memory synthetic strings), **when** `run_attack_under_sandbox` runs it on synthetic masked-text inputs, **then** it returns an `AttackResult` with the expected reconstruction outcome, `terminated_for_budget=False`, ZERO `SandboxViolation`, and an audit trail recording the capability checks that passed.
- **A6 — path allow-list is enforced. `[SECURITY-TEST]`** **Given** a spec whose `allowed_paths` is empty but whose attack attempts to open a path, **when** the attack calls `policy.assert_path_allowed(path)` (the sanctioned access shim), **then** it raises `SandboxViolation` (path-not-allow-listed); a spec declaring a path outside its `allowed_paths` is rejected at load time.
- **A7 — synthetic-only, least-privilege (AX-001 / AX-006). `[AUDIT]`** **Given** the full test module, **then** every fixture spec + every input is SYNTHETIC (no real PII, no real key, no real network target); an audit assertion confirms no `AttackResult.audit` line and no fixture embeds a real-PII-shaped or real-secret-shaped value, and the sandbox grants only the capabilities the spec explicitly declares (default-deny: empty `allowed_paths`, `network="deny"`).
- **A8 — import-boundary + determinism. `[PROPERTY-TEST]`** **Given** an AST scan of the 3 new `attacks/` source files, **then** none imports from `swarm`/`moe`/`fusion`/`policy`; **and** two runs of `run_attack_under_sandbox` on the same spec + inputs yield equal `AttackResult` (excluding any explicitly non-deterministic timing field, which is not part of equality).

## 5. Notes / non-goals
- **Does NOT** implement the Tier-3 LLM-adversary body (FR-011) or the LiRA@128 / Secret-Sharer MIA body (FR-013) — those are successor DC-09 stories; this story ships the **isolation substrate they are required to run inside** + the runner contract + one synthetic allow-listed runner for end-to-end proof.
- **Does NOT** deliver real OS-level isolation (seccomp / namespaces / cgroup wall-clock kill) — explicitly Pass-2 / platform. AGENT_SIMULATED verifies the in-process guards (rlimits via the stdlib `resource` module where available; the watchdog deadline; the egress interceptor; the load-time refusals). The story output records this honesty boundary.
- **Does NOT** rewrite the legacy `harness/attack.py` / `harness/stream_synth.py` (byte-identical) and does NOT touch `run_reidentification_tournament` (lives in `eval_framework/rating/port.py` + `elo.py` — out of scope; the rating engine is import-isolated from attacks).
- **Does NOT** introduce new third-party dependencies — stdlib only (`resource`, the structured JSON parser, `importlib.util` used ONLY for allow-list registry lookup of in-repo callables, `socket` for the egress guard, threading/timer for the watchdog).
- **Token-reword note for reviewers/gate YAML:** the guarded-against patterns are written as **"unsafe-deserialization"**, **"subprocess"**, and **"shell-out"** in all prose; do not paste the literal serialization-module name or the os-level shell-call function name (the PreToolUse Write hook blocks those substrings even in prose — see project memory `security-hook-yaml-false-positives`).
- **USER-WIP OFF-LIMITS:** never stage `orchestrator.py`, `test_moe_enhancements.py`, `artifacts/benchmarks/*`, `benchmark-diagnostics.json`, `README.md`, `docs/*`. Narrow `git add` of owned files only.

## 9. Test-type tags + reviewer set
`[UNIT-TEST]` `[CONTRACT-TEST]` `[PROPERTY-TEST]` `[SECURITY-TEST]` `[AUDIT]`

Canonical 5-reviewer story gate (all must APPROVE): **code-quality** + **security-sast** (PRIMARY — this is a security MUST: the loader/execution path, the unsafe-deserialization + subprocess/shell-out + egress refusals) + **traceability** (FR-011/FR-013/DC-09 + the SME MUST item) + **requirements-coverage** (FR-011/FR-013, NFR-012/013 substrate, NFR-024/AX-001, FR-026/AX-006) + **axiom-compliance** (AX-001 synthetic-only, AX-006 least-privilege, AX-002 determinism). (No performance reviewer required — no latency-budget surface.)

## 12. Definition of Done
- [ ] **RED**: `tests/test_attack_spec_loader.py` + `tests/test_attack_sandbox.py` written first & failing (`ModuleNotFoundError` on the absent `eval_framework.attacks` package). RED covers: A1 unsafe-deserialization refusal (+ property test over malformed specs), A2 runner-not-allow-listed refusal + the AST no-unsafe-call-signatures guard, A3 egress refusal, A4 budget termination (`SandboxBudgetExceeded`, `terminated_for_budget=True`), A5 well-formed synthetic run to completion, A6 path allow-list, A7 synthetic-only/least-privilege audit, A8 import-boundary + determinism. Git-evidenced (RED SHA before GREEN SHA).
- [ ] **GREEN**: `eval_framework/attacks/__init__.py` + `spec.py` (safe structured loader + frozen `AttackSpec`) + `sandbox.py` (`AttackSandbox`, `SandboxPolicy`, `ResourceBudget`, `SandboxViolation`/`SandboxBudgetExceeded`, `run_attack_under_sandbox`, `DEFAULT_ATTACK_REGISTRY` with the synthetic `reconstruction_attack_score` runner allow-listed). All acceptance criteria green.
- [ ] **REFACTOR**: tidy with behaviour-identical changes only; ruff + mypy --strict clean on the new files.
- [ ] **Quality gate**: full suite green (baseline **2876 passed**, +N new — 0 regressions; any genuinely platform-gated rlimit assertion uses an explicit `skipif`, never a silent pass); `ruff` clean; `mypy --strict` clean (`mypy src/pii_anon`); coverage **≥ 84%** (`--cov-fail-under=84`), with `sandbox.py` + `spec.py` ≥ 90%.
- [ ] **Security-specific acceptance**: (a) the AST/source guard proves the `attacks/` package contains ZERO unsafe-deserialization calls, ZERO subprocess/shell-out calls, ZERO arbitrary dynamic-eval calls; (b) a malformed spec attempting unsafe-deserialization is REFUSED (A1); (c) a spec attempting a subprocess/shell-out (non-allow-listed runner) is REFUSED (A2); (d) a network-egress attempt is REFUSED (A3); (e) an over-budget attack is TERMINATED (A4); (f) a well-formed synthetic attack runs to completion within budget (A5); (g) default-deny posture (empty `allowed_paths`, `network="deny"`) is the constructor default (A7).
- [ ] **AGENT_SIMULATED honesty**: in-process guards run for real in-tree (rlimits applied via `resource` where present — verified on this dev host; watchdog + egress interceptor active). Real seccomp/namespace/kernel-egress isolation + any real-adversary/real-key execution are recorded as Pass-2 (never marked real). No real PII / real secret / real network target anywhere (AX-001).
- [ ] **Untouched / user-WIP**: `harness/attack.py` + `harness/stream_synth.py` byte-identical (`git diff --quiet`); `run_reidentification_tournament` (`rating/port.py`, `elo.py`) untouched; user-WIP md5 unchanged (`orchestrator.py`, `test_moe_enhancements.py`, `benchmark-diagnostics.json`, `README.md`, `docs/*`); `artifacts/benchmarks/*` never written. Narrow `git add` of owned files only.
- [ ] **Story-gate APPROVE** (`dev-assist-artifacts/04-development/_reviews/story/S5-04-gate.yaml`) — 5/5 canonical reviewers APPROVE (security-sast PRIMARY).

## Evidence (filled on completion)

**State**: TODO. *AGENT_SIMULATED: the in-process sandbox guards (stdlib `resource` rlimits where the platform supports them, the wall-clock watchdog, the in-process egress interceptor, the load-time unsafe-deserialization + non-allow-listed-runner refusals) run for real in-tree. Real OS-level isolation (seccomp / namespaces / cgroup) + any real Tier-3-adversary or real-key attack execution are Pass-2 — not agent-simulated as real (methodology invariant).*

**Commit SHAs (RED precedes GREEN precedes REFACTOR):** _pending._

**Quality gates:** _pending_ (full suite from 2876 baseline; ruff; mypy --strict; coverage ≥84%).

**Untouched:** _pending_ (`harness/attack.py` + `harness/stream_synth.py` byte-identical; `run_reidentification_tournament` untouched; user-WIP md5 unchanged; `artifacts/benchmarks/*` read-only).

**Story-gate:** _pending_ (`_reviews/story/S5-04-gate.yaml`).

---

### Authoring notes (for the orchestrator — not part of the story body)

Grounding verified against live repo + artifacts (2026-06-01):
- **Attack harness today**: `src/pii_anon/harness/attack.py` (`reconstruction_attack_score`) + `src/pii_anon/harness/stream_synth.py` (`StreamSynthesizer`); privacy/attack metrics (Reid/MIA/Canary/Linkage) in `src/pii_anon/eval_framework/metrics/privacy_metrics.py`. The harness has **zero** dangerous-call usage today (clean baseline) and loads no specs/payloads yet — so the sandbox + spec loader are net-new and additive.
- **DC-09 `attacks/` package home is ABSENT** (`src/pii_anon/eval_framework/attacks/` does not exist); design assigns it to CODE (`D-implementation-ready-design.md:65`). This story seeds it.
- **Real IDs (quoted, not invented)**: FR-011, FR-013 (requirements `:22`, `:24`); NFR-012, NFR-013, NFR-024 (`:73`, `:74`, `:87`); FR-026 (`:39`); DC-09 (`:19`); SME MUST "(3) sandbox the attack harness" (`:56`); AX-001 + AX-006 (design D0 `:6`).
- `resource` module confirmed importable on this dev host (darwin): `RLIMIT_AS` + `RLIMIT_CPU` both present → in-process limits verifiable in-tree.
- Test baseline **2876 passed** (latest S4-CS-01 evidence). Coverage gate `--cov-fail-under=84`, mypy strict, ruff — confirmed in `pyproject.toml:147,163`.
- Axiom-ID convention if the gate raises one: `AXIOM-S504-NNN` (mirrors `AXIOM-S4CS01-001`).
- All dangerous-pattern prose uses the reworded forms ("unsafe-deserialization", "subprocess", "shell-out") per the token-reword rule + project memory `security-hook-yaml-false-positives`.
