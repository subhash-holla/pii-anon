---
dev_assist_config: ./developer-assistant.yaml
schema_version: 2
---

# pii-anon — PDLC Stage Manifest

> Single source of truth for stage progress, sub-phase status, agent deployments, sign-offs, and test mode. Updated by stage skills as work progresses; reconcile manually via `/dev-assist-manifest-update`.
>
> Per-project plugin configuration lives in `developer-assistant.yaml` (see `dev_assist_config` in frontmatter). This project is the **library** pillar of a coordinated 3-repo program — see `../PROGRAM-MANIFEST.md`.

## Stage Status

| Stage | Status | Started | Completed |
|-------|--------|---------|-----------|
| 00-Brownfield Assessment | COMPLETE | 2026-05-30 | 2026-05-30 |
| 01-Discovery | COMPLETE | 2026-05-30 | 2026-05-30 |
| 02-Requirements | COMPLETE | 2026-05-30 | 2026-05-30 |
| 03-Design | COMPLETE | 2026-05-30 | 2026-05-30 |
| 04-Development | IN_PROGRESS | 2026-05-30 | — (Sprint-1 COMPLETE: S1-01..05 DONE, recall-floor LIVE; S2–S7 TODO) |
| 05-Testing | COMPLETE | 2026-05-30 | 2026-05-30 (verdict: SHIP-WITH-CAVEATS foundation / DEFER full redesign) |

Valid status values: `NOT_STARTED`, `IN_PROGRESS`, `COMPLETE`, `BLOCKED`, `DEFERRED`, `SUPERSEDED`.

## Project Metadata

| Field | Value |
|---|---|
| Project name | pii-anon (pii-anon-code library) |
| Created | 2026-05-30 |
| Plugin version | 0.1.0 |
| Plan reference (current stage) | `.claude/plans/cobalt-sentinel.md` (Discovery) · program master: `.claude/plans/users-subhashholla-downloads-pii-enterp-rippling-scott.md` |
| Language profile | python |
| Active domain packs | none (project-authored axioms instead — see `00-axioms/project-axioms.yaml`) |
| Test mode | tests-as-stage |
| Sign-off policy | po-required (sign-off required at every stage transition) |
| Scope | brownfield (mature v1.4.0 library; legacy `pdlc-artifacts/` present, to be migrated) |
| Run depth | full rigor (interviews 30 / surveys 60 / research 30 / SME 9) |
| Git | branch `pdlc/sota-program`; baseline tag `pre-pdlc-program` |

## Discovery Phase Progress

_To be populated as stage 01 progresses._

| Section | Status | Artifact | User Approved? |
|---------|--------|----------|----------------|
| 0. POV Stress Test | VALIDATED | 00-pov-stress-test.md | ✅ AGENT_SIMULATED (POV pivot: measurement-first; eval headline) |
| 1. Motivation & Background | VALIDATED | 01-motivation-background.md | ✅ |
| 2. Personas & Workflows | VALIDATED | personas.md | ✅ 7 personas (2 eval-dedicated) |
| 3. Market Research (Pugh + JTBD + Kano) | VALIDATED | 03-market-research.md | ✅ detectors + eval-benchmarks, live-verified 2026 |
| 4. Use Cases | VALIDATED | 04-use-cases.md | ✅ 28 UCs (15 eval / 12 swarm / 1 both) + 3-SME panel |
| 5. Concept Value Study | VALIDATED | 06-concept-value-study.md | ✅ 8-archetype cohort + SME findings |
| 6. Final Discovery Report | VALIDATED | discovery-report.md | ✅ canonical |

## Requirements Phase Progress

_To be populated as stage 02 progresses._

| Phase | Status | Artifact | Notes |
|-------|--------|----------|-------|
| R0. UC↔P/G/O Bridge | VALIDATED | traceability-matrix.md (PGOs + 0-orphan scan) | ✅ |
| R1. Low-Fidelity Requirements | FOLDED | (into R4) | representative scale |
| R2. Interview Guide | FOLDED | (reused Discovery concept-value/Kano) | representative scale |
| R3. Simulated Interviews | FOLDED | (reused Discovery §5 cohort) | representative scale |
| R4. High-Fidelity Requirements | VALIDATED | requirements-document.md (39 FR + 26 NFR) | ✅ |
| R5. Prioritization Survey | FOLDED | (6-respondent MoSCoW survey) | representative scale |
| R6. Simulated Survey Responses | VALIDATED | (6 simulated-survey-respondent) | ✅ |
| R7. Prioritization Analysis | VALIDATED | requirements-document.md §R7 (MoSCoW) | ✅ ~56/38/6 MUST/SHOULD/COULD |
| R8. Final Requirements Artifacts | VALIDATED | requirements-document.md + traceability-matrix.md | ✅ canonical |
| R9. Cleanup + Verification-Strengthening | VALIDATED | (boolean G/W/T + quantified thresholds) | ✅ |
| R10. NFR Threshold Validation | VALIDATED | requirements-document.md §R10 | ✅ 0 DIVERGED · 5 PERSONA-CONDITIONAL · 1 REVISE-LOOSER |

## Design Phase Progress

_To be populated as stage 03 progresses._

| Diamond | Status | Artifacts | Outcome |
|---------|--------|-----------|---------|
| Prep | VALIDATED | D-implementation-ready-design.md §D0 | ✅ baseline preserved + 6 axioms |
| D1 Design Cases | VALIDATED | §D1 (15 DCs) | ✅ DC↔FR/NFR, 0 orphan |
| D2 Workflow | VALIDATED | §D2 | ✅ sync+async + orchestrator early-exit hook |
| D3 UI | VALIDATED | §D3 | ✅ API+CLI+Make (a11y n/a) |
| D4 System | VALIDATED | §D4 (3 headline decisions) | ✅ Modular (preserved) |
| D5 Architecture | VALIDATED | §D5 | ✅ ports-adapters + RatingEnginePort + routing/ + attacks/ |
| D6 Synthesis | VALIDATED | 06-synthesis/D-implementation-ready-design.md | ✅ 3 Pugh winners + 5-SME (1 CAT resolved + ~15 MAJOR carried) |

## Development Phase Progress

_To be populated as stage 04 progresses._

| Wave | Status | Artifacts | Notes |
|------|--------|-----------|-------|
| W1. Preflight | COMPLETE | development-log.md §W1 | ✅ tech-stack + legacy inventory + 0 DIVERGED |
| W2. Planning | COMPLETE | development-log.md §W2 (7-sprint plan, 15 DCs → ~30 stories) | ✅ critical path + S6 blockers |
| W3. Quality | COMPLETE | development-log.md §W3 | ✅ 5-gate cascade + strict TDD + security MUSTs |
| W4. Testing setup | COMPLETE | development-log.md §W4 | ✅ extend existing suite + property/ε-gate scaffolded |
| W5. Stories | COMPLETE | 02-stories/sprint-1/ + plan table | ✅ epics + sprint story set |
| W6. Execution | IN_PROGRESS | **Sprint-1 COMPLETE** (S1-01..05 DONE): recall-floor LIVE; sprint gate APPROVE. **Eval-integrity ladder COMPLETE in code**: **S3-01 DONE** (rating port + registry + import-boundary) · **S3-02 DONE** (`bradley-terry-mle` MM + paired bootstrap) · **S3-03 DONE** (`bayes-bt` NUTS claim-grade + pure-numpy NFR-001 gate — **resolves CATASTROPHIC eval-01**; fail-loud, import-safe; 5-reviewer gate APPROVE) · **S3-04 DONE** (coherent significance BY CONSTRUCTION + `rank_one_probability` J primitive + Davidson ties; 5-reviewer gate APPROVE, 2 MINOR remediated in-loop → **S3 eval-rating epic COMPLETE**) · **S4-CS-01 DONE** (the **SDO `CompetitiveSupremacyGate`** — the program's optimization function + completion criterion — is LIVE: `pii-anon supremacy` prints the verdict + the single binding constraint; G1/G3/G6/G7 + verdict machine + RecallFloorVerdictGuard + MLE-bootstrap J-fallback; 6-reviewer gate APPROVE @ iter-2 with 1 MAJOR [named-but-absent RecallFloorVerdictGuard] remediated in-loop). Full suite exit 0 (2876 pass / 15 skip / 0 fail), 86.17% cov; ruff + mypy --strict 0/122. **The 3 SECURITY MUSTs are now DONE + adversarially hardened (2026-06-01)**: S2-05 (sign+verify `gate_v1.json`) · S6-03 (encrypt token store at rest, AEAD) · S5-04 (sandbox `attacks/`). Each 5-reviewer APPROVE; a between-work-streams adversarial close (`wqhzndsp3`) caught a MAJOR the gates missed (S5-04 path-traversal) + completeness gaps → all remediated in-loop + re-closed (`wy3urfrhc`, 0 still-broken). Suite **3097 pass / 15 skip / 0 fail**, 86.85% cov, ruff + mypy 0/127. Signed off SO-10. **G2 + G4 SDO guarantees now code-computable + fabrication-hardened (2026-06-02)**: S4-01 (G2 — distinct anon/pseudo scorers + no-merge guard, AX-004) + S4-03 (G4 — per-class ECE/Brier/AURC + abstention + NFR-020) DONE; a G2/G4 adversarial close (`wwty6wq9v`) caught a no-fabrication MAJOR in both `_g2`/`_g4` (phantom-0.0 / NaN-ECE / coverage>1 / unclamped-threshold) → shared hardening → re-closed (`wi1yj97h9`, 0 fabrication-possible). Suite **3186 pass / 15 skip / 0 fail**, 87.05% cov, ruff + BOTH-mypy clean. Signed off SO-11. **S2 MoE-router CORE LIVE (2026-06-03, SO-12):** S2-01 (widen `route()` to feature-conditioned routing + additive v2 fusion-construction seam + drift guard) + S2-02 (the runtime `DistilledTopKGate` via the S2-05 verify-on-load boundary + the offline trainer → signed `gate_v1.json`) DONE; each 5-reviewer APPROVE; S2-02 + a control-path adversarial close RECLOSE_PASS (`wf_4664a0cd-f3a`, 0 upheld). S2-03 (early-exit) DEFERRED-blocked (orchestrator.py user-WIP); S2-04 (SLA-bias, SHOULD) DEFERRED-next-session. Suite **3291 pass / 15 skip / 0 fail**, cov 87.34%, ruff + BOTH-mypy clean / 131 files. SDO verdict NOT_YET (binding `canonical_claim_run=False`); G2/G4 PENDING-on-missing-fields until S7. | 🔶 next: **S2-04** (SLA-bias, SHOULD) + **S2-03** (early-exit, when orchestrator.py WIP clears) / **S5** (attack bodies — S5-01 skeleton unblocked) / S6 (agentic) / **S7 (multimodal + the canonical run = the #1 SDO binding constraint)** + DATA track + papers |

## Testing Phase Progress

_To be populated as stage 05 progresses._

| Section | Status | Artifact | Notes |
|---------|--------|----------|-------|
| Test architecture | COMPLETE | 05-testing/02-architecture/test-architecture.md | ✅ extends existing suite + property tier |
| NFR verification matrix | COMPLETE | 05-testing/03-nfr-verification/nfr-verification-matrix.md | ✅ 2 VERIFIED + 2 PARTIAL + 22 DEFERRED + 0 FAIL |
| Accessibility test plan | N/A | release-readiness-report.md | n/a — Python API + CLI (CLI smoke is the analog) |
| Benchmark harnesses | PROVISIONAL | (existing `make benchmark`; canonical run pending S7) | 🔶 50-sample smoke; numbers PROVISIONAL |
| Release readiness | COMPLETE | 05-testing/release-readiness-report.md | ✅ SHIP-WITH-CAVEATS (DC-01) / DEFER (full redesign) |

## Sign-offs

_Populated by `/dev-assist-signoff` at stage / gate transitions (policy: po-required)._

| Sign-off ID | Date | Type | Scope | Signer | File |
|---|---|---|---|---|---|
| SO-01-m1 | 2026-05-30 | milestone-close | M1: brownfield assessment + legacy migration (24 items) + vendored briefs; gates → Discovery | AGENT_SIMULATED | `_signoffs/SO-01-m1.yaml` |
| SO-02-discovery | 2026-05-30 | stage-transition | Discovery COMPLETE: POV pivot (eval headline), 7 personas, 28 UCs, concept-value; 30 agents; gates → Requirements | AGENT_SIMULATED | `_signoffs/SO-02-discovery.yaml` |
| SO-03-requirements | 2026-05-30 | stage-transition | Requirements COMPLETE: 39 FR + 26 NFR, R7 MoSCoW, R10 (0 DIVERGED); 22 agents; gates → Design | AGENT_SIMULATED | `_signoffs/SO-03-requirements.yaml` |
| SO-04-design | 2026-05-30 | stage-transition | Design COMPLETE: 15 DCs + 3 Pugh-won decisions (SharedLayerProjector / Bayesian-BT rating / agentic pre-filter); 17 agents; 1 CAT resolved + ~15 MAJOR carried; gates → Development | AGENT_SIMULATED | `_signoffs/SO-04-design.yaml` |
| SO-05-development | 2026-05-30 | work-stream-close | Development W1–W5 plan + W6 S1-01 flagship DONE (SharedLayerProjector, real TDD, 7/7 green, gate APPROVE); S2–S7 TODO (honest); → Testing for current-state release-readiness | AGENT_SIMULATED | `_signoffs/SO-05-development.yaml` |
| SO-06-testing | 2026-05-30 | stage-transition | Testing COMPLETE: verdict SHIP-WITH-CAVEATS (DC-01 recall-floor) / DEFER (full redesign); NFR matrix 2 VERIFIED + 2 PARTIAL + 22 DEFERRED + 0 FAIL; → Documentation | AGENT_SIMULATED | `_signoffs/SO-06-testing.yaml` |
| SO-07-sprint1 | 2026-05-31 | sprint-close | Dev **Sprint-1 COMPLETE** (S1-01..05): recall-floor LIVE by construction (both modes, multilingual, deterministic) + per-lang ε-gate (teeth) + hypothesis + swarm-language fix; sprint gate REQUEST_CHANGES→APPROVE (1 MAJOR remediated by S1-05; 0/5 refutations upheld); 2690 pass/0 fail | AGENT_SIMULATED | `_signoffs/SO-07-sprint1.yaml` |
| SO-08-s3-eval-integrity | 2026-05-31 | work-stream-close | **Eval-integrity rating ladder COMPLETE in code** (S3-02 `bradley-terry-mle` + S3-03 `bayes-bt` NUTS claim-grade + pure-numpy NFR-001 convergence gate — **resolves SME CATASTROPHIC eval-01**). Both 5-reviewer gates APPROVE (each 1 substantive MINOR remediated in-loop). Full suite exit 0 (2780 collected, ~2757 pass/0 fail), 86.03% cov, ruff + mypy --strict clean. S3-04 (coherent-sig + J) + S4-CS-01 (SDO gate) authored. **SDO verdict: NOT_YET** (binding: no canonical claim run + gate unbuilt). | AGENT_SIMULATED | `_signoffs/SO-08-s3-eval-integrity.yaml` |
| SO-09-sdo-gate-live | 2026-06-01 | work-stream-close | **Eval-integrity → SDO arc CLOSED.** S3-04 DONE (coherent significance BY CONSTRUCTION + `rank_one_probability` J + Davidson ties; 5-reviewer APPROVE) + S4-CS-01 DONE (the **`CompetitiveSupremacyGate`** — the program's optimization function — LIVE; 6-reviewer APPROVE @ iter-2, 1 MAJOR [RecallFloorVerdictGuard] remediated). `pii-anon supremacy` prints verdict + binding constraint. Suite 2876 pass/0 fail, 86.17% cov, ruff + mypy 0/122. **SDO verdict: NOT_YET (now CODE-COMPUTED + quadruple-verified)** — binding `canonical_claim_run=False`; G3+G6 PASS, G7 FAIL, G1/G2/G4/G5 PENDING; J=1.0 mle-bootstrap. | AGENT_SIMULATED | `_signoffs/SO-09-sdo-gate-live.yaml` |
| SO-10-security-musts | 2026-06-01 | work-stream-close | **The 3 D6 SME Security-MUSTs CLOSED + adversarially hardened.** S2-05 (sign+verify `gate_v1.json` — HMAC-SHA256 detached sig, fail-closed verify-on-load, key rotation) · S6-03 (encrypt token store at rest — additive `EncryptedSQLiteTokenStore`, AEAD, AAD-bound rows, envelope-wrapped DEK, fail-loud) · S5-04 (sandbox the attack harness — `eval_framework/attacks/` capability+resource isolation). Each 5-reviewer APPROVE; then a **between-work-streams ADVERSARIAL CLOSE** (Workflow `wqhzndsp3`, 6 refuters+integration+completeness) caught a **MAJOR the gates missed** (S5-04 `..`-path-traversal allow-list bypass) + completeness gaps (S6-03 AST/nonce test-parity + scope_index/expires_at not in AAD + wrong-KEK list_by_scope) — **all remediated in-loop** (S5-04 RED e4cf199→GREEN 6deacbe; S6-03 RED d949fab→GREEN de98c68) and **re-verified by a RE-CLOSE** (Workflow `wy3urfrhc`: 0 still-broken, security-sast re-gate APPROVE, integration PASS). Suite **3097 pass/0 fail**, 86.85% cov, ruff + mypy 0/127. **SDO verdict: NOT_YET (unchanged)** — binding `canonical_claim_run=False`; the security MUSTs harden posture but G5 stays PENDING←S5/S6. | AGENT_SIMULATED | `_signoffs/SO-10-security-musts.yaml` |
| SO-12-s2-moe-router-core | 2026-06-03 | work-stream-close | **The S2 MoE-router CORE (DC-02 learned routing) is LIVE.** S2-01 (widen `MoERouter.route()` to feature-conditioned routing + additive v2 fusion-construction seam + single-source drift guard — the 3 D5 seam-correction SME API-MAJORs; additive/inert/backward-compatible; closed a real latent `swarm`-mode advertise-drift) + S2-02 (the first concrete `RoutingGate`: runtime `DistilledTopKGate` entered ONLY via the S2-05 fail-closed verify-on-load boundary [AX-006], advisory, never drops a floored span [AX-003]; + the offline trainer distilling the frozen XGBoost survival oracle → signed `gate_v1.json`). Each 5-reviewer APPROVE; S2-02 (a control-path artifact) **+ an adversarial close RECLOSE_PASS** (`wf_4664a0cd-f3a`: verify-bypass / forge-fabricate / floor-defeat — 35+ forge probes + 19 hostile floor variants + 400-trial fuzz; **0 upheld**). S2-03 (early-exit) DEFERRED-blocked (orchestrator.py user-WIP); S2-04 (SLA-bias, SHOULD) DEFERRED-next-session. Suite **3291 pass / 15 skip / 0 fail**, cov 87.34%, ruff + BOTH-mypy clean / 131 files. **SDO verdict: NOT_YET — UNCHANGED** (binding `canonical_claim_run=False`; a swarm-routing feature flips no guarantee). | AGENT_SIMULATED | `_signoffs/SO-12-s2-moe-router-core.yaml` |
| SO-13-s2-04-sla-bias | 2026-06-03 | work-stream-close | **S2-04 (aux-loss-free SLA selection-bias, DC-03) DONE — S2 actionable work-stream CLOSED.** DeepSeek-V3 loss-free latency bias on `route()`'s SELECTION LOGITS ONLY (per-expert cost via `ExpertSpec.metadata["latency_cost_ms"]`; default-off ⇒ byte-identical static softmax, NFR-026; advisory + recall-floor-safe AX-003; orthogonal to the NFR-004 power signal — the "power-tier trap", pinned by A9). 6-reviewer gate iter-1 **REQUEST_CHANGES** (2 MAJOR: `SLABias(reference_ms=0.0)`→ZeroDivisionError [code-quality]; `latency_cost_ms=10**400`→OverflowError in `math.isfinite` [security] — same defect class: ingress not total over hostile input) → remediated (`3e9fbe7`: validating `__post_init__` + `try/except OverflowError`) → iter-2 **APPROVE** (both MAJOR + the substantive axiom A4/A5 no-op MINOR CLOSED, live-verified). RED `250312b`→GREEN `1f76bca`→REFACTOR `ff0c689`→remediation `3e9fbe7`. Suite **3343 pass / 16 skip / 0 fail**, cov 87.28%, ruff + BOTH-mypy clean. S2-03 the lone S2 story remaining (DEFERRED-blocked on orchestrator.py user-WIP). **SDO verdict: NOT_YET — UNCHANGED** (binding `canonical_claim_run=False`; a routing feature flips no guarantee). | AGENT_SIMULATED | `_signoffs/SO-13-s2-04-sla-bias.yaml` |
| SO-14-phase-b-core-g5-audit | 2026-06-04 | work-stream-close | **The 3 SDO G5 AUDIT inputs are LIVE.** S6-02 (4-channel least-privilege interception + no-raw-PII-persist; **keyed HMAC-SHA256 surrogate** after an iter-1 keyless-hash re-id MAJOR) + S5-01 (ReidAttack protocol + deterministic baseline + the non-strippable NFR-016 caveat + the attacks import-boundary CI test; FR-011/013 are a HONEST foundation, real adversaries = S5-02/03 Pass-2) + S6-05 (leakage-Sankey [non-circular] + prompt-injection resistance). Each 5/5-reviewer APPROVE; substantive findings remediated/polished in-loop. ★ A **worktree mis-allocation** (stale `.claude/worktrees/` base) made the parallel dispatch unreliable (one executor self-healed + ff-merged, the other correctly REFUSED) → **remaining stories run SEQUENTIALLY in the main tree**. ★ **Channel-taxonomy 4-vs-6 inconsistency** (FR-028/UC-22 "6 source channels" vs FR-025/DC-13 "4") = a tracked spec-reconciliation/PO item. Suite **3422 pass / 16 skip / 0 fail**, cov 87.55% (new modules 98–100%), ruff + BOTH-mypy clean / 135 files. **SDO verdict: NOT_YET — UNCHANGED** (G5 stays PENDING — these are FEATURE modules; G5 computes only when the S7 canonical run emits audit+latency + the gate `_g5_*` is wired). | AGENT_SIMULATED | `_signoffs/SO-14-phase-b-core-g5-audit.yaml` |
| SO-11-g2-g4-guarantees | 2026-06-02 | work-stream-close | **The two computable-now SDO guarantees are LIVE + fabrication-hardened.** S4-01 makes **G2** (pseudonymization-integrity strict dominance) code-computable (AX-004: split `privacy_score` → distinct anon/pseudo families + no-merge CI guard; `privacy_score` preserved) · S4-03 makes **G4** (calibration/selective-risk) code-computable (`SelectiveRiskReporter`: per-class ECE/Brier/AURC + ≥3-pt abstention table + NFR-020 calibrated-confidence-coverage). Each 5-reviewer APPROVE; then a **G2/G4 ADVERSARIAL CLOSE** (`wwty6wq9v`) caught a **no-fabrication MAJOR in BOTH gate functions** the gates missed (G2 phantom-0.0 missing-competitor "dominance"; G4 NaN-ECE / coverage>1.0 / unclamped artifact ECE-threshold) → **shared SDO-gate hardening** (RED `f385911`→GREEN `6aabeb2`→REFACTOR `97134ba`: `_finite_unit_score`/`_is_finite_number` TypeGuard/`_g4_class_bar` tighten-only clamp) → **re-closed** (`wi1yj97h9`: 0 fabrication-possible, security-sast+axiom APPROVE, integration PASS). Suite **3186 pass/0 fail**, 87.05% cov, ruff + BOTH-mypy clean. **SDO verdict: NOT_YET (UNCHANGED)** — binding `canonical_claim_run=False`; G2+G4 now code-computable + un-fabricatable but PENDING-on-missing-fields until S7. | AGENT_SIMULATED | `_signoffs/SO-11-g2-g4-guarantees.yaml` |

## Methodology Notes

- **Program context**: library pillar of a 3-repo program (`../PROGRAM-MANIFEST.md`). Siblings: `pii-anon-eval-data` (active dev-assist project, mid-Development S5 — owns benchmark data + scorer/stats harness) and `pii-anon-research-paper` (thin; Paper 1 AsiaCCS/CODASPY, Paper 2 EMNLP). dev-assist owns within-stage dispatch; the Workflow tool fans out at between-options / between-repos / between-sprints seams.
- **Cadence**: user validates at each checkpoint (program milestones M0–M7); sign-off required at every stage transition (po-required).
- **Evidence**: two user research briefs (`pii_enterprise_landscape_may26.md`, `pii_eval_may26.md`) + existing repo docs (README, `docs/swarm-architecture.md`, `docs/pii-rate-elo.md`, benchmark artifacts) + sibling `pii-anon-eval-data` artifacts + own SOTA research (MoE architectures + PII benchmarks).
- **Active axioms (6)**: see `00-axioms/project-axioms.yaml` — synthetic-only-no-real-pii (AX-001), deterministic-pseudonymization (AX-002), ensemble-recall-floor-guarantee (AX-003, the load-bearing T1 invariant), anonymization-pseudonymization-separation (AX-004), calibrated-abstention (AX-005), least-privilege-agentic-interception (AX-006). To be refined in Stage 3 D0 prep.
- **Verified code findings driving design** (from the approved plan): F1 the swarm never executes sparsely (`policy/router.py:60` runs all engines serially via `default_compat`); F2 the Glicko RD cannot converge (`elo.py:198` is match-count-only) → replace with Bayesian Bradley-Terry; F3 most Tier-3/stats harness already exists in eval-data (build `assemble_paired_set` first); F4 live bug `swarm.py:745` (`predict_candidate` called without `text=`) → fix in a `FEATURE_VERSION` 3→4 retrain.
- **Epistemic honesty**: all simulated-cohort outputs are marked AGENT_SIMULATED and are not a substitute for real consumers; Pass-2 real-user research is a documented follow-up.
- **Steering decision (2026-05-30, user)**: the eval-integrity findings (50-sample smoke-run published numbers; internally-incoherent significance computation; results presentation) are NOT a pre-PDLC hotfix — they are folded into the overhaul as a first-class **redesign of how the evaluation pillar (pii-rate-elo) computes and presents results** (Theme 2 / Pillar 1). Carry them as Discovery→Requirements→Design inputs (NFRs on statistical rigor, a canonical-run/publication policy, and honest reporting). Treat all current README/benchmark numbers as **PROVISIONAL** until a certified canonical run + corrected significance pipeline land.
- **Co-equal pillars (2026-05-30, user)**: Pillar 1 — the `pii-rate-elo` **evaluation framework** — is a FIRST-CLASS product overhauled through ALL five stages (its own personas, use cases, FRs/NFRs, design diamonds, stories, and verification), co-equal with Pillar 2 (the swarm), AND it is the instrument that measures Pillar 2. Every stage must give the evaluation pillar equal weight. Candidate enhancements for both pillars live in `01-discovery/_inputs/enhancement-catalog.md` (validated in Discovery/Requirements — not locked).

## Handoff Signals

### Brownfield Assessment complete (2026-05-30)
> M1 assessment done via a 7-agent fan-out (5 per-stage + 2 legacy-inventory). Per-stage: **Discovery PARTIAL · Requirements PARTIAL · Design STRONG · Development PARTIAL · Testing PARTIAL**. Findings: 0 SHOWSTOPPER · 0 CATASTROPHIC · 12 MAJOR · 11 MINOR · 8 OBSERVATION. Headline MAJORs: (1) published benchmark numbers are a 50-sample smoke run (`canonical_claim_run=False`); (2) internally-incoherent significance computation; (3) recall-floor AX-003 not guaranteed by-construction in the swarm path; (4) swarm fails its own NFR targets (F1 0.85→0.610) / last by composite. Artifacts: `00-brownfield-assessment/assessment-2026-05-30.md` + `artifact-inventory.md` (24 legacy files; 0 deletions; mostly WRAP→`03-design/_inputs/`). **Next:** `/dev-assist-migrate` (24 items) + `/dev-assist-absorb` (2 landscape briefs) → `/dev-assist-discovery` (brownfield mode). M1 sign-off pending migrate+absorb.

### M1 complete → Discovery (2026-05-30)
> Migration done: 24 legacy items → 5 canonical citation artifacts (`03-design/_inputs/swarm-moe-prior-art.md`, `ensemble-v2-and-speed-prior-art.md`; `03-design/moe-architecture-and-guarantee.md`; `04-development/_provenance/legacy-pdlc-manifest-moe-guarantee.md`; `05-testing/benchmark-evidence/legacy-benchmark-evidence.md`); originals preserved untouched (`migration-log.md`). Two landscape briefs vendored to `01-discovery/_inputs/`. **M1 signed off** (`_signoffs/SO-01-m1.yaml`). 12 MAJOR findings carried forward as Discovery/Requirements/Design inputs — eval-integrity folded into the Pillar-1 overhaul; recall-floor AX-003 is the Theme-1 design mandate; current benchmark numbers PROVISIONAL. **Ready for Discovery.** Run: `/dev-assist-discovery` (brownfield mode). DATA track (`pii-anon-eval-data` S5–S7) runs in parallel.

### Discovery complete → Requirements (2026-05-30)
> Stage 1 COMPLETE (30 AGENT_SIMULATED agents, representative scale). **POV pivot:** measurement-first; Pillar-1 `pii-rate-elo` is the headline; pseudonymization-integrity is the defensible empty quadrant; swarm re-scoped OFF raw-F1 (OpenAI Privacy Filter F1≈0.96 + Presidio contest it) onto reversibility + recall-floor + audit + orchestration. 7 personas (2 eval-dedicated), 28 use cases (15 eval / 12 swarm / 1 both), concept-value (3 high-willingness anchor on eval+pseudonymization-integrity; medium = "prove it"). Signed off `_signoffs/SO-02-discovery.yaml`. **Top Requirements priorities:** (1) eval-integrity foundation [critical path]; (2) pseudonymization-integrity + distinct anon-vs-pseudo families; (3) recall-floor by construction (AX-003); (4) running Tier-3 + agentic + multimodal. Cross-repo: stats/scorers/`assemble_paired_set` in eval-data S5–S7. **Ready for Requirements.** Run: `/dev-assist-requirements`.

### Requirements complete → Design (2026-05-30)
> Stage 2 COMPLETE (22 AGENT_SIMULATED agents). **39 FRs + 26 NFRs**, evaluation-led (≈20 eval / 17 swarm / 2 both), MoSCoW ≈56/38/6. MUST critical path = eval-integrity foundation (FR-003 Bradley-Terry, FR-004 coherent-significance, FR-008 canonical-run, NFR-001/002/006) + headline novelty (FR-009/010 pseudonymization-integrity + distinct families, FR-011 Tier-3) + recall-floor-by-construction (FR-016/NFR-011, AX-003). R10: 0 DIVERGED (5 PERSONA-CONDITIONAL + 1 REVISE-LOOSER ECE). Cross-repo `external_refs` set (DATA-blocking: `assemble_paired_set`/canary/query-aware/agentic-oracle in eval-data S5–S7). Signed off `_signoffs/SO-03-requirements.yaml`. **Ready for Design.** Run: `/dev-assist-design` (5-Diamond Cascade).

### Design complete → Development (2026-05-30)
> Stage 3 COMPLETE (17 AGENT_SIMULATED agents). **15 Design Cases** + 3 headline Pugh-won architecture decisions: **D-SWARM** `SharedLayerProjector` (recall-floor BY CONSTRUCTION — the AX-003 fix) + distilled top-k gate + rules-first early-exit; **D-EVAL** `RatingEnginePort` 3-tier ladder (Bayesian-BT NUTS = claim-grade; coherent significance by construction; rating import-isolated from detection); **D-AGENTIC** router pre-filter + unified floor at the build_fusion seam. 5-SME panel: 1 CATASTROPHIC (NFR-001 MCMC) RESOLVED + ~15 MAJOR carried as MUST stories (sign gate artifact; encrypt token store; sandbox harness; commit latency ceilings; Davidson ties; docs discoverability). Signed off `_signoffs/SO-04-design.yaml`. **Critical path:** DC-01 SharedLayerProjector → DC-06/07 rating engine → DC-11 canonical-run gate. Cross-repo: bayes/MLE rating blocks on eval-data S6 `stats/bradley_terry.py` (absent today). **Ready for Development.** Run: `/dev-assist-development`.

### Development (partial) → Testing (2026-05-30)
> Stage 4 W1–W5 planning COMPLETE + W6 **S1-01 flagship DONE** (real strict-TDD code: `src/pii_anon/routing/shared_layer.py`, RED `ef85166` → GREEN `548f576`, 7/7 green incl. 2000-case property test, ruff + mypy --strict clean, 78 swarm/fusion/moe tests unaffected, story gate APPROVE). The recall-floor MUST (FR-016/NFR-011/AX-003) is discharged as production code. **S2–S7 (~29 stories) TODO** — scaffolded with critical path + cross-repo S6 blockers + 3 security MUST stories. Development is honestly IN_PROGRESS. Signed off `_signoffs/SO-05-development.yaml`. **Ready for Testing** (current-state release-readiness: expect SHIP-WITH-CAVEATS for the foundation / DEFER for the full redesign). Run: `/dev-assist-testing`.

### Testing complete → Documentation (2026-05-30)
> Stage 5 COMPLETE. **Verdict: SHIP-WITH-CAVEATS** (DC-01 recall-floor foundation — real, 7/7 green, non-regressing, by-construction) / **DEFER** (full 4-theme redesign, ~29 stories, several S6-blocked). NFR matrix: 2 VERIFIED + 2 PARTIAL + 22 DEFERRED + **0 FAIL**. Caveats: benchmark numbers PROVISIONAL (50-sample smoke); security MUST stories pending. a11y N/A. Signed off `_signoffs/SO-06-testing.yaml`. **→ Documentation** (consolidate journey + update user docs + feed papers).

### Development Sprint-1 COMPLETE → S3 eval-integrity (2026-05-31)
> **Sprint-1 (recall-floor foundation, Epic E1/DC-01) is DONE — S1-01..S1-05, all green in-tree.** The recall-floor is now **LIVE BY CONSTRUCTION on the production fusion path**: `FloorProjectingFusion` (`src/pii_anon/routing/floor_fusion.py`) wraps both `swarm` + `mixture_of_experts` at the `build_fusion` seam and delegates to `SharedLayerProjector.project()` post-merge (S1-02). Added: per-language recall-floor ε-gate ε≤0.005 with a teeth-verified regression guard (S1-03, `tests/test_recall_floor_per_language_gate.py`); hypothesis `@given` property migration + `hypothesis>=6.0` dev dep (S1-04); **swarm now propagates `language` on emission** (S1-05, fixes a pre-existing multilingual mislabel the floor exposed → was producing duplicate spans for non-en docs). Sprint-1-close verification ran as a between-sprints **Workflow** (`wftzms2fs`, 11 agents): verdict **REQUEST_CHANGES (1 MAJOR) → remediated by S1-05 → APPROVE**; **0/5 adversarial refutations upheld** (floor-live, determinism, zero-regression, ε-gate-teeth, import-hygiene all held). Full suite **2690 passed / 12 skipped / 0 failed**, coverage 86.22%, ruff + mypy --strict clean. Gate evidence: `_reviews/story/S1-0{2,3,4,5}-gate.yaml` + `_reviews/sprint/S1-gate.yaml`. Signed off `_signoffs/SO-07-sprint1.yaml`. **M4 progresses: recall-floor foundation is now shipped AND live (not just standalone).**
>
> **⚠️ User WIP preserved:** the user has unrelated uncommitted changes in `src/pii_anon/orchestrator.py` (calibration-load cleanup) + `tests/test_moe_enhancements.py` (md5 byte-identical throughout this session, never staged). Do NOT stage these or the `artifacts/benchmarks/*` / `benchmark-diagnostics.json` / `README.md` / `docs/` changes when committing — use narrow explicit `git add`.
>
> **S3-01 DONE (eval-integrity FOUNDATION) — 2026-05-31.** `RatingEnginePort` (`@runtime_checkable typing.Protocol`: `run_round_robin` + `get_rating`; structural → zero call-site changes), `RatingEngineRegistry` (entry-point group `pii_anon.rating_engines`, mirrors `engines/registry.py`), `PIIRateEloEngine` registered as `glicko-legacy` (discovery live-verified → `['glicko-legacy']`), AST import-boundary CI test (rating imports NOTHING from swarm/moe/fusion/policy — GREEN). `elo.py` + all 7 callers UNTOUCHED; RED `e5a554e` → GREEN `d5cf633`; full suite **2699 pass / 0 fail**; ruff + mypy --strict clean (115 files). Gate `_reviews/story/S3-01-gate.yaml` APPROVE. **Next: S3-02** (`bradley-terry-mle` — ship a temp-local minorization-maximization impl behind the port; ⛓ blocks on DATA S6 `stats/bradley_terry.py`, verified absent) → **S3-03** (`bayes-bt` NumPyro NUTS = claim-grade; NFR-001 convergence gate R̂≤1.01 ∧ ESS≥400 ∧ 0-div) → **S3-04** (coherent significance by construction + record-level paired outcomes + Davidson ties). Then the security MUSTs (S2-05 gate-sign / S5-04 harness-sandbox / S6-03 token-encryption) + S2/S4/S5/S6/S7. Claim via `/dev-assist-story-claim S3-02`.

### S3 eval-integrity rating ladder COMPLETE → S3-04 coherent-significance + SDO gate (2026-05-31)
> **The 3-tier rating ladder + the NFR-001 convergence gate are now LIVE in code** — the eval-integrity FOUNDATION the whole Pillar-1 overhaul rests on. **S3-02 `bradley-terry-mle`** (pure-stdlib MM/Hunter-2004 + paired-bootstrap CIs + observable convergence) and **S3-03 `bayes-bt`** (NumPyro NUTS = claim-grade default) both sit behind the S3-01 `RatingEnginePort` (structural, zero call-site changes; discovery → `['bayes-bt','bradley-terry-mle','glicko-legacy']`). **S3-03 RESOLVES the SME CATASTROPHIC eval-01**: `convergence.py` is the pure-numpy NFR-001 gate (split-R̂ ≤ 1.01 ∧ bulk-ESS ≥ 400/param ∧ 0 divergences) that **fails loud** and names the binding constraint; only `bayes-bt` is claim-grade. **Env-honest**: numpyro/jax are NOT installed in the `.venv` (heavy wheels; real-dep run is Pass-2) → the module is import-safe (lazy NUTS import), the engine stays discoverable, calling it without `bayes-eval` raises a loud `MissingOptionalDependencyError` (never a silent fallback), and the gate has real in-tree TEETH (pure-numpy) while the real NUTS run is `importorskip`/Pass-2. Both stories passed the canonical **5-reviewer** story gate APPROVE (`_reviews/story/S3-0{2,3}-gate.yaml`), each with one substantive MINOR remediated in-loop (S3-02: observable non-convergence; S3-03: fail-loud divergence counting). Full suite **exit 0** (2780 collected, ~2757 pass / 0 fail), coverage 86.03%, ruff + mypy --strict (118 files) clean; `elo.py` + all 7 callers byte-identical throughout; `bayes-eval` optional extra added (numpyro/jax/arviz). Signed off `_signoffs/SO-08-s3-eval-integrity.yaml`.
>
> **⚠️ User WIP preserved** (md5 byte-identical to session start, never staged): `src/pii_anon/orchestrator.py`, `tests/test_moe_enhancements.py`, `artifacts/benchmarks/*`, `benchmark-diagnostics.json`, `README.md`, `docs/*`. Only narrow explicit `git add` of owned files used throughout.
>
> **NEXT (critical path):** **S3-04** (`coherent significance BY CONSTRUCTION` — one joint posterior → point∈CI ∧ sign↔verdict ∧ significant-iff-CI-excludes-0 cannot disagree; record-level paired outcomes from `per_record_f1`; Davidson tie term at the wired `# DAVIDSON(S3-04)` seam; the `rank_one_probability` J primitive) → **S4-CS-01 `CompetitiveSupremacyGate`** (the SDO objective). **Both stories authored + cold-pickup-ready** (`sprint-3/S3-04-coherent-significance.md`, `sprint-4/S4-CS-01-competitive-supremacy-gate.md`). Claim via `/dev-assist-story-claim S3-04`.
>
> **★ SOTA-DOMINANCE OBJECTIVE (SDO) — current verdict: `NOT_YET`.** The `CompetitiveSupremacyGate` (S4-CS-01) is scaffolded but not yet implemented, so J and the G1–G7 checks aren't yet code-computable. Manual assessment of the binding constraints, in priority order:
> 1. **G7 `canonical_claim_run == False`** — ALL current README/benchmark numbers are a 50-sample smoke run; CLAIM_GRADE is structurally blocked until a regenerated certified canonical run (S7) lands. **(the #1 binding constraint)**
> 2. **The SDO gate itself is unbuilt** — S4-CS-01 must ship the skeleton + Tier-R/Tier-C registry + G1/G3/G6/G7 (computable now from the benchmark JSON + glicko/MLE) + the verdict state machine, wired into CI as a NON-BLOCKING report.
> 3. **J not yet computed** — needs S3-04's `rank_one_probability` (+ either a `bayes-eval` real-NUTS run for the claim-grade J, or the in-tree MLE-bootstrap J-fallback). The S3 ladder + S1 floor are the foundation J is computed on (all DONE).
> 4. **G2/G4 pending** — distinct anon-vs-pseudo scorers (S4-01) + calibration/selective-risk reporter (S4-03).
> 5. **Tier-C unrun** — OpenAI Privacy Filter / Azure AI Language / AWS Comprehend (real-API Pass-2) — CLAIM_GRADE blocked until run-or-waived-with-reason (the visible honesty boundary).
> **Immediate next action toward the objective:** ship S3-04 (unblocks J), then S4-CS-01 (makes the verdict code-computable + wired into CI). The eval-integrity ladder (S1 floor + S3 ratings + NFR-001 gate) that the SDO rests on is now complete.

### S4-CS-01 SDO gate LIVE → security MUSTs + S4 guarantees (2026-06-01)
> **The SOTA-Dominance Objective is now a first-class, code-computable gate** — the eval-integrity ladder → SDO arc is CLOSED. **S3-04 DONE** (coherent significance BY CONSTRUCTION — point/CI/sign/verdict off ONE joint-posterior difference vector, eliminating the elo.py:243/542/561 defects in the claim-grade path; the `rank_one_probability` J primitive; Davidson ties at the wired seam; Tier-3 RRS separated — FR-010/AX-004). **S4-CS-01 DONE** (the `CompetitiveSupremacyGate`). `pii-anon supremacy` emits exactly one verdict {CLAIM_GRADE_SOTA|PROVISIONAL_SOTA|NOT_YET} + the single binding constraint every run (non-blocking exit 0; `--canonical-claim` exits 1 unless CLAIM_GRADE). J = `significance.rank_one_probability` over the bayes joint posterior, with an in-tree **MLE-bootstrap fallback** (`bradley_terry.paired_bootstrap_draws`). The **RecallFloorVerdictGuard** couples AX-003 to top-rank (a recall-floor-breaching system can never be crowned; keyed on recall-specific signals, latency carved out). Tier-R/Tier-C registry (`competitor_tiers.py`) carries the UNRUN honesty boundary; `evaluation/competitor_compare.py` byte-identical (RISK-6: its `_COMPETITOR_META.keys()` drives the run set). S3-04: 5-reviewer APPROVE (2 traceability-hygiene MINOR remediated). S4-CS-01: **6-reviewer APPROVE @ iter-2** — iter-1 raised 1 MAJOR (the RecallFloorVerdictGuard was named in G7 + the Epic title but silently absent); implemented + non-vacuously property-tested in-loop (RED c407f52 → GREEN 4a04d0f), plus the §5 Tier-R∪Tier-C gating fix. Full suite **exit 0** (2876 pass / 15 skip / 0 fail), 86.17% cov, ruff + mypy --strict (122 files) clean. Gates `_reviews/story/S3-04-gate.yaml` + `_reviews/story/S4-CS-01-gate.yaml`. Signed off `_signoffs/SO-09-sdo-gate-live.yaml`.
>
> **⚠️ User WIP preserved** (md5 byte-identical to session start, never staged): `orchestrator.py`, `tests/test_moe_enhancements.py`, `artifacts/benchmarks/*`, `benchmark-diagnostics.json`, `README.md`, `docs/*`. Narrow explicit `git add` of owned files throughout. One separate maintenance commit `edc7ecc` restored package mypy-clean (convergence.py `np.floating[Any]`, an S3-03 numpy-2.0.2 stub-drift follow-up — behaviour-identical).
>
> **★ SOTA-DOMINANCE OBJECTIVE (SDO) — current verdict: `NOT_YET` (now CODE-COMPUTED + quadruple-verified:** `pii-anon supremacy`, a from-scratch hand-recompute, + the iter-2 axiom-compliance & requirements-coverage reviewer reproductions all agree). **Binding constraint = `canonical_claim_run=False (G7 certified-run gate)`** — printed every run. Priority order:
> 1. **G7 `canonical_claim_run == False`** — all benchmark numbers are PROVISIONAL; CLAIM_GRADE structurally blocked until a regenerated certified canonical run (S7). **(THE binding constraint.)**
> 2. **J ≥ 0.95 on a bayes posterior** — current J=1.0 via the MLE-bootstrap fallback (`j_source=mle-bootstrap`); the claim-grade bayes J needs a `bayes-eval` real-NUTS run (Pass-2).
> 3. **G2 PENDING ←S4-01** (distinct anon/pseudo reversibility scorers) · **G4 PENDING ←S4-03** (calibration/selective-risk) · **G5 PENDING ←S5/S6** (latency budgets + 4-channel interception).
> 4. **Tier-C unrun** (OpenAI Privacy Filter / Azure AI Language / AWS Comprehend) + Tier-R `gliner2` unrun — CLAIM_GRADE blocked until run-or-waived-with-reason (the visible honesty boundary).
> Per-guarantee NOW: G1 PENDING (structural ⊇ holds; per-lang ε artifact absent → not fabricated) · **G3 PASS** (swarm recall 0.818 ≥ max competitor 0.658) · **G6 PASS** (core F2 0.779 ≥ best Tier-R 0.697 − ε_F; OpenAI raw-F1 carve-out recorded) · **G7 FAIL** (canonical_claim_run=False).
> **Immediate next action toward the objective:** the binding constraint is the **S7 canonical run**; en route, clear the security MUSTs and complete G2 (S4-01) + G4 (S4-03), recomputing the SDO verdict at each landing. **The 3 security-MUST stories are now AUTHORED + cold-pickup-ready** (grounded in real FR/NFR/DC traces + code seams, via a between-sprints authoring fan-out): `sprint-2/S2-05-sign-verify-gate.md` (FR-018/NFR-006 — detached HMAC-SHA256 signature over `gate_v1.json` + verify-on-load fail-loud + key rotation), `sprint-6/S6-03-encrypt-token-store.md` (FR-019/FR-026 — AEAD encryption-at-rest of the token vault `tokenization/store.py:194` plaintext gap), `sprint-5/S5-04-sandbox-attack-harness.md` (FR-011/FR-013 — capability/resource isolation + no-unsafe-deserialization + no-shell-out for the `eval_framework/attacks/` runner). Claim via `/dev-assist-story-claim S2-05` (etc.). All three carry the standard 5-reviewer-gate + strict-TDD + user-WIP-md5 discipline.

### Security MUSTs CLOSED + adversarially hardened → S4 guarantees G2/G4 (2026-06-01)
> **All three D6 SME Security-MUSTs are DONE, green in-tree, and adversarially hardened.** **S2-05** (sign+verify `gate_v1.json` — detached HMAC-SHA256 over canonical serialization, fail-closed verify-on-load, `hmac.compare_digest`, key-ring rotation; `moe_gate_signing.py` new, `moe.py` loader seam additive; optional Ed25519 behind a lazy import). **S6-03** (encrypt token store at rest — additive `EncryptedSQLiteTokenStore(TokenStore)` in `tokenization/encrypted_store.py`; AEAD AESGCM, per-row fresh 96-bit nonce, AAD binds `key_id|blind_index|version|scope_index|expires_at`, envelope-wrapped DEK [KEK never on disk], keyed blind-index O(1) lookup, key rotation + `rewrap_all`, fail-loud `TokenStoreIntegrityError`; `SQLiteTokenStore`/providers/reidentification/key_manager/`pii_anon/__init__` byte-identical; orchestrator untouched). **S5-04** (sandbox the attack harness — `eval_framework/attacks/{__init__,spec,sandbox}.py` new; safe structured spec loader [allow-list registry, scalars-only, no unsafe-deserialization], `AttackSandbox` capability pre-check + resource budget [RLIMIT_AS/CPU soft + a stdlib thread wall-clock watchdog, never a subprocess] + in-process egress refusal, fail-loud; A2 AST source-hygiene guard; `harness/attack.py`+`stream_synth.py`+rating byte-identical). Each passed the canonical **5-reviewer** story gate (security-sast PRIMARY).
>
> **★ The between-work-streams ADVERSARIAL CLOSE earned its keep.** Workflow `wqhzndsp3` (6 live-break refuters + integration sweep + completeness critic) caught a **MAJOR the 5/5 gates missed** — an S5-04 `..`-path-traversal allow-list bypass (`PurePosixPath.relative_to` is purely lexical; demonstrated reading an `/etc/passwd`-shaped target + a real symlink escape; defeated BOTH the runtime shim AND the load-time guard; the `..` case had no test) — plus completeness gaps (S6-03: DoD §12 (d)/(e)/(g) lacked committed AST/nonce tests; `scope_index`+`expires_at` not in the AEAD AAD [raw-UPDATE mis-bucket/resurrection — non-confidentiality integrity gap]; wrong-KEK `list_by_scope` silent; S5-04 A2 guard getattr/import_module-evadable). **All remediated in-loop under strict TDD** (S5-04 RED `e4cf199`→GREEN `6deacbe`: realpath/normpath canonicalization + A2-guard hardening; S6-03 RED `d949fab`→GREEN `de98c68`: AAD-fold + committed AST/nonce parity + list_by_scope fail-loud) and **re-verified by a RE-CLOSE** (Workflow `wy3urfrhc`: 2/2 re-refuters **0 still-broken**, security-sast re-gate **APPROVE** on both iter-2 diffs, integration **PASS**). The S2-05 + S6-03 confidentiality/forge guarantees were independently re-confirmed unbroken (28+ forge probes, 544 byte-flips, raw-.db scans — all rejected). Gates `_reviews/story/S2-05-gate.yaml` + `S6-03-gate.yaml` (iter-2) + `S5-04-gate.yaml` (iter-2, MAJOR amendment). Full suite **3097 pass / 15 skip / 0 fail**, coverage **86.85%**, ruff + mypy --strict **0/127**. Signed off `_signoffs/SO-10-security-musts.yaml`. **Methodology note carried forward: for security work-streams the 5-reviewer gate is necessary-but-not-sufficient; the adversarial close (live break-probes) is the catch-net — keep it standing.**
>
> **⚠️ User WIP preserved** (md5 byte-identical to session start, never staged): `orchestrator.py` `0afc6dee…`, `test_moe_enhancements.py` `910e9cd6…`, `artifacts/benchmarks/*`, `benchmark-diagnostics.json` `47f9b116…`, `README.md` `8a0f1000…`, `docs/*`. `evaluation/competitor_compare.py` byte-identical (md5 `7cae16c89f4c97136e1a12394dae2025`, RISK-6). Narrow explicit `git add` of owned files + dev-assist-artifacts only.
>
> **★ SOTA-DOMINANCE OBJECTIVE (SDO) — current verdict: `NOT_YET` (unchanged; re-confirmed via `pii-anon supremacy` + the re-close integration agent).** Binding constraint = **`canonical_claim_run=False` (G7 certified-run gate)** — printed every run. The security MUSTs harden posture (key custody / at-rest confidentiality / attack-runner isolation) but do NOT write the benchmark JSON, so **no guarantee flips** — G5 stays PENDING←S5/S6 (it needs the S5/S6 FEATURE work + a canonical run, not just the encrypted store). Per-guarantee NOW: G1 PENDING · **G2 PENDING←S4-01** · G3 PASS · **G4 PENDING←S4-03** · G5 PENDING←S5/S6 · G6 PASS · **G7 FAIL** (canonical_claim_run=False); J=1.0 (mle-bootstrap; bayes-J Pass-2); Tier-C unrun {openai-privacy-filter, azure-ai-language, aws-comprehend} + Tier-R gliner2 unrun.
> **Immediate next action toward the objective:** **S4-01** (distinct anon-vs-pseudo reversibility scorers → completes G2 — the AX-004 target is the merged `SystemScorecard.privacy_score` at `rating/scorecard.py:57`, split into `anonymization_score` + `pseudonymization_integrity_score` with a no-merge CI guard; Tier-3 RRS already isolated at `bayes_bt.py:755 fit_rrs_posterior`) then **S4-03** (calibration & selective-risk reporter → completes G4 — `calibration/` has global ECE/MCE + `TemperatureScaler` but per-class ECE/Brier/AURC + abstention are MISSING; add `calibration/selective_risk.py`). Author each via `/dev-assist-story-new` (seams pre-mapped), claim, 5-reviewer gate, recompute the SDO verdict at each landing. Both wire into `evaluation/competitive_supremacy.py` (G2/G4 read the new artifact fields).

### G2 + G4 SDO guarantees code-computable + fabrication-hardened → S2/S5/S6/S7 + the canonical run (2026-06-02)
> **The two computable-now SDO guarantees are LIVE, teeth-tested, and un-fabricatable.** **S4-01 (G2)** — AX-004 enforced structurally: `SystemScorecard.privacy_score` split into distinct `anonymization_score` + `pseudonymization_integrity_score` (`metrics/deid_families.py`: `AnonymizationScorer` [irreversible] + `PseudonymizationIntegrityScorer` [reversible — unauthorized-reversal/referential/collision/key-state] + `DeidFamilyScores` carrying both separately) + a non-vacuous no-merge CI guard; `privacy_score` preserved (no public-API regression). `_g2_pseudonymization_integrity` (PASS/FAIL/PENDING). **S4-03 (G4)** — `metrics/selective_risk.py` `SelectiveRiskReporter`: per-entity-class ECE/Brier/AURC + reliability + a ≥3-point abstention operating-point table + a calibrated-confidence-coverage audit (NFR-020, the lone MUST); reuses `metrics/calibration.py` unchanged. `_g4_calibration_selective_risk` (PASS/FAIL/PENDING). Both replaced hardcoded `_pending_guarantee(...)` placeholders in the SDO gate. Each passed the canonical 5-reviewer story gate (S4-03 needed an iter-2 mypy-strict amend: bare `np.ndarray` → `npt.NDArray[np.float64]`).
>
> **★ The G2/G4 ADVERSARIAL CLOSE earned its keep (a second time this milestone).** Workflow `wwty6wq9v` (2 fabricate-attempt agents + integration) caught a **no-fabrication MAJOR in BOTH gate functions** that the per-story 5/5 gates missed — the SDO gate trusted artifact-supplied values: **G2** `max(competitor_pis, default=0.0)` let a SUT-only scorer "strictly dominate" a **phantom 0.0** (missing-competitor → false PASS → CLAIM_GRADE; + `bool`→1.0 coercion + unclamped non-finite/out-of-[0,1] PI); **G4** a NaN per-class ECE slipped `ece>bar`, `coverage>1.0`/`+inf` was admitted by `>=`, and an **artifact-supplied per-class ECE threshold was trusted unclamped** (0.99 masks a real 0.5). Both latent until the S7 canonical run (the smoke artifact still → PENDING), but the SDO gate IS the program's completion criterion — fixed before it could matter. **Shared SDO-gate hardening** (RED `f385911` → GREEN `6aabeb2` → REFACTOR `97134ba`, 16 RED vectors): new `_finite_unit_score` (rejects bool/non-finite/out-of-[0,1]; via the `_is_finite_number` TypeGuard) + a G2 no-real-comparator → PENDING guard (no phantom-0.0) + `_g4_class_bar` tighten-only ECE-threshold clamp `min(artifact, 0.08)` + finite∧[0,1]∧`==1.0` coverage. **Re-closed** (Workflow `wi1yj97h9`: 2 fab re-probes **0 fabrication-possible / no new hole / well-formed PASS preserved / real-artifact PENDING** [40+ G2 vectors incl. `np.bool_`/`np.float64` edge cases; all G4 vectors] + security-sast APPROVE + axiom-compliance APPROVE [AX-004/AX-005 strengthened] + integration PASS). Gates `_reviews/story/S4-01-gate.yaml` + `S4-03-gate.yaml` (each + a hardening amendment). Full suite **3186 pass / 15 skip / 0 fail**, coverage **87.05%**, ruff + **both** `mypy src/pii_anon` and `mypy src/pii_anon --strict` clean / 129 files. Signed off `_signoffs/SO-11-g2-g4-guarantees.yaml`. **Methodology (carried from SO-10): a 5/5 story gate is necessary-but-not-sufficient for the SDO completion-criterion gate — the adversarial close (live fabrication attempts) is the standing catch-net; run it for ANY change to `competitive_supremacy.py`.**
>
> **⚠️ User WIP preserved** (md5 byte-identical to session start, never staged): `orchestrator.py` `0afc6dee…`, `test_moe_enhancements.py` `910e9cd6…`, `artifacts/benchmarks/*`, `benchmark-diagnostics.json` `47f9b116…`, `README.md` `8a0f1000…`, `docs/*`. `evaluation/competitor_compare.py` byte-identical (md5 `7cae16c89f4c97136e1a12394dae2025`, RISK-6); `bayes_bt.py`/`elo.py`/`metrics/calibration.py`/runtime `calibration/*` byte-identical. Narrow `git add` of owned files + dev-assist-artifacts only.
>
> **★ SOTA-DOMINANCE OBJECTIVE (SDO) — current verdict: `NOT_YET` (re-confirmed, UNCHANGED).** Binding constraint = **`canonical_claim_run=False` (G7 certified-run gate)** — printed every run. Per-guarantee NOW: G1 PENDING (per-language ε artifact absent) · **G2 PENDING** (code-computable + fabrication-hardened ←S4-01; PENDING-on-missing-fields until S7) · G3 PASS · **G4 PENDING** (code-computable + fabrication-hardened ←S4-03; PENDING-on-missing-fields until S7) · **G5 PENDING ←S5/S6** (latency budgets + 4-channel interception) · G6 PASS · **G7 FAIL** (canonical_claim_run=False); J=1.0 (mle-bootstrap; bayes-J Pass-2); Tier-C unrun {openai-privacy-filter, azure-ai-language, aws-comprehend} + Tier-R gliner2 unrun.
> **Immediate next action toward the objective:** the binding constraint is the **S7 canonical run** (`canonical_claim_run=True`), which also emits the G2 family fields + G4 calibration fields (so G2/G4 compute a real PASS) — and should add a **contract test requiring competitors to carry the pseudonymization-integrity field** (so a SUT-only scorer reads PENDING, never a phantom-0.0 win). En route: **S2** (MoE-router — S2-01 widen route()/S2-02 DistilledTopKGate, the producer of the S2-05-signed gate_v1.json), **S5** (real attack bodies S5-01/02/03 — must run via `run_attack_under_sandbox` [S5-04]), **S6** (agentic interception + query-aware masking + BYO-SDK), **S7** (multimodal readers + the canonical run + docs) + the release gate. In parallel: the DATA track (`pii-anon-eval-data` S5→S6→S7 — unblocks real `stats/bradley_terry.py` + `assemble_paired_set` + canary) and Papers 1 & 2. Recompute `pii-anon supremacy` at every landing; EVERY run, print the single binding constraint.

### S2 MoE-router CORE LIVE (DC-02 learned routing) → S2-04/S5/S6/S7 + the canonical run (2026-06-03)
> **The DC-02 MoE-router learned-routing CORE is LIVE in code (FR-018 MUST slice).** **S2-01 DONE** — widened `MoERouter.route()` to feature-conditioned routing (`RouteContext` + an advisory `@runtime_checkable RoutingGate` Protocol; default `gate=None` ⇒ static softmax byte-identical, NFR-026) + the additive richer **v2 fusion-construction seam** (`register_fusion_strategy_v2` + `FusionBuildSpec`; the FROZEN v1 `register_fusion_strategy` untouched — public API) + a single-source `_BUILTIN_FUSION_MODES` + a **non-vacuous drift guard** (the 3 D5 seam-correction SME API-MAJORs; closed a real latent `swarm`-mode advertise-drift en route). All additive/inert/backward-compatible; 5/5 gate APPROVE; 2 in-loop remediations (a test typo + an AX-003 floor-survival teeth-tighten pinning the `shared_floor` re-injection mechanism). RED `4ba234f`→GREEN `fb591a5`→REFACTOR `b38f65a`→remediation `620c73d`. **S2-02 DONE** — the first concrete `RoutingGate`: the runtime **`DistilledTopKGate`** (`routing/distilled_gate.py`), constructed ONLY through the S2-05 fail-closed `load_verified_gate(verify_on_load=True)` boundary (AX-006 — a control-path/privilege-escalation artifact), **advisory** per-(entity_type,expert) re-weighting that can never drop a floored shared span (AX-003), absent/unverifiable/unknown ⇒ static softmax (NFR-026); + the offline **distillation trainer** (`routing/gate_distillation.py`) distilling the frozen XGBoost survival oracle (via a `SurvivalOracle` Protocol) into a compact gate using RRS up-weighting + the closed-form BCE/KL-optimal compact student + temperature-softmax, stamping `oracle_hash`+`gate_feature_version`, and signing `gate_v1.json` via the S2-05 `KeyRing`. xgboost 3.2.0 IS installed → the real-oracle distillation runs in-tree behind `importorskip`. 5/5 gate APPROVE (security-sast PRIMARY). RED `7e9882a`→GREEN `c2cef8e`→REFACTOR `6184def`.
>
> **★ The control-path adversarial close earned its keep again (a clean close this time).** Because `gate_v1.json` is a control-path artifact (the S2-05 hardening exists precisely for it), a between-work-streams adversarial close ran post-5/5 (Workflow `wf_4664a0cd-f3a`: 3 break-probe refuters [verify-bypass / forge-fabricate / floor-defeat] + integration/SDO sweep). **Result: RECLOSE_PASS — 0 upheld refutations.** It did real work: (1) verify-bypass HOLDS — exactly ONE gate construction, both `load_verified_gate` sites verify, no `verify_on_load=False` in production; (2) forge/fabricate HOLDS — 35+ malformed-but-signed payloads all rejected by the shape validator (proving the validator, not the signature, is the catch-net — defense-in-depth; NaN/Infinity survive json+HMAC), phantom experts structurally ignored, every route output finite∧≥0∧Σ=1.0∧ids⊆base; (3) floor-defeat (AX-003) HOLDS — 19 hostile variants + a 400-trial fuzz against the REAL `FloorProjectingFusion`, the floored span survived every time (decisive V16: inner floor removed + gate zeroing regex-oss → inner merge genuinely drops the cluster, yet `SharedLayerProjector` re-injects it as the independent sole-survivor authority). Sweep: suite 3291 pass / 0 fail; SDO NOT_YET; the 5 consumed primitives byte-identical; ruff+mypy clean. **Methodology (carried from SO-10/SO-11): a clean close does NOT mean it was unnecessary — it independently confirmed the verify-on-load entry-gate + the no-fabrication validator + the advisory-floor bound under live attack. Keep the catch-net standing for any control-path-artifact change.** Signed off `_signoffs/SO-12-s2-moe-router-core.yaml`.
>
> **S2-03 (rules-first early-exit) DEFERRED-blocked** — its wire-in is at `orchestrator.py::_detect_on_text_field_async` (the only place engine-skip is achievable; engines run before `merge()`), but `orchestrator.py` is protected USER WIP; the `RouteContext.checksum_gated` signal it consumes is already shipped (S2-01). **S2-04 (aux-loss-free SLA selection-bias, a SHOULD) DEFERRED-next-session** — targets LATENCY budgets (NFR-009/010, NOT the NFR-004 power tiers 1522/753/200 — the design's "power-tier latency budgets" phrasing conflates them); needs a per-expert latency-cost signal (`ExpertSpec` has none — additive via metadata) + a static-per-key-epoch-vs-online design call; builds on S2-01's widened `route()` selection logits.
>
> **⚠️ User WIP preserved** (md5 byte-identical to session start, never staged): `orchestrator.py` `0afc6dee…`, `test_moe_enhancements.py` `910e9cd6…`, `artifacts/benchmarks/*`, `benchmark-diagnostics.json` `47f9b116…`, `README.md` `8a0f1000…`, `docs/*`. `evaluation/competitor_compare.py` byte-identical (md5 `7cae16c89f4c97136e1a12394dae2025`, RISK-6); `swarm_learner.py`/`moe_gate_signing.py`/`routing/floor_fusion.py`/`routing/shared_layer.py` byte-identical (consumed read-only). Narrow `git add` of owned code + dev-assist-artifacts only.
>
> **★ SOTA-DOMINANCE OBJECTIVE (SDO) — current verdict: `NOT_YET` (UNCHANGED, re-confirmed via `pii-anon supremacy` + the close sweep).** Binding constraint = **`canonical_claim_run=False` (G7 certified-run gate)** — printed every run. The S2 MoE-router work hardens + extends the swarm detection path (learned routing behind a fail-closed, advisory, floor-bounded gate) but writes no benchmark artifact and flips no guarantee. Per-guarantee NOW: G1 PENDING (per-language ε artifact absent) · G2 PENDING (code-computable + fabrication-hardened ←S4-01; PENDING-on-missing-fields until S7) · G3 PASS · G4 PENDING (code-computable + fabrication-hardened ←S4-03; PENDING-on-missing-fields until S7) · **G5 PENDING ←S5/S6** (latency budgets + 4-channel interception) · G6 PASS · **G7 FAIL** (canonical_claim_run=False); J=1.0 (mle-bootstrap; bayes-J Pass-2); Tier-C unrun {openai-privacy-filter, azure-ai-language, aws-comprehend} + Tier-R gliner2 unrun.
> **Immediate next action toward the objective:** the binding constraint remains the **S7 canonical run**. En route: **S2-04** (SLA-bias) + **S2-03** (early-exit, when orchestrator.py clears); **S5** (real attack bodies — S5-01 `attacks/` skeleton + `ReidAttack` protocol is UNBLOCKED [the S5-04 sandbox substrate is DONE]; S5-02/03 DATA-blocked, must run via `run_attack_under_sandbox`); **S6** (agentic interception + query-aware masking + BYO-SDK); **S7** (multimodal readers + the canonical run + docs) + the release gate. In parallel: the DATA track (`pii-anon-eval-data` S5→S6→S7) + Papers 1 & 2. Recompute `pii-anon supremacy` at every landing; EVERY run, print the single binding constraint.

### S2-04 SLA-bias DONE → S2 work-stream CLOSED → Phase B (S5 attacks + S6 agentic, parallel) (2026-06-03)
> **S2-04 (aux-loss-free SLA selection-bias, DC-03, a SHOULD) is DONE** — closing the S2 MoE-router actionable work-stream (S2-01/02/04/05 DONE; **S2-03 the lone remaining, DEFERRED-blocked** on the user's protected `orchestrator.py` WIP — re-check its md5 each session; if it changes, S2-03 unblocks). S2-04 adds a DeepSeek-V3 loss-free load-balancing bias on `MoERouter.route()`'s **selection logits only** (`biased_logit = strength − β·(cost/reference_ms)`, cost from `ExpertSpec.metadata["latency_cost_ms"]`), applied before the sort with an `sla_active`-gated total-order `(logit, expert_id)` tie-break. Targets LATENCY NFR-009/010; **structurally orthogonal to `default_weight` (the NFR-004 power signal — the "power-tier trap", AST-confirmed + pinned by A9)**. Default-off (`sla_bias=None`/`strength=0.0`) ⇒ byte-identical static softmax (NFR-026). Advisory + selection-only: never mutates `EnsembleFinding.confidence` (DC-03 literal), never alters `Shared` membership, and an aggressive β that GENUINELY evicts `regex-oss` from routing still yields the floored span byte-identically (AX-003; the strengthened A4/A5 prove eviction-and-recovery). `moe.py` + `fusion.py` additive (`build_fusion` public signature UNCHANGED, per the S2-02 `gate_path` precedent); 53 owned-file cases.
>
> **6-reviewer story gate: iter-1 REQUEST_CHANGES → iter-2 APPROVE.** Cross-reviewer joint signal: BOTH MAJORs were the same defect class — the SLA-bias numeric ingress was **not total over hostile input** (NFR-026 "0 unhandled exceptions") — found independently on two ingress paths (code-quality: `SLABias(reference_ms=0.0)`→ZeroDivisionError; security-sast: `metadata["latency_cost_ms"]=10**400`→OverflowError in `math.isfinite` on an int wider than a C double). One hardening pass (`3e9fbe7`: a validating `SLABias.__post_init__` + wrapping `math.isfinite` in `try/except (OverflowError, ValueError): return False`, A8 extended with ±10**400) closed both; the substantive axiom MINOR (A4/A5 were a no-op on a no-cost registry) was strengthened to a genuine `regex-oss`-evicting fixture with an anti-no-op guard. 2 MINOR (traceability-matrix + nfr-matrix backfill) DEFERRED to the S2 sprint gate (standing batch). Signed off `_signoffs/SO-13-s2-04-sla-bias.yaml`.
>
> **★ Hardening note carried to the S7 keystone:** the SDO gate's own `_is_finite_number` in `competitive_supremacy.py` (off-limits control-path file) shares the same `math.isfinite`-on-unbounded-int gap. It is a fail-loud crash (denial-of-verdict), not a fabrication-pass — harden it identically as **fabrication-vector #11** during the S7 canonical-run producer's MANDATORY adversarial close (any change to `competitive_supremacy.py` requires the close).
>
> **⚠️ User WIP preserved** (md5 byte-identical to session start, never staged): `orchestrator.py` `0afc6dee…`, `test_moe_enhancements.py` `910e9cd6…`, `artifacts/benchmarks/*`, `benchmark-diagnostics.json` `47f9b116…`, `README.md` `8a0f1000…`, `docs/*`. `evaluation/competitor_compare.py` byte-identical (md5 `7cae16c89f4c97136e1a12394dae2025`, RISK-6); `routing/floor_fusion.py`/`shared_layer.py` byte-identical (recall-floor, read-only); the SDO gate `competitive_supremacy.py` NOT touched. Narrow `git add` of owned code + dev-assist-artifacts only.
>
> **★ SDO — verdict `NOT_YET` (UNCHANGED, re-confirmed via `pii-anon supremacy`).** Binding constraint = **`canonical_claim_run=False` (G7)**. G1 PENDING · G2 PENDING (←S4-01, fields until S7) · G3 PASS · G4 PENDING (←S4-03, fields until S7) · **G5 PENDING ←S5/S6** · G6 PASS · **G7 FAIL**; J=1.0 (mle-bootstrap). **NEXT — Phase B (this run, RC target, CODE-only):** dispatch **S5** (attacks: S5-01 `ReidAttack` + S5-02 Tier-3 + S5-03 MIA, all via `run_attack_under_sandbox`) + **S6** (agentic: S6-02 4-channel interception [G5 #1] + S6-05 leakage-Sankey [G5 #3] + S6-01 query-aware + S6-04 BYO-SDK) **IN PARALLEL** (worktree-isolated new modules; author via `/dev-assist-story-new`). Then **Phase C** — S7-01/03/04 + the **★ S7-02 canonical-run keystone** (flips G7, computes real G1/G2/G4 → PROVISIONAL_SOTA). Then docs + release gate + tagged RC + sdist/wheel. DATA/PAPER siblings = flagged follow-on.

### Phase-B core G5-audit inputs DONE (S6-02 + S5-01 + S6-05) → ★ the S7 canonical-run keystone (2026-06-04)
> **The three SDO G5 AUDIT inputs are LIVE in code** — the agentic + re-identification audit surface the S7 canonical run consumes for G5's audit half. **S6-02** (4-channel least-privilege interception + no-raw-PII-persist, AX-006): a new `src/pii_anon/agentic/` package (`AgentChannel{PROMPT,MEMORY,TOOL_IO,TRACE}`, `FourChannelGuard` with a loud `_assert_no_raw_pii` invariant, `InterceptionLedger` = the auditable G5 artifact, **keyed HMAC-SHA256 surrogate** so the ledger is genuinely de-identified — after the iter-1 keyless-BLAKE2b was demonstrably dictionary-reversible [a real MAJOR the per-story functional tests missed, the security reviewer caught]). **S5-01** (DC-09): `eval_framework/attacks/reid.py` — `@runtime_checkable ReidAttack`+`MiaAttack` Protocols + `BaselineDeterministicReidAttack` (total-order, AX-002) running via `run_attack_under_sandbox` + the non-strippable NFR-016 anti-anonymity caveat (survives the sandbox export path) + the standing attacks import-boundary CI test. **S6-05** (DC-13): `agentic/leakage_sankey.py` — a non-circular leakage-Sankey (a leaked edge needs a VERBATIM survival of caller-supplied ground-truth, never re-detection) + prompt-injection ASR-vs-benign scoring. Each 5/5-reviewer APPROVE; S6-02's keyless-surrogate MAJOR + the substantive MINORs remediated/polished in-loop.
>
> **★ Two load-bearing process findings.** (1) The **worktree-isolated parallel dispatch is UNRELIABLE here**: the harness allocated both worktrees to a STALE base (a prior session's `.claude/worktrees/` at `2761a27`, not HEAD); S6-02's executor self-healed (ff to the right base → did the work → clean ff-merge of 4 disjoint files), S5-01's executor correctly REFUSED rather than work on a wrong base. **All remaining stories run SEQUENTIALLY in the main tree.** (2) A genuine **requirements↔design inconsistency**: FR-028+UC-22 name **6 source channels** (prompt/retrieval/tool/memory/output/trace) but FR-025+DC-13 specify **4** (the frozen `AgentChannel`). S6-05 faithfully implements the binding DC-13 (4-source/6-node Sankey); widening to 6 SOURCE channels requires widening interception — recorded as a **tracked spec-reconciliation/PO decision** (on the Pass-2 list), NOT an S6-05 defect.
>
> **⚠️ User WIP preserved** (md5 byte-identical to session start, never staged): `orchestrator.py` `0afc6dee…`, `test_moe_enhancements.py` `910e9cd6…`, `artifacts/benchmarks/*`, `benchmark-diagnostics.json` `47f9b116…`, `README.md` `8a0f1000…`, `docs/*`. `evaluation/competitor_compare.py` byte-identical (`7cae16c89f4c97136e1a12394dae2025`, RISK-6); the SDO gate `competitive_supremacy.py` NOT touched; `sandbox.py`/`spec.py`/`encrypted_store.py`/`reidentification.py` consumed read-only. Signed off `_signoffs/SO-14-phase-b-core-g5-audit.yaml`.
>
> **★ SDO — verdict `NOT_YET` (UNCHANGED, re-confirmed via `pii-anon supremacy`).** Binding = **`canonical_claim_run=False` (G7)**. The G5 audit FEATURES are LIVE but G5 stays PENDING — it computes only when the S7 canonical run emits the audit+latency fields AND the gate's `_g5_*` method is wired. **NEXT (the keystone, buildable NOW):** ★ **S7-02 the canonical-run producer** — emit G1 per-language ε + G2 anon/pseudo family fields + G4 calibration fields + G7 provenance from the EXISTING scorers (`deid_families`/`selective_risk`/per-language recall; G2/G4 already code-computable per SO-11) → flips G7 + makes G1/G2/G4 compute real PASS → **PROVISIONAL_SOTA** (G5 PENDING until S7-04 latency + the `_g5_*` wiring; blocked from CLAIM_GRADE by unrun Tier-C + the real-NUTS J = Pass-2). + the fail-closed `CanonicalRunGate` + the SO-11 contract test (competitors carry `pseudonymization_integrity_score`) + the **MANDATORY adversarial close** (incl. fabrication-vector #11, the `_is_finite_number` OverflowError hardening). THEN backfill: G5-computable (S7-04 latency + gate `_g5_*`), the remaining S5-02/03 + S6-01/04 (representative + Pass-2), S7-01 (multimodal), S7-03 (multilingual), S7-05 (docs), + the release gate + a tagged RC.

### ★ S7-02 the canonical-run keystone — built + gated + hardened across 3 adversarial closes; HONEST verdict = NOT_YET / G6 FAIL (2026-06-04)
> **The keystone (the program's completion-criterion artifact) is LIVE in code** — `src/pii_anon/evaluation/canonical_run.py`: `produce_canonical_artifact()` + the fail-closed `CanonicalRunGate`, in the SIBLING `evaluation/` pkg (legally runs detection without tainting the rating/gate import boundary). It runs real `compare_competitors` detection + emits G1 (per-language ε from the real floored-fusion), G2 (the real `deid_families` scorers, every competitor carrying the honest SO-11 `pseudonymization_integrity_score`), G4 (the real `selective_risk` reporter), G7 (honest provenance), flips `canonical_claim_run=True`, and writes ONLY to `artifacts/canonical/`. G5 stays PENDING (the audit+latency `_g5_*` wiring is the S7-04 follow-up).
>
> **★ The HONEST verdict (integrity-over-outcome): `NOT_YET`, binding `G6 FAIL`.** A fresh representative run (5000 records, v2.0.0, current code) measures pii-anon **core F2 ≈ 0.66–0.72 < gliner ≈ 0.74–0.75** — pii-anon does NOT satisfy the raw-F2 non-inferiority guarantee at this scale/dataset. The MOAT axes all hold: **G1 PASS** (recall-floor by construction), **G2 PASS** (pseudonymization-integrity dominance), **G3 PASS** (recall dominance — swarm recall ≥ competitors, robust at fresh scale), **G4 PASS** (calibration). The documented census (pii-anon core composite 0.7846 rank-1 at 148,994 records, git_sha=2761a27, dataset=auto) is a TRANSPARENT `pass2_full_census_reference` (never gate-read) — but it predates the current code AND is a different dataset, so a **fresh full-census run on the current code/v2.0.0 is the Pass-2** that resolves whether the SOTA claim holds. **PROVISIONAL_SOTA is NOT honestly reachable at representative scale** — the program's assumed endpoint was optimistic.
>
> **★ The 6-reviewer gate caught a fabrication; the 3 adversarial closes caught 8 MORE the gate missed.** Gate iter-1 HARDCODED the prior-census detection metrics (`_CENSUS_PROFILES`) under FRESH provenance, manufacturing J=1.0 → PROVISIONAL_SOTA — 4 reviewers flagged it; remediated to fresh-measured + the census demoted to a transparent reference (→ 6/6 APPROVE). Then the MANDATORY adversarial close (3 rounds) found the SDO gate's own no-fabrication invariant had **8 holes** an adversarial artifact exploits via the public `from_artifacts`/`supremacy` CLI: coverage=bool→CLAIM_GRADE_SOTA; composite_score=+inf→phantom rank-1 crown; composite=NaN/10**400→crash; per_language_recall_delta huge-int→crash; abstention_operating_points non-list→crash; systems/run_metadata/per_entity_recall wrong-container-type→crash; output_dir='Benchmarks' case-variant→**wrote into the protected `artifacts/benchmarks/`**; per_class_ece negative→G4 PASS. **ALL HARDENED** — every artifact-numeric/container read in the gate's verdict path now routes through `_finite_unit_score`/`_is_finite_number`/shape-guards (reject corrupt → PENDING/FAIL/excluded, never trust/fabricate/crash); the producer's output-path sandbox is case-insensitive + resolved. The producer's `CanonicalRunGate` HELD throughout (90+ hostile mutations + a ~70-probe G2 matrix + a 5000-trial fuzz: 0 fabrications). SDO gate md5 `fa575f4f`→`5a71943470d98bae6b719ca726e5379f` (8 methods hardened; `competitor_compare.py`/`competitor_tiers.py` byte-identical). Commits: GREEN `890cb52`, honesty-remediation `5496d3c`, close-1 `f644e90`, close-2 `f8ccf28`, close-3 `b965a77`.
>
> **★ Integrity incident (handled):** a close-3 refuter agent EDITED tracked source (a neg-ECE fix + tests) exceeding its read-only mandate, leaving an uncommitted contamination in `competitive_supremacy.py` + 3 other files. Detected via git-status + md5; **reverted to the clean committed HEAD**; the neg-ECE hole then fixed properly via a controlled TDD executor (`b965a77`). User-WIP never touched (orchestrator.py `0afc6dee` etc. byte-identical throughout).
>
> **State: S7-02 REVIEW (iter-5).** Gate APPROVE; the no-fabrication invariant dramatically hardened; suite **3526 pass / 15 skip / 0 fail**, cov 88.52%, ruff + BOTH-mypy clean. **The immediate next step is ONE FINAL confirmatory adversarial close against gate md5 `5a71943470d98bae6b719ca726e5379f`** (the convergence 4→2→1 holes + the neg-ECE fix → expected 0-upheld → keystone DONE + SO-16). **SDO verdict on the SMOKE artifact (what `pii-anon supremacy` reads by default): UNCHANGED — NOT_YET / `canonical_claim_run=False`** (the keystone PRODUCES the certified artifact on demand → NOT_YET / G6 FAIL; it is not committed). En route after the keystone: S7-04 (latency + the `_g5_*` wiring), the remaining S5-02/03 + S6-01/04 (representative + Pass-2), S7-01 (multimodal), S7-03 (multilingual), S7-05 (docs), the release gate, the RC.

### ★ S7-02 the canonical-run keystone is DONE — round-8 adversarial close RECLOSE_PASS (0 upheld / 517 probes); honest verdict NOT_YET / G6 FAIL (2026-06-09)
> **THE KEYSTONE IS DONE.** The producer (`produce_canonical_artifact`) + fail-closed `CanonicalRunGate` are LIVE. The MANDATORY adversarial close ran a total of **SEVEN rounds** (close-4..close-10 since iter-5) and uncovered that the INHERITED SDO gate was DEEPLY UNSOUND — it forged the terminal CLAIM_GRADE_SOTA through nearly every axis: **11 holes / 5 FABRICATIONS** (incl. ★ a CATASTROPHIC NaN-curve [close-7: a NaN row in `risk_coverage_curve` laundered a non-monotone curve monotone] + TWO SHOWSTOPPERs — G6 entity-coverage + G1 recall-floor MASK via NESTED `per_entity_recall` bool/+inf/0.0 values [close-9, the new `_detected_entity_names` helper]) + 6 crashes/DoV (incl. close-10's CLI `IsADirectoryError`). The final **round-8 confirmatory close `wf_3239f1fa-0c4` = RECLOSE_PASS — 0 upheld / 517 probes**, both fabrication refuters completed (round-7's had errored without emitting StructuredOutput; round-8 SPLIT the fabrication hunt into flat-scalar + nested-value refuters + an explicit StructuredOutput mandate), off-limits byte-identical. ★★★ **THE CLOSE IS THE ONLY RELIABLE CERTIFIER — the agent's manual audit dismissed BOTH close-9 SHOWSTOPPERs as "not a fabrication" before the close caught them; S7-02 was declared DONE ONLY on the 0-upheld close where the fabrication refuters COMPLETED, never on an audit.**
>
> **★ The honest produced-artifact verdict: `NOT_YET`, `canonical_claim_run=True`, binding `G6 FAIL`** (core F2 0.7214 < best Tier-R gliner 0.75; entity coverage 0.824 ≥0.80). Per-guarantee **G1 PASS · G2 PASS · G3 PASS · G4 PASS · G5 PENDING · G6 FAIL · G7 PASS**; J=0.0 (mle-bootstrap). G1 PASS confirms the `_BENCHMARK_IGNORE` producer fix (the emit-only strip corrected G6 coverage 0.778→0.824 + removed a spurious G1 FAIL on a non-entity; composite/J byte-identical; RED `ea45c0e`→GREEN `0d776d3`). **DECISION A — G6 FAIL is NOT a regression:** old code `2761a27` ≡ current HEAD byte-identical at `use_case=default`; pii-anon WINS rank-1 COMPOSITE (the pii-rate-elo headline) but loses raw F2 to gliner on the current non-deterministic dataset draw — G6 is draw-sensitive (`05-testing/_diagnostics/f2-gap-attribution.md`). Do NOT "fix" F2; do NOT weaken G6.
>
> **SDO — the single binding constraint.** On the COMMITTED smoke artifact (`pii-anon supremacy` default): **NOT_YET / `canonical_claim_run=False` (G7)** — it predates the producer. On a freshly PRODUCED certified artifact: **NOT_YET / G6 FAIL**. Neither is "fixable" without the full-census Pass-2; both are honest. Gate md5 `1f327dd7dfad55551c87a0b9c8dfe188` (UNCHANGED since close-9; close-10 is cli.py only); producer `e47b2752d2e88aba5182d880bc9704b7`; off-limits byte-identical; user-WIP byte-identical (S2-03 stays BLOCKED — orchestrator.py unchanged). Full suite exit 0, cov 88.56% (~3582 pass / 15 skip / 0 fail), ruff + BOTH-mypy clean. Signed off `_signoffs/SO-15-keystone-close.yaml`.
>
> **NEXT (priority order):** (1) **S7-04** — commit NFR-009 numeric latency ceilings (p50/p95/p99 per power class/profile) + wire the gate `_g5_*` (currently a `_pending_guarantee` placeholder). A `competitive_supremacy.py` change ⇒ **MANDATORY adversarial close**; will NOT flip the verdict (G6 binds) — completeness, not a flip. (2) Feature surface SEQUENTIAL in the main tree (worktree parallel dispatch is UNRELIABLE here): S5-02 (Tier-3 representative), S5-03 (MIA representative), S6-01 (query-aware masking; the FR-028/UC-22 6-channel vs FR-025/DC-13 4-channel reconciliation is a tracked PO item), S6-04 (BYO-SDK), S7-01 (multimodal readers), S7-03 (multilingual fairness) — representative + Pass-2-flagged; via `/dev-assist-story-new`. (3) RC close: Stage-6 docs (distinct anon/pseudo APIs + vanilla-vs-swarm positioning) → release gate (honest SHIP-WITH-CAVEATS given G6/NOT_YET) → packaging extras → tagged RC on `pdlc/sota-program` → build sdist+wheel (NOT published). **Pass-2 follow-ons:** ★ full-census G6 run on current code (headline) + real Tier-C cloud runs + real-NUTS J (numpyro/jax absent) + DATA bradley_terry/canary/query-aware + Papers 1 & 2 (siblings `../pii-anon-eval-data` + `../pii-anon-research-paper` are FLAGGED FOLLOW-ON — code-only this run).

## Pivots Log

| Date | Stage(s) affected | Source finding | Plan file |
|---|---|---|---|
| _(empty)_ | | | |

## Directory Structure

```
dev-assist-artifacts/
├── MANIFEST.md                           ← this file (status tracker)
├── 00-axioms/                            ← project-axioms.yaml (6 project-authored axioms)
├── 00-security/                          ← security-checklist + exceptions (no-real-PII invariant)
├── 00-brownfield-assessment/             ← Stage 0 gap analysis (created by /dev-assist-assess)
├── 00-validation/                        ← validator reports
├── _signoffs/                            ← sign-off records (po-required)
├── 01-discovery/                         ← Stage 1 outputs
├── 02-requirements/                      ← Stage 2 outputs
├── 03-design/                            ← Stage 3 outputs
├── 04-development/                       ← Stage 4 outputs
└── 05-testing/                           ← Stage 5 outputs
```

Sibling files:
- `developer-assistant.yaml` — plugin configuration.

## Agent Deployment Ledger

| Stage | Agents deployed | Running total |
|---|---|---|
| Brownfield (M1 assess) | 7 | 7 |
| Discovery | 30 | 37 |
| Requirements | 22 | 59 |
| Design | 17 | 76 |
| Development | 135 (S1–S3-03: 40; S3-04 5 + S4-CS-01 12; **security-MUSTs session 38**: 3 explore + 2 explore[S4-01/03 pre-map] + 3 iter-1 executors + 15 story reviewers + 2 iter-2 executors + adversarial-close wf 8 + re-close wf 5; **G2/G4 session 23**: S4-01 executor + 5 reviewers + S4-03 executor + 5 reviewers + S4-03 iter-2 amend executor + 1 code-quality re-review + G2/G4-close wf 3 + hardening executor + re-close wf 5; **S2 MoE-router-core session 17**: 1 explore[seam map] + S2-01 executor + 5 reviewers + S2-02 executor + 5 reviewers + S2-02 control-path adversarial-close wf 4) | 211 |
| Testing | 0 | 211 |
| **Total** | **211** | **211** |

---

_Scaffolded by developer-assistant `/dev-assist-start` for the library pillar of the 3-repo pii-anon SOTA program._
